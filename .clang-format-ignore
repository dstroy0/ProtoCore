# clang-format cannot parse .ino: it infers Objective-C and this config declares only
# Language: Cpp, so every sketch errors out. It also sorts includes, which pushes a
# src/-relative include above protocore.h - and Arduino resolves the library by scanning
# the sketch includes IN ORDER, so that reordering makes 131 of 152 sketches uncompilable.
examples/**/*.ino
penetration_testing/**/*.ino

# Generated theme/asset blobs (src/web_assets/wizard/gen_theme_blobs.py, build_assets.py). Machine
# output regenerated on every run, so formatting it by hand is undone immediately.
src/network_drivers/application/binary_asset_blobs.c
