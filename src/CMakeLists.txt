# ProtoCore v1.0.16 - Copyright (C) 2026 Douglas Quigg (dstroy0) <dquigg123@gmail.com>
# SPDX-License-Identifier: AGPL-3.0-or-later
#
# GENERATED by tools/ci_tooling/build/gen_modules.py. Do not edit.
# Regenerate with:  python tools/harness.py build modules
#
# One target per module - a .c under src/ and the .h beside it. The dependencies are read out of what
# each module includes, resolved back to whichever module owns that header, so nothing here is
# maintained by hand.
#
# OBJECT libraries: a module contributes its objects to whatever links it, which is the same single
# link the flat source list produced, while still carrying its include directories and its
# dependencies as usage requirements. The header graph is acyclic, which is what OBJECT requires -
# `build modules --cycles` checks it, and only header edges can close a loop. Two .c files calling
# each other's published handle is ordinary C and is PRIVATE, so it is not part of that graph.

# Everything compiles against the entry point and the primitive types it assembles.
add_library(pc_config INTERFACE)
# The same set every env compiles with (tools/ci_tooling/build/gen_cmake.py BASE_INCLUDES): the host
# arm answers the platform seams in software, so a host build of the library reaches it the same way
# a suite does.
target_include_directories(pc_config INTERFACE
  "${PROTOCORE_ROOT}/test/core_setup/hal/host"
  "${PROTOCORE_ROOT}/test/support"
  "${PROTOCORE_ROOT}/src"
  "${PROTOCORE_ROOT}/include"
  "${PROTOCORE_ROOT}")

# One module. DEPS are other modules; each is PUBLIC, so a consumer that links this one also gets
# their headers and their own dependencies without naming them.
function(protocore_module name)
  cmake_parse_arguments(M "" "" "SOURCES;PUBLIC;PRIVATE" ${ARGN})
  add_library(${name} OBJECT ${M_SOURCES})
  target_link_libraries(${name} PUBLIC pc_config ${M_PUBLIC})
  if(M_PRIVATE)
    target_link_libraries(${name} PRIVATE ${M_PRIVATE})
  endif()
endfunction()

# A module with no .c beside its .h: header-only, and a consumer still depends on it. INTERFACE, so
# it contributes no objects but carries its own dependencies onward exactly as a compiled one does.
function(protocore_header_module name)
  cmake_parse_arguments(M "" "" "DEPS" ${ARGN})
  add_library(${name} INTERFACE)
  target_link_libraries(${name} INTERFACE pc_config ${M_DEPS})
endfunction()

protocore_header_module(pc_crypto_asymmetric_ed25519_comb_table)
protocore_header_module(pc_crypto_cipher_aes_sbox)
protocore_header_module(pc_crypto_crypto_opt)
protocore_module(pc_crypto_x509_x509
  SOURCES "${PROTOCORE_ROOT}/src/crypto/x509/x509.c"
  PRIVATE
    pc_mmgr_protomem
    pc_shared_der_der
)
protocore_module(pc_crypto_x509_x509_verify
  SOURCES "${PROTOCORE_ROOT}/src/crypto/x509/x509_verify.c"
  PUBLIC
    pc_crypto_x509_x509
  PRIVATE
    pc_crypto_asymmetric_ecdsa
    pc_crypto_asymmetric_ed25519
    pc_crypto_asymmetric_rsa
    pc_mmgr_protomem
    pc_mmgr_secure
    pc_shared_der_der
)
protocore_module(pc_mmgr_arena
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/arena.c"
  PRIVATE
    pc_mmgr_protomem
)
protocore_module(pc_mmgr_bitio
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/bitio.c"
)
protocore_module(pc_mmgr_bytes
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/bytes.c"
  PUBLIC
    pc_mmgr_endian
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_mmgr_span
)
protocore_module(pc_mmgr_endian
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/endian.c"
)
protocore_module(pc_mmgr_float_bits
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/float_bits.c"
  PRIVATE
    pc_mmgr_rawmemcpy
)
protocore_module(pc_mmgr_membuild
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/membuild.c"
  PRIVATE
    pc_mmgr_float_bits
    pc_mmgr_protostr
    pc_mmgr_rawmemcpy
    pc_shared_hex_hex
)
protocore_module(pc_mmgr_plaintext
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/plaintext.c"
  PUBLIC
    pc_mmgr_span
  PRIVATE
    pc_mmgr_arena
)
protocore_module(pc_mmgr_protoframe
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/protoframe.c"
  PUBLIC
    pc_mmgr_membuild
  PRIVATE
    pc_mmgr_protostr
    pc_shared_speed_opt_speed_opt
)
protocore_module(pc_mmgr_protomem
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/protomem.c"
  PUBLIC
    pc_mmgr_rawmemcpy
  PRIVATE
    pc_mmgr_swar
)
protocore_module(pc_mmgr_protostr
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/protostr.c"
  PUBLIC
    pc_mmgr_swar
)
protocore_module(pc_mmgr_rawmemcpy
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/rawmemcpy.c"
)
protocore_header_module(pc_mmgr_ring DEPS pc_mmgr_rawmemcpy pc_mmgr_span)
protocore_module(pc_mmgr_secure
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/secure.c"
  PUBLIC
    pc_mmgr_protomem
    pc_mmgr_span
  PRIVATE
    pc_mmgr_arena
)
protocore_module(pc_mmgr_span
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/span.c"
)
protocore_module(pc_mmgr_swar
  SOURCES "${PROTOCORE_ROOT}/src/mmgr/swar.c"
  PUBLIC
    pc_mmgr_rawmemcpy
)
protocore_module(pc_network_drivers_application_binary_asset_blobs
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/binary_asset_blobs.c"
)
protocore_header_module(pc_network_drivers_application_ntp_ntp)
protocore_module(pc_network_drivers_application_web_assets
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/web_assets.c"
)
protocore_module(pc_network_drivers_datalink_datalink
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/datalink/datalink.c"
)
protocore_module(pc_network_drivers_network_dns_dns_resolver
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/dns/dns_resolver.c"
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_mmgr_rawmemcpy
    pc_mmgr_secure
    pc_network_drivers_network_dns_dns_wire
    pc_network_drivers_transport_udp_server_server
    pc_server_clock_clock
    pc_shared_ip_ip
)
protocore_module(pc_network_drivers_network_dns_dns_wire
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/dns/dns_wire.c"
  PRIVATE
    pc_mmgr_rawmemcpy
)
protocore_module(pc_network_drivers_physical_physical
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/physical/physical.c"
  PUBLIC
    pc_shared_ip_ip
  PRIVATE
    pc_mmgr_plaintext
    pc_network_drivers_physical_radio_power
)
protocore_module(pc_network_drivers_presentation_codec_base64_base64
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/base64/base64.c"
  PRIVATE
    pc_mmgr_protostr
    pc_mmgr_swar
)
protocore_module(pc_network_drivers_presentation_codec_cbor_cbor
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/cbor/cbor.c"
  PUBLIC
    pc_mmgr_span
    pc_network_drivers_presentation_codec_codec
  PRIVATE
    pc_mmgr_bytes
    pc_mmgr_protomem
    pc_mmgr_protostr
)
protocore_header_module(pc_network_drivers_presentation_codec_codec DEPS pc_mmgr_span)
protocore_module(pc_network_drivers_presentation_codec_deflate_rfc1951
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/deflate/rfc1951.c"
  PUBLIC
    pc_mmgr_bitio
    pc_mmgr_protomem
)
protocore_module(pc_network_drivers_presentation_codec_hpack_prim_hpack_prim
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/hpack_prim/hpack_prim.c"
  PRIVATE
    pc_mmgr_protomem
)
protocore_module(pc_network_drivers_presentation_codec_json_json
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/json/json.c"
  PRIVATE
    pc_mmgr_membuild
    pc_mmgr_protostr
    pc_shared_hex_hex
)
protocore_module(pc_network_drivers_presentation_codec_multipart_multipart
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/multipart/multipart.c"
  PUBLIC
    pc_network_drivers_presentation_presentation
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
)
protocore_module(pc_network_drivers_presentation_http_http2_h2_server
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http2/h2_server.c"
  PRIVATE
    pc_mmgr_protomem
    pc_network_drivers_presentation_http_http2_h2_conn
    pc_network_drivers_presentation_http_http_parser_http_parser
    pc_network_drivers_session_session
    pc_network_drivers_tls_tls
    pc_network_drivers_transport_tcp_tcp
)
protocore_module(pc_network_drivers_presentation_http_route_http_route
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/route/http_route.c"
  PUBLIC
    pc_network_drivers_presentation_http_http
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_secure
)
protocore_header_module(pc_network_drivers_presentation_ssh_common DEPS pc_crypto_aead_chachapoly pc_crypto_asymmetric_bignum pc_crypto_cipher_aes256ctr pc_crypto_mac_hmac_sha256 pc_crypto_pqc_sntrup761 pc_mmgr_bytes pc_mmgr_protostr pc_network_drivers_presentation_ssh_transport_ssh_kexhash)
protocore_module(pc_network_drivers_presentation_ssh_ssh
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/ssh.c"
  PUBLIC
    pc_network_drivers_presentation_ssh_common
)
protocore_module(pc_network_drivers_presentation_ssh_transport_extension
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/extension.c"
  PUBLIC
    pc_network_drivers_presentation_ssh_common
  PRIVATE
    pc_network_drivers_presentation_ssh_transport_transport
)
protocore_module(pc_network_drivers_presentation_ssh_transport_phase_machine
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/phase_machine.c"
  PUBLIC
    pc_network_drivers_presentation_ssh_common
  PRIVATE
    pc_network_drivers_presentation_ssh_transport_transport
)
protocore_module(pc_network_drivers_presentation_ssh_transport_ssh_kexhash
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/ssh_kexhash.c"
  PRIVATE
    pc_crypto_hash_sha256
    pc_crypto_hash_sha512
)
protocore_module(pc_network_drivers_presentation_ssh_transport_ssh_rsa
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/ssh_rsa.c"
  PUBLIC
    pc_crypto_asymmetric_rsa
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_secure
)
protocore_module(pc_network_drivers_tls_handshake_handshake
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/tls/handshake/handshake.c"
  PRIVATE
    pc_crypto_asymmetric_curve25519
    pc_crypto_asymmetric_ed25519
    pc_crypto_ct_eq
    pc_crypto_x509_x509
    pc_crypto_x509_x509_verify
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_mmgr_rawmemcpy
    pc_mmgr_secure
    pc_network_drivers_tls_tls
)
protocore_module(pc_network_drivers_tls_key_schedule_key_schedule
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/tls/key_schedule/key_schedule.c"
  PRIVATE
    pc_crypto_hash_sha256
    pc_crypto_hash_sha384
    pc_crypto_kdf_hkdf
    pc_crypto_kdf_hkdf_sha384
    pc_crypto_mac_hmac_sha256
    pc_crypto_mac_hmac_sha384
)
protocore_module(pc_network_drivers_tls_record_record
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/tls/record/record.c"
  PUBLIC
    pc_crypto_aead_aes128gcm
    pc_crypto_aead_aesgcm
    pc_network_drivers_presentation_http_http3_tls13_msg
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_secure
    pc_network_drivers_tls_key_schedule_key_schedule
)
protocore_module(pc_network_drivers_tls_tls
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/tls/tls.c"
  PUBLIC
    pc_network_drivers_presentation_http_http3_tls13_msg
    pc_network_drivers_tls_key_schedule_key_schedule
    pc_network_drivers_tls_record_record
  PRIVATE
    pc_crypto_rng_rng
    pc_mmgr_secure
    pc_network_drivers_transport_tcp_protocol_protocol
)
protocore_module(pc_network_drivers_transport_net_addr_net_addr
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/net_addr/net_addr.c"
  PUBLIC
    pc_shared_ip_ip
  PRIVATE
    pc_mmgr_rawmemcpy
)
protocore_header_module(pc_network_drivers_transport_tcp_common DEPS pc_mmgr_ring pc_network_drivers_transport_tcp_evt pc_shared_ip_ip)
protocore_header_module(pc_network_drivers_transport_tcp_evt)
protocore_module(pc_network_drivers_transport_tcp_tcp
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/tcp/tcp.c"
  PUBLIC
    pc_network_drivers_transport_tcp_client_client
    pc_network_drivers_transport_tcp_protocol_protocol
    pc_network_drivers_transport_tcp_server_server
)
protocore_header_module(pc_network_drivers_transport_udp_common DEPS pc_mmgr_bytes pc_mmgr_ring pc_shared_ip_ip)
protocore_module(pc_network_drivers_transport_udp_udp
  SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/udp/udp.c"
  PUBLIC
    pc_network_drivers_transport_udp_client_client
    pc_network_drivers_transport_udp_server_server
)
protocore_module(pc_server_clock_clock
  SOURCES "${PROTOCORE_ROOT}/src/server/clock/clock.c"
)
protocore_module(pc_server_core_exc_coredump
  SOURCES "${PROTOCORE_ROOT}/src/server/core/exc_coredump.c"
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_server_core_exc_decoder
)
protocore_header_module(pc_server_core_proto_handler)
protocore_module(pc_server_io_middleware
  SOURCES "${PROTOCORE_ROOT}/src/server/io/middleware.c"
  PRIVATE
    pc_mmgr_protoframe
    pc_server_clock_clock
    pc_shared_mime_mime
)
protocore_header_module(pc_server_peripherals_i2c)
protocore_header_module(pc_server_peripherals_spi)
protocore_header_module(pc_server_peripherals_uart)
protocore_module(pc_server_regex
  SOURCES "${PROTOCORE_ROOT}/src/server/regex.c"
)
protocore_module(pc_server_signaling_signaling
  SOURCES "${PROTOCORE_ROOT}/src/server/signaling/signaling.c"
  PRIVATE
    pc_mmgr_plaintext
    pc_network_drivers_transport_tcp_protocol_protocol
    pc_network_drivers_transport_tcp_tcp
)
protocore_module(pc_server_storage_filesystem
  SOURCES "${PROTOCORE_ROOT}/src/server/storage/filesystem.c"
  PUBLIC
    pc_mmgr_protoframe
    pc_server_storage_mnt
  PRIVATE
    pc_mmgr_plaintext
    pc_mmgr_protomem
    pc_mmgr_protostr
)
protocore_module(pc_server_update_ota_service
  SOURCES "${PROTOCORE_ROOT}/src/server/update/ota_service.c"
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_network_drivers_presentation_codec_base64_base64
    pc_network_drivers_presentation_http_http_parser_http_parser
    pc_server_clock_clock
    pc_shared_mime_mime
)
protocore_module(pc_services_fieldbus_j1939_j1939
  SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/j1939/j1939.c"
  PUBLIC
    pc_shared_can_can
  PRIVATE
    pc_mmgr_plaintext
    pc_mmgr_protomem
)
protocore_module(pc_services_iot_coap_coaps
  SOURCES "${PROTOCORE_ROOT}/src/services/iot/coap/coaps.c"
  PUBLIC
    pc_network_drivers_presentation_security_dtls_dtls_conn
  PRIVATE
    pc_services_iot_coap_coap
)
protocore_module(pc_services_iot_coap_coaps_server
  SOURCES "${PROTOCORE_ROOT}/src/services/iot/coap/coaps_server.c"
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
    pc_mmgr_ring
    pc_mmgr_secure
    pc_network_drivers_presentation_security_dtls_dtls_conn
    pc_network_drivers_transport_udp_server_server
    pc_server_clock_clock
    pc_services_iot_coap_coaps
    pc_shared_ip_ip
)
protocore_module(pc_services_iot_protobuf_protobuf
  SOURCES "${PROTOCORE_ROOT}/src/services/iot/protobuf/protobuf.c"
  PRIVATE
    pc_mmgr_plaintext
    pc_mmgr_protomem
    pc_mmgr_protostr
)
protocore_module(pc_services_southbound_sb_modbus
  SOURCES "${PROTOCORE_ROOT}/src/services/southbound/sb_modbus.c"
  PUBLIC
    pc_services_fieldbus_modbus_modbus
    pc_services_southbound_southbound
  PRIVATE
    pc_services_fieldbus_modbus_modbus_master
)
protocore_header_module(pc_services_storage_wal_wal_fs DEPS pc_mmgr_protomem pc_server_storage_mnt pc_services_storage_wal_wal_store)
protocore_module(pc_services_timing_position_nmea0183_nmea0183
  SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/nmea0183/nmea0183.c"
  PRIVATE
    pc_mmgr_protomem
    pc_mmgr_protostr
)
protocore_header_module(pc_shared_can_can)
protocore_module(pc_shared_crc_crc
  SOURCES "${PROTOCORE_ROOT}/src/shared/crc/crc.c"
)
protocore_module(pc_shared_der_der
  SOURCES "${PROTOCORE_ROOT}/src/shared/der/der.c"
  PRIVATE
    pc_mmgr_protomem
)
protocore_module(pc_shared_hex_hex
  SOURCES "${PROTOCORE_ROOT}/src/shared/hex/hex.c"
)
protocore_module(pc_shared_http_date_http_date
  SOURCES "${PROTOCORE_ROOT}/src/shared/http_date/http_date.c"
  PUBLIC
    pc_shared_time_compat_time_compat
  PRIVATE
    pc_mmgr_membuild
)
protocore_module(pc_shared_ip_ip
  SOURCES "${PROTOCORE_ROOT}/src/shared/ip/ip.c"
  PRIVATE
    pc_mmgr_protomem
    pc_shared_hex_hex
)
protocore_header_module(pc_shared_mime_mime)
protocore_module(pc_shared_pcap_pcap
  SOURCES "${PROTOCORE_ROOT}/src/shared/pcap/pcap.c"
  PUBLIC
    pc_mmgr_endian
)
protocore_header_module(pc_shared_speed_opt_speed_opt)
protocore_module(pc_shared_time_compat_time_compat
  SOURCES "${PROTOCORE_ROOT}/src/shared/time_compat/time_compat.c"
)
protocore_module(pc_shared_utf8_utf8
  SOURCES "${PROTOCORE_ROOT}/src/shared/utf8/utf8.c"
)

# PROTOCORE_ENABLE_ACCEPT_THROTTLE
if(PROTOCORE_ENABLE_ACCEPT_THROTTLE)
  protocore_module(pc_network_drivers_transport_tcp_server_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/tcp/server/server.c"
    PUBLIC
      pc_network_drivers_transport_tcp_evt
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_plaintext
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_diffserv_diffserv
      pc_network_drivers_transport_net_addr_net_addr
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_lower_lower
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_server_core_worker
  )
endif()

# PROTOCORE_ENABLE_AD9238
if(PROTOCORE_ENABLE_AD9238)
  protocore_module(pc_server_peripherals_ad9238_ad9238
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/ad9238/ad9238.c"
  )
endif()

# PROTOCORE_ENABLE_ADS
if(PROTOCORE_ENABLE_ADS)
  protocore_module(pc_services_fieldbus_ads_ads
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/ads/ads.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_ADS1115
if(PROTOCORE_ENABLE_ADS1115)
  protocore_module(pc_server_peripherals_ads1115_ads1115
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/ads1115/ads1115.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_server_clock_clock
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_AES128GCM
if(PROTOCORE_ENABLE_AES128GCM)
  protocore_module(pc_crypto_aead_aes128gcm
    SOURCES "${PROTOCORE_ROOT}/src/crypto/aead/aes128gcm.c"
    PRIVATE
      pc_crypto_cipher_aes_block
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_crypto_mac_ghash
      pc_mmgr_protomem
      pc_mmgr_rawmemcpy
      pc_mmgr_secure
  )
endif()

# PROTOCORE_ENABLE_AES256CTR
if(PROTOCORE_ENABLE_AES256CTR)
  protocore_module(pc_crypto_cipher_aes256ctr
    SOURCES "${PROTOCORE_ROOT}/src/crypto/cipher/aes256ctr.c"
    PRIVATE
      pc_crypto_cipher_aes_block
      pc_crypto_crypto_opt
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_AESCCM
if(PROTOCORE_ENABLE_AESCCM)
  protocore_module(pc_crypto_aead_aesccm
    SOURCES "${PROTOCORE_ROOT}/src/crypto/aead/aesccm.c"
    PRIVATE
      pc_crypto_cipher_aes_block
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_AESGCM
if(PROTOCORE_ENABLE_AESGCM)
  protocore_module(pc_crypto_aead_aesgcm
    SOURCES "${PROTOCORE_ROOT}/src/crypto/aead/aesgcm.c"
    PRIVATE
      pc_crypto_cipher_aes_block
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_crypto_mac_ghash
      pc_mmgr_protomem
      pc_mmgr_rawmemcpy
  )
endif()

# PROTOCORE_ENABLE_AES_BLOCK
if(PROTOCORE_ENABLE_AES_BLOCK)
  protocore_module(pc_crypto_cipher_aes_block
    SOURCES "${PROTOCORE_ROOT}/src/crypto/cipher/aes_block.c"
    PUBLIC
      pc_crypto_cipher_aes_sbox
      pc_mmgr_protomem
    PRIVATE
      pc_crypto_crypto_opt
  )
endif()

# PROTOCORE_ENABLE_AES_CMAC
if(PROTOCORE_ENABLE_AES_CMAC)
  protocore_module(pc_crypto_mac_aes_cmac
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/aes_cmac.c"
    PRIVATE
      pc_crypto_cipher_aes_block
      pc_crypto_crypto_opt
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_AMQP
if(PROTOCORE_ENABLE_AMQP)
  protocore_module(pc_services_iot_amqp_amqp
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/amqp/amqp.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_ATC
if(PROTOCORE_ENABLE_ATC)
  protocore_module(pc_services_machine_tool_atc_atc
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/atc/atc.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_AUDIT_LOG
if(PROTOCORE_ENABLE_AUDIT_LOG)
  protocore_module(pc_server_security_audit_log_audit_log
    SOURCES "${PROTOCORE_ROOT}/src/server/security/audit_log/audit_log.c"
    PRIVATE
      pc_crypto_hash_sha256
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_server_clock_clock
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_AUTH
if(PROTOCORE_ENABLE_AUTH)
  protocore_module(pc_network_drivers_presentation_http_auth_auth
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/auth/auth.c"
    PRIVATE
      pc_crypto_ct_eq
      pc_crypto_hash_sha256
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_presentation_codec_base64_base64
      pc_network_drivers_presentation_http_http
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_server_clock_clock
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_AUTH_LOCKOUT
if(PROTOCORE_ENABLE_AUTH_LOCKOUT)
  protocore_module(pc_network_drivers_presentation_http_http
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http.c"
    PUBLIC
      pc_network_drivers_presentation_http_http_parser_http_parser
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_network_drivers_presentation_http_auth_auth
      pc_network_drivers_presentation_http_route_http_route
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_server_clock_clock
      pc_server_io_webdav_handler
      pc_server_security_auth_lockout_auth_lockout
      pc_server_security_csrf_csrf
      pc_server_security_forwarded_trust_forwarded_trust
  )
  protocore_module(pc_server_security_auth_lockout_auth_lockout
    SOURCES "${PROTOCORE_ROOT}/src/server/security/auth_lockout/auth_lockout.c"
    PRIVATE
      pc_mmgr_secure
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_BACNET
if(PROTOCORE_ENABLE_BACNET)
  protocore_module(pc_services_fieldbus_bacnet_bacnet
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/bacnet/bacnet.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_BIGNUM
if(PROTOCORE_ENABLE_BIGNUM)
  protocore_module(pc_crypto_asymmetric_bignum
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/bignum.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_BLE_GATT
if(PROTOCORE_ENABLE_BLE_GATT)
  protocore_module(pc_services_radio_ble_gatt_ble_gatt
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/ble_gatt/ble_gatt.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_BUS_CAPTURE
if(PROTOCORE_ENABLE_BUS_CAPTURE)
  protocore_module(pc_server_signaling_bus_capture
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/bus_capture.c"
    PUBLIC
      pc_shared_can_can
    PRIVATE
      pc_mmgr_secure
      pc_shared_pcap_pcap
  )
endif()

# PROTOCORE_ENABLE_C37118
if(PROTOCORE_ENABLE_C37118)
  protocore_module(pc_services_energy_c37118_c37118
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/c37118/c37118.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_CANOPEN
if(PROTOCORE_ENABLE_CANOPEN)
  protocore_module(pc_services_fieldbus_canopen_canopen
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/canopen/canopen.c"
    PUBLIC
      pc_shared_can_can
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_CC1101
if(PROTOCORE_ENABLE_CC1101)
  protocore_module(pc_services_radio_cc1101_cc1101
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/cc1101/cc1101.c"
  )
endif()

# PROTOCORE_ENABLE_CCLINK
if(PROTOCORE_ENABLE_CCLINK)
  protocore_module(pc_services_fieldbus_cclink_cclink
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/cclink/cclink.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_CHACHA20
if(PROTOCORE_ENABLE_CHACHA20)
  protocore_module(pc_crypto_cipher_chacha20
    SOURCES "${PROTOCORE_ROOT}/src/crypto/cipher/chacha20.c"
    PRIVATE
      pc_crypto_crypto_opt
  )
endif()

# PROTOCORE_ENABLE_CHACHAPOLY
if(PROTOCORE_ENABLE_CHACHAPOLY)
  protocore_module(pc_crypto_aead_chachapoly
    SOURCES "${PROTOCORE_ROOT}/src/crypto/aead/chachapoly.c"
    PRIVATE
      pc_crypto_cipher_chacha20
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_crypto_mac_poly1305
  )
endif()

# PROTOCORE_ENABLE_CIA402
if(PROTOCORE_ENABLE_CIA402)
  protocore_module(pc_services_fieldbus_cia402_cia402
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/cia402/cia402.c"
    PUBLIC
      pc_shared_can_can
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_services_fieldbus_canopen_canopen
  )
endif()

# PROTOCORE_ENABLE_CIP
if(PROTOCORE_ENABLE_CIP)
  protocore_module(pc_services_fieldbus_cip_cip
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/cip/cip.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_CLOUDEVENTS
if(PROTOCORE_ENABLE_CLOUDEVENTS)
  protocore_module(pc_services_iot_cloudevents_cloudevents
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/cloudevents/cloudevents.c"
    PUBLIC
      pc_network_drivers_presentation_http_http_parser_http_parser
    PRIVATE
      pc_network_drivers_presentation_codec_json_json
  )
endif()

# PROTOCORE_ENABLE_COAP
if(PROTOCORE_ENABLE_COAP)
  protocore_module(pc_services_iot_coap_coap
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/coap/coap.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_CONFIG_IO
if(PROTOCORE_ENABLE_CONFIG_IO)
  protocore_module(pc_server_storage_config_io_config_io
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/config_io/config_io.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_server_storage_config_store_config_store
  )
endif()

# PROTOCORE_ENABLE_CONFIG_STORE
if(PROTOCORE_ENABLE_CONFIG_STORE)
  protocore_module(pc_server_storage_config_store_config_store
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/config_store/config_store.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
  )
endif()

# PROTOCORE_ENABLE_CONTROL
if(PROTOCORE_ENABLE_CONTROL)
  protocore_module(pc_services_system_control_control
    SOURCES "${PROTOCORE_ROOT}/src/services/system/control/control.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_COTP
if(PROTOCORE_ENABLE_COTP)
  protocore_module(pc_services_fieldbus_cotp_cotp
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/cotp/cotp.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_CSRF
if(PROTOCORE_ENABLE_CSRF)
  protocore_module(pc_server_security_csrf_csrf
    SOURCES "${PROTOCORE_ROOT}/src/server/security/csrf/csrf.c"
    PRIVATE
      pc_crypto_ct_eq
      pc_crypto_mac_hmac_sha256
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_CT_EQ
if(PROTOCORE_ENABLE_CT_EQ)
  protocore_module(pc_crypto_ct_eq
    SOURCES "${PROTOCORE_ROOT}/src/crypto/ct_eq.c"
    PUBLIC
      pc_mmgr_protomem
    PRIVATE
      pc_crypto_crypto_opt
  )
endif()

# PROTOCORE_ENABLE_CURVE25519
if(PROTOCORE_ENABLE_CURVE25519)
  protocore_module(pc_crypto_asymmetric_curve25519
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/curve25519.c"
    PRIVATE
      pc_crypto_asymmetric_fe25519
      pc_crypto_crypto_opt
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DASHBOARD
if(PROTOCORE_ENABLE_DASHBOARD)
  protocore_module(pc_server_web_dashboard_dashboard
    SOURCES "${PROTOCORE_ROOT}/src/server/web/dashboard/dashboard.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protoframe
      pc_mmgr_protostr
      pc_mmgr_secure
  )
  protocore_module(pc_server_web_dashboard_dashboard_routes
    SOURCES "${PROTOCORE_ROOT}/src/server/web/dashboard/dashboard_routes.c"
    PRIVATE
      pc_network_drivers_application_web_assets
      pc_network_drivers_presentation_http_websocket_websocket
      pc_server_web_dashboard_dashboard
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_DBM
if(PROTOCORE_ENABLE_DBM)
  protocore_module(pc_services_storage_dbm_dbm
    SOURCES "${PROTOCORE_ROOT}/src/services/storage/dbm/dbm.c"
    PUBLIC
      pc_services_storage_wal_wal_store
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DDS
if(PROTOCORE_ENABLE_DDS)
  protocore_module(pc_services_iot_dds_dds
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/dds/dds.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DEVICENET
if(PROTOCORE_ENABLE_DEVICENET)
  protocore_module(pc_services_fieldbus_devicenet_devicenet
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/devicenet/devicenet.c"
    PUBLIC
      pc_shared_can_can
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DEVICE_ID
if(PROTOCORE_ENABLE_DEVICE_ID)
  protocore_module(pc_server_signaling_device_id
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/device_id.c"
    PRIVATE
      pc_crypto_hash_sha1
      pc_mmgr_secure
      pc_mmgr_span
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_DF1
if(PROTOCORE_ENABLE_DF1)
  protocore_module(pc_services_fieldbus_df1_df1
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/df1/df1.c"
    PRIVATE
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_DIFFSERV
if(PROTOCORE_ENABLE_DIFFSERV)
  protocore_module(pc_network_drivers_transport_diffserv_diffserv
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/diffserv/diffserv.c"
    PRIVATE
      pc_mmgr_plaintext
  )
  protocore_module(pc_network_drivers_transport_tcp_client_client
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/tcp/client/client.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_ring
      pc_network_drivers_network_dns_dns_resolver
      pc_network_drivers_transport_diffserv_diffserv
      pc_network_drivers_transport_tcp_lower_lower
      pc_server_clock_clock
  )
  protocore_module(pc_network_drivers_transport_tcp_lower_lower
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/tcp/lower/lower.c"
    PUBLIC
      pc_network_drivers_transport_tcp_common
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_diffserv_diffserv
      pc_network_drivers_transport_tcp_protocol_protocol
  )
  protocore_module(pc_network_drivers_transport_udp_client_client
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/udp/client/client.c"
    PUBLIC
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_rawmemcpy
      pc_network_drivers_transport_diffserv_diffserv
      pc_network_drivers_transport_net_addr_net_addr
  )
  protocore_module(pc_network_drivers_transport_udp_server_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/udp/server/server.c"
    PUBLIC
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_plaintext
      pc_network_drivers_transport_diffserv_diffserv
      pc_network_drivers_transport_net_addr_net_addr
      pc_network_drivers_transport_udp_common
  )
endif()

# PROTOCORE_ENABLE_DIRECTNET
if(PROTOCORE_ENABLE_DIRECTNET)
  protocore_module(pc_services_fieldbus_directnet_directnet
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/directnet/directnet.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DMA
if(PROTOCORE_ENABLE_DMA)
  protocore_module(pc_mmgr_dma
    SOURCES "${PROTOCORE_ROOT}/src/mmgr/dma.c"
  )
endif()

# PROTOCORE_ENABLE_DMX
if(PROTOCORE_ENABLE_DMX)
  protocore_module(pc_server_peripherals_dmx_dmx
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/dmx/dmx.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DNC
if(PROTOCORE_ENABLE_DNC)
  protocore_module(pc_services_machine_tool_dnc_dnc
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/dnc/dnc.c"
  )
  protocore_module(pc_services_machine_tool_dnc_dnc_stream
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/dnc/dnc_stream.c"
    PUBLIC
      pc_services_machine_tool_dnc_dnc
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_DNP3
if(PROTOCORE_ENABLE_DNP3)
  protocore_module(pc_services_energy_dnp3_dnp3
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/dnp3/dnp3.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_DNS_SERVER
if(PROTOCORE_ENABLE_DNS_SERVER)
  protocore_module(pc_network_drivers_network_dns_dns
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/dns/dns.c"
    PUBLIC
      pc_network_drivers_network_dns_dns_resolver
      pc_network_drivers_network_dns_dns_server
  )
  protocore_module(pc_network_drivers_network_dns_dns_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/dns/dns_server.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_network_drivers_network_dns_dns_wire
      pc_network_drivers_transport_udp_server_server
  )
endif()

# PROTOCORE_ENABLE_DOCSTORE
if(PROTOCORE_ENABLE_DOCSTORE)
  protocore_module(pc_services_storage_docstore_docstore
    SOURCES "${PROTOCORE_ROOT}/src/services/storage/docstore/docstore.c"
    PUBLIC
      pc_services_storage_dbm_dbm
    PRIVATE
      pc_mmgr_protostr
      pc_network_drivers_presentation_codec_json_json
  )
endif()

# PROTOCORE_ENABLE_DSHOT
if(PROTOCORE_ENABLE_DSHOT)
  protocore_module(pc_server_peripherals_dshot_dshot
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/dshot/dshot.c"
  )
endif()

# PROTOCORE_ENABLE_DTLS
if(PROTOCORE_ENABLE_DTLS)
  protocore_module(pc_network_drivers_presentation_security_dtls_dtls_conn
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/security/dtls/dtls_conn.c"
    PUBLIC
      pc_crypto_hash_sha256
      pc_network_drivers_presentation_security_dtls_dtls_handshake
      pc_network_drivers_presentation_security_dtls_dtls_record
      pc_network_drivers_tls_key_schedule_key_schedule
    PRIVATE
      pc_crypto_asymmetric_curve25519
      pc_crypto_asymmetric_ed25519
      pc_crypto_ct_eq
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_presentation_http_http3_tls13_msg
      pc_server_clock_clock
  )
  protocore_module(pc_network_drivers_presentation_security_dtls_dtls_handshake
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/security/dtls/dtls_handshake.c"
    PRIVATE
      pc_crypto_ct_eq
      pc_crypto_mac_hmac_sha256
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_presentation_security_dtls_dtls_record
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/security/dtls/dtls_record.c"
    PUBLIC
      pc_crypto_aead_aes128gcm
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_tls_key_schedule_key_schedule
  )
endif()

# PROTOCORE_ENABLE_ECDSA
if(PROTOCORE_ENABLE_ECDSA)
  protocore_module(pc_crypto_asymmetric_ecdsa
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/ecdsa.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_hash_sha256
      pc_crypto_mac_hmac_sha256
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_ED25519
if(PROTOCORE_ENABLE_ED25519)
  protocore_module(pc_crypto_asymmetric_ed25519
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/ed25519.c"
    PRIVATE
      pc_crypto_asymmetric_curve25519
      pc_crypto_asymmetric_ed25519_comb_table
      pc_crypto_asymmetric_fe25519
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_crypto_hash_sha512
  )
endif()

# PROTOCORE_ENABLE_EDGE_CACHE
if(PROTOCORE_ENABLE_EDGE_CACHE)
  protocore_module(pc_server_web_edge_cache_edge_cache
    SOURCES "${PROTOCORE_ROOT}/src/server/web/edge_cache/edge_cache.c"
    PRIVATE
      pc_crypto_hash_sha256
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_httpcache_httpcache
      pc_shared_http_date_http_date
  )
  protocore_module(pc_server_web_edge_cache_edge_cache_proxy
    SOURCES "${PROTOCORE_ROOT}/src/server/web/edge_cache/edge_cache_proxy.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_application_http_range
      pc_network_drivers_presentation_http_http
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_network_drivers_presentation_http_httpcache_httpcache
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_client_client
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_server_core_proto_handler
      pc_server_web_edge_cache_edge_cache
      pc_server_web_edge_cache_edge_cache_sd
      pc_server_web_edge_cache_edge_fetch
      pc_server_web_edge_cache_edge_mesh
      pc_services_net_http_client_http_client
      pc_services_storage_dbm_dbm
      pc_shared_http_date_http_date
      pc_shared_mime_mime
  )
  protocore_module(pc_server_web_edge_cache_edge_cache_sd
    SOURCES "${PROTOCORE_ROOT}/src/server/web/edge_cache/edge_cache_sd.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_httpcache_httpcache
      pc_server_web_edge_cache_edge_cache
      pc_services_storage_dbm_dbm
      pc_shared_http_date_http_date
  )
  protocore_module(pc_server_web_edge_cache_edge_fetch
    SOURCES "${PROTOCORE_ROOT}/src/server/web/edge_cache/edge_fetch.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_network_drivers_presentation_http_httpcache_httpcache
      pc_server_web_edge_cache_edge_cache
      pc_services_net_http_client_http_client
      pc_shared_http_date_http_date
  )
endif()

# PROTOCORE_ENABLE_EDGE_MESH
if(PROTOCORE_ENABLE_EDGE_MESH)
  protocore_module(pc_server_web_edge_cache_edge_mesh
    SOURCES "${PROTOCORE_ROOT}/src/server/web/edge_cache/edge_mesh.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_server_web_edge_cache_edge_cache
      pc_server_web_edge_cache_edge_cache_sd
      pc_server_web_edge_cache_edge_fetch
      pc_services_storage_dbm_dbm
  )
endif()

# PROTOCORE_ENABLE_ENIP
if(PROTOCORE_ENABLE_ENIP)
  protocore_module(pc_services_fieldbus_enip_enip
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/enip/enip.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_ENOCEAN
if(PROTOCORE_ENABLE_ENOCEAN)
  protocore_module(pc_services_radio_enocean_enocean
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/enocean/enocean.c"
    PRIVATE
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_ESPNOW
if(PROTOCORE_ENABLE_ESPNOW)
  protocore_module(pc_services_radio_espnow_espnow
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/espnow/espnow.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_EUROMAP77
if(PROTOCORE_ENABLE_EUROMAP77)
  protocore_module(pc_services_opcua_models_euromap77_euromap77
    SOURCES "${PROTOCORE_ROOT}/src/services/opcua/models/euromap77/euromap77.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
      pc_services_opcua_opcua
  )
endif()

# PROTOCORE_ENABLE_EXC_DECODER
if(PROTOCORE_ENABLE_EXC_DECODER)
  protocore_module(pc_server_core_exc_decoder
    SOURCES "${PROTOCORE_ROOT}/src/server/core/exc_decoder.c"
    PUBLIC
      pc_server_storage_mnt
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protostr
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_FAILSAFE
if(PROTOCORE_ENABLE_FAILSAFE)
  protocore_module(pc_server_core_failsafe
    SOURCES "${PROTOCORE_ROOT}/src/server/core/failsafe.c"
    PRIVATE
      pc_mmgr_plaintext
  )
endif()

# PROTOCORE_ENABLE_FANUC_J519
if(PROTOCORE_ENABLE_FANUC_J519)
  protocore_module(pc_services_machine_tool_fanuc_j519_fanuc_j519
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/fanuc_j519/fanuc_j519.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_FDC2214
if(PROTOCORE_ENABLE_FDC2214)
  protocore_module(pc_server_peripherals_fdc2214_fdc2214
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/fdc2214/fdc2214.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_FE25519
if(PROTOCORE_ENABLE_FE25519)
  protocore_module(pc_crypto_asymmetric_fe25519
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/fe25519.c"
    PUBLIC
      pc_crypto_ct_eq
    PRIVATE
      pc_crypto_crypto_opt
  )
endif()

# PROTOCORE_ENABLE_FILE_SERVING
if(PROTOCORE_ENABLE_FILE_SERVING)
  protocore_module(pc_network_drivers_application_file_serving_file_serving
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/file_serving/file_serving.c"
    PUBLIC
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_network_drivers_presentation_http_route_http_route
      pc_server_storage_mnt
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protostr
      pc_network_drivers_application_http_range
      pc_network_drivers_presentation_http_http
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_storage_filesystem
      pc_shared_mime_mime
      pc_shared_time_compat_time_compat
  )
endif()

# PROTOCORE_ENABLE_FINS
if(PROTOCORE_ENABLE_FINS)
  protocore_module(pc_services_fieldbus_fins_fins
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/fins/fins.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_FLOW_EXPORT
if(PROTOCORE_ENABLE_FLOW_EXPORT)
  protocore_module(pc_services_net_flow_export_flow_export
    SOURCES "${PROTOCORE_ROOT}/src/services/net/flow_export/flow_export.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_plaintext
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_FOCAS
if(PROTOCORE_ENABLE_FOCAS)
  protocore_module(pc_services_machine_tool_focas_focas
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/focas/focas.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_FORWARD
if(PROTOCORE_ENABLE_FORWARD)
  protocore_module(pc_network_drivers_network_forward_forward
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/forward/forward.c"
    PUBLIC
      pc_network_drivers_physical_physical
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_server_clock_clock
  )
  protocore_module(pc_network_drivers_network_network
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/network/network.c"
    PUBLIC
      pc_network_drivers_network_dns_dns
      pc_network_drivers_network_forward_forward
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_FORWARDED_TRUST
if(PROTOCORE_ENABLE_FORWARDED_TRUST)
  protocore_module(pc_server_security_forwarded_trust_forwarded_trust
    SOURCES "${PROTOCORE_ROOT}/src/server/security/forwarded_trust/forwarded_trust.c"
    PRIVATE
      pc_mmgr_secure
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_FTP
if(PROTOCORE_ENABLE_FTP)
  protocore_module(pc_services_file_transfer_ftp_ftp
    SOURCES "${PROTOCORE_ROOT}/src/services/file_transfer/ftp/ftp.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_FTP_SESSION
if(PROTOCORE_ENABLE_FTP_SESSION)
  protocore_module(pc_services_file_transfer_ftp_ftp_session
    SOURCES "${PROTOCORE_ROOT}/src/services/file_transfer/ftp/ftp_session.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_tcp_client_client
      pc_server_clock_clock
      pc_services_file_transfer_ftp_ftp
      pc_shared_log_log
  )
endif()

# PROTOCORE_ENABLE_GATEWAY
if(PROTOCORE_ENABLE_GATEWAY)
  protocore_module(pc_server_net_gateway_gateway
    SOURCES "${PROTOCORE_ROOT}/src/server/net/gateway/gateway.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_GHASH
if(PROTOCORE_ENABLE_GHASH)
  protocore_module(pc_crypto_mac_ghash
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/ghash.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
  )
endif()

# PROTOCORE_ENABLE_GOOSE
if(PROTOCORE_ENABLE_GOOSE)
  protocore_module(pc_services_energy_goose_goose
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/goose/goose.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_GPIB
if(PROTOCORE_ENABLE_GPIB)
  protocore_module(pc_services_instrumentation_gpib_gpib
    SOURCES "${PROTOCORE_ROOT}/src/services/instrumentation/gpib/gpib.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_GPIO_MAP
if(PROTOCORE_ENABLE_GPIO_MAP)
  protocore_module(pc_server_signaling_gpio_map
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/gpio_map.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_server_clock_clock
  )
  protocore_module(pc_server_signaling_gpio_map_routes
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/gpio_map_routes.c"
    PRIVATE
      pc_server_signaling_gpio_map
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_GRAPHQL
if(PROTOCORE_ENABLE_GRAPHQL)
  protocore_module(pc_services_iot_graphql_graphql
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/graphql/graphql.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_GRPC_WEB
if(PROTOCORE_ENABLE_GRPC_WEB)
  protocore_module(pc_services_iot_grpcweb_grpcweb
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/grpcweb/grpcweb.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_GUARDRAILS
if(PROTOCORE_ENABLE_GUARDRAILS)
  protocore_module(pc_server_core_guardrails_guardrails
    SOURCES "${PROTOCORE_ROOT}/src/server/core/guardrails/guardrails.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
  )
endif()

# PROTOCORE_ENABLE_HAAS_MDC
if(PROTOCORE_ENABLE_HAAS_MDC)
  protocore_module(pc_services_machine_tool_haas_mdc_haas_mdc
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/haas_mdc/haas_mdc.c"
    PRIVATE
      pc_mmgr_membuild
  )
endif()

# PROTOCORE_ENABLE_HAPPY_EYEBALLS
if(PROTOCORE_ENABLE_HAPPY_EYEBALLS)
  protocore_module(pc_network_drivers_transport_happy_eyeballs_happy_eyeballs
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/happy_eyeballs/happy_eyeballs.c"
    PUBLIC
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_plaintext
  )
endif()

# PROTOCORE_ENABLE_HART
if(PROTOCORE_ENABLE_HART)
  protocore_module(pc_services_fieldbus_hart_hart
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/hart/hart.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_HISLIP
if(PROTOCORE_ENABLE_HISLIP)
  protocore_module(pc_services_instrumentation_hislip_hislip
    SOURCES "${PROTOCORE_ROOT}/src/services/instrumentation/hislip/hislip.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_HKDF
if(PROTOCORE_ENABLE_HKDF)
  protocore_module(pc_crypto_kdf_hkdf
    SOURCES "${PROTOCORE_ROOT}/src/crypto/kdf/hkdf.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_mac_hmac_sha256
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_HKDF_SHA384
if(PROTOCORE_ENABLE_HKDF_SHA384)
  protocore_module(pc_crypto_kdf_hkdf_sha384
    SOURCES "${PROTOCORE_ROOT}/src/crypto/kdf/hkdf_sha384.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_mac_hmac_sha384
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_HMAC_SHA256
if(PROTOCORE_ENABLE_HMAC_SHA256)
  protocore_module(pc_crypto_mac_hmac_sha256
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/hmac_sha256.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_hash_sha256
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_HMAC_SHA384
if(PROTOCORE_ENABLE_HMAC_SHA384)
  protocore_module(pc_crypto_mac_hmac_sha384
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/hmac_sha384.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_hash_sha384
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_HMAC_SHA512
if(PROTOCORE_ENABLE_HMAC_SHA512)
  protocore_module(pc_crypto_mac_hmac_sha512
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/hmac_sha512.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_hash_sha512
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_HMMD
if(PROTOCORE_ENABLE_HMMD)
  protocore_module(pc_server_peripherals_hmmd_hmmd
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/hmmd/hmmd.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_server_peripherals_uart
  )
endif()

# PROTOCORE_ENABLE_HOSTLINK
if(PROTOCORE_ENABLE_HOSTLINK)
  protocore_module(pc_services_fieldbus_hostlink_hostlink
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/hostlink/hostlink.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_HOTSWAP
if(PROTOCORE_ENABLE_HOTSWAP)
  protocore_module(pc_server_storage_hotswap_hotswap
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/hotswap/hotswap.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_secure
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_HTTP2
if(PROTOCORE_ENABLE_HTTP2)
  protocore_module(pc_network_drivers_presentation_http_http2_h2_conn
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http2/h2_conn.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_http2_h2_frame
      pc_network_drivers_presentation_http_http2_hpack
  )
  protocore_module(pc_network_drivers_presentation_http_http2_h2_frame
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http2/h2_frame.c"
    PRIVATE
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_presentation_http_http2_hpack
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http2/hpack.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_codec_hpack_prim_hpack_prim
  )
  protocore_module(pc_network_drivers_session_session
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/session/session.c"
    PUBLIC
      pc_network_drivers_transport_tcp_evt
      pc_server_core_proto_handler
      pc_server_core_worker
    PRIVATE
      pc_mmgr_plaintext
      pc_network_drivers_presentation_presentation
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_server_server
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_server_storage_filesystem
  )
  protocore_module(pc_protocore
    SOURCES "${PROTOCORE_ROOT}/src/protocore.c"
    PRIVATE
      pc_crypto_rng_rng
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_network_drivers_application_ntp_service_ntp_service
      pc_network_drivers_application_web_assets
      pc_network_drivers_application_webdav_webdav
      pc_network_drivers_presentation_http_http
      pc_network_drivers_presentation_http_http2_h2_server
      pc_network_drivers_presentation_http_http3_h3_server
      pc_network_drivers_presentation_http_http3_quic_server
      pc_network_drivers_presentation_http_route_http_route
      pc_network_drivers_presentation_presentation
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_server_core_proto_handler
      pc_server_core_worker
      pc_server_io_webdav_handler
      pc_server_security_csrf_csrf
      pc_services_file_transfer_http_delivery_http_delivery
      pc_services_timing_position_time_source_time_source
      pc_shared_hex_hex
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_HTTP3
if(PROTOCORE_ENABLE_HTTP3)
  protocore_module(pc_network_drivers_presentation_http_http3_h3_conn
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/h3_conn.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_http3_h3_frame
      pc_network_drivers_presentation_http_http3_qpack
      pc_network_drivers_presentation_http_http3_quic_conn
      pc_network_drivers_presentation_http_http3_quic_varint
  )
  protocore_module(pc_network_drivers_presentation_http_http3_h3_frame
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/h3_frame.c"
    PRIVATE
      pc_mmgr_protomem
      pc_network_drivers_presentation_http_http3_quic_varint
  )
  protocore_module(pc_network_drivers_presentation_http_http3_h3_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/h3_server.c"
    PUBLIC
      pc_network_drivers_presentation_http_http3_quic_server
    PRIVATE
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_network_drivers_presentation_http_http
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_protocol_protocol
  )
  protocore_module(pc_network_drivers_presentation_http_http3_qpack
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/qpack.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_codec_hpack_prim_hpack_prim
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_conn
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_conn.c"
    PRIVATE
      pc_crypto_aead_aes128gcm
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_presentation_http_http3_quic_crypto
      pc_network_drivers_presentation_http_http3_quic_frame
      pc_network_drivers_presentation_http_http3_quic_packet
      pc_network_drivers_presentation_http_http3_quic_tls
      pc_network_drivers_presentation_http_http3_quic_varint
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_crypto
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_crypto.c"
    PUBLIC
      pc_crypto_kdf_hkdf
    PRIVATE
      pc_crypto_aead_aes128gcm
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_presentation_http_http3_quic_packet
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_frame
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_frame.c"
    PRIVATE
      pc_mmgr_protomem
      pc_network_drivers_presentation_http_http3_quic_varint
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_packet
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_packet.c"
    PRIVATE
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_server.c"
    PUBLIC
      pc_network_drivers_presentation_http_http3_h3_conn
      pc_network_drivers_presentation_http_http3_quic_conn
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_ring
      pc_mmgr_secure
      pc_network_drivers_presentation_http_http3_quic_packet
      pc_network_drivers_presentation_http_http3_quic_tls
      pc_network_drivers_presentation_http_http3_quic_tp
      pc_network_drivers_transport_udp_server_server
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_tls
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_tls.c"
    PUBLIC
      pc_network_drivers_presentation_http_http3_quic_crypto
      pc_network_drivers_presentation_http_http3_quic_tp
      pc_network_drivers_tls_key_schedule_key_schedule
    PRIVATE
      pc_crypto_asymmetric_curve25519
      pc_crypto_ct_eq
      pc_crypto_pqc_mlkem
      pc_mmgr_protomem
      pc_network_drivers_presentation_http_http3_tls13_msg
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_tp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_tp.c"
    PUBLIC
      pc_network_drivers_presentation_http_http3_quic_packet
    PRIVATE
      pc_mmgr_protomem
      pc_network_drivers_presentation_http_http3_quic_varint
  )
  protocore_module(pc_network_drivers_presentation_http_http3_quic_varint
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/quic_varint.c"
  )
endif()

# PROTOCORE_ENABLE_HTTP_CACHE
if(PROTOCORE_ENABLE_HTTP_CACHE)
  protocore_module(pc_network_drivers_presentation_http_httpcache_httpcache
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/httpcache/httpcache.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_HTTP_CLIENT
if(PROTOCORE_ENABLE_HTTP_CLIENT)
  protocore_module(pc_services_net_http_client_http_client
    SOURCES "${PROTOCORE_ROOT}/src/services/net/http_client/http_client.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_client_client
      pc_server_clock_clock
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_HTTP_DELIVERY
if(PROTOCORE_ENABLE_HTTP_DELIVERY)
  protocore_module(pc_services_file_transfer_http_delivery_http_delivery
    SOURCES "${PROTOCORE_ROOT}/src/services/file_transfer/http_delivery/http_delivery.c"
    PRIVATE
      pc_mmgr_membuild
  )
  protocore_module(pc_services_file_transfer_http_delivery_http_delivery_routes
    SOURCES "${PROTOCORE_ROOT}/src/services/file_transfer/http_delivery/http_delivery_routes.c"
    PRIVATE
      pc_network_drivers_application_web_assets
      pc_services_file_transfer_http_delivery_http_delivery
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_HW_HEALTH
if(PROTOCORE_ENABLE_HW_HEALTH)
  protocore_module(pc_server_signaling_hw_health
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/hw_health.c"
    PRIVATE
      pc_mmgr_membuild
  )
endif()

# PROTOCORE_ENABLE_ICCP
if(PROTOCORE_ENABLE_ICCP)
  protocore_module(pc_services_energy_iccp_iccp
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/iccp/iccp.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_IEC60870
if(PROTOCORE_ENABLE_IEC60870)
  protocore_module(pc_services_energy_iec60870_iec60870
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/iec60870/iec60870.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_IFACE_BRIDGE
if(PROTOCORE_ENABLE_IFACE_BRIDGE)
  protocore_module(pc_server_net_iface_bridge_iface_bridge
    SOURCES "${PROTOCORE_ROOT}/src/server/net/iface_bridge/iface_bridge.c"
    PUBLIC
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
  )
  protocore_module(pc_server_net_iface_bridge_iface_bridge_hw
    SOURCES "${PROTOCORE_ROOT}/src/server/net/iface_bridge/iface_bridge_hw.c"
    PRIVATE
      pc_mmgr_secure
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_server_core_proto_handler
      pc_server_net_iface_bridge_iface_bridge
      pc_server_peripherals_i2c
      pc_server_peripherals_spi
      pc_server_peripherals_uart
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_IKEV2
if(PROTOCORE_ENABLE_IKEV2)
  protocore_module(pc_services_security_ikev2_ikev2
    SOURCES "${PROTOCORE_ROOT}/src/services/security/ikev2/ikev2.c"
    PRIVATE
      pc_crypto_aead_aesgcm
      pc_crypto_asymmetric_curve25519
      pc_crypto_asymmetric_ecdsa
      pc_crypto_asymmetric_rsa
      pc_crypto_hash_sha256
      pc_crypto_mac_hmac_sha256
      pc_mmgr_protomem
      pc_mmgr_secure
  )
  protocore_module(pc_services_security_ikev2_ikev2_natt
    SOURCES "${PROTOCORE_ROOT}/src/services/security/ikev2/ikev2_natt.c"
    PUBLIC
      pc_services_security_ikev2_ikev2
    PRIVATE
      pc_crypto_hash_sha1
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_mmgr_span
  )
  protocore_module(pc_services_system_esp_esp
    SOURCES "${PROTOCORE_ROOT}/src/services/system/esp/esp.c"
    PRIVATE
      pc_crypto_aead_aesgcm
      pc_mmgr_protomem
      pc_mmgr_secure
  )
  protocore_module(pc_services_system_esp_ipsec_db
    SOURCES "${PROTOCORE_ROOT}/src/services/system/esp/ipsec_db.c"
    PUBLIC
      pc_services_security_ikev2_ikev2
      pc_services_system_esp_esp
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_INA219
if(PROTOCORE_ENABLE_INA219)
  protocore_module(pc_server_peripherals_ina219_ina219
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/ina219/ina219.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_INTERBUS
if(PROTOCORE_ENABLE_INTERBUS)
  protocore_module(pc_services_fieldbus_interbus_interbus
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/interbus/interbus.c"
    PRIVATE
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_IOLINK
if(PROTOCORE_ENABLE_IOLINK)
  protocore_module(pc_services_fieldbus_iolink_iolink
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/iolink/iolink.c"
  )
endif()

# PROTOCORE_ENABLE_J2735
if(PROTOCORE_ENABLE_J2735)
  protocore_module(pc_services_transportation_j2735_j2735
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/j2735/j2735.c"
  )
endif()

# PROTOCORE_ENABLE_JWT
if(PROTOCORE_ENABLE_JWT)
  protocore_module(pc_services_security_jwt_jwt
    SOURCES "${PROTOCORE_ROOT}/src/services/security/jwt/jwt.c"
    PRIVATE
      pc_crypto_ct_eq
      pc_crypto_mac_hmac_sha256
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_presentation_codec_base64_base64
  )
endif()

# PROTOCORE_ENABLE_KDF
if(PROTOCORE_ENABLE_KDF)
  protocore_module(pc_crypto_kdf_kdf
    SOURCES "${PROTOCORE_ROOT}/src/crypto/kdf/kdf.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_mac_hmac_sha256
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_LD2410
if(PROTOCORE_ENABLE_LD2410)
  protocore_module(pc_server_peripherals_ld2410_ld2410
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/ld2410/ld2410.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_server_peripherals_uart
  )
endif()

# PROTOCORE_ENABLE_LDC1614
if(PROTOCORE_ENABLE_LDC1614)
  protocore_module(pc_server_peripherals_ldc1614_ldc1614
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/ldc1614/ldc1614.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_LINK_MANAGER
if(PROTOCORE_ENABLE_LINK_MANAGER)
  protocore_module(pc_server_signaling_link_manager
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/link_manager.c"
  )
endif()

# PROTOCORE_ENABLE_LOGBUF
if(PROTOCORE_ENABLE_LOGBUF)
  protocore_module(pc_server_core_logbuf
    SOURCES "${PROTOCORE_ROOT}/src/server/core/logbuf.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
  protocore_module(pc_shared_log_log
    SOURCES "${PROTOCORE_ROOT}/src/shared/log/log.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_server_core_logbuf
  )
endif()

# PROTOCORE_ENABLE_LONWORKS
if(PROTOCORE_ENABLE_LONWORKS)
  protocore_module(pc_services_fieldbus_lonworks_lonworks
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/lonworks/lonworks.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_LORA
if(PROTOCORE_ENABLE_LORA)
  protocore_module(pc_services_radio_lora_lora
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/lora/lora.c"
  )
endif()

# PROTOCORE_ENABLE_LSV2
if(PROTOCORE_ENABLE_LSV2)
  protocore_module(pc_services_machine_tool_lsv2_lsv2
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/lsv2/lsv2.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_LWM2M
if(PROTOCORE_ENABLE_LWM2M)
  protocore_module(pc_services_iot_lwm2m_lwm2m_tlv
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/lwm2m/lwm2m_tlv.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_MBPLUS
if(PROTOCORE_ENABLE_MBPLUS)
  protocore_module(pc_services_fieldbus_mbplus_mbplus
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/mbplus/mbplus.c"
    PRIVATE
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_MBUS
if(PROTOCORE_ENABLE_MBUS)
  protocore_module(pc_services_fieldbus_mbus_mbus
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/mbus/mbus.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_MD
if(PROTOCORE_ENABLE_MD)
  protocore_module(pc_crypto_hash_md
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/md.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_secure
  )
endif()

# PROTOCORE_ENABLE_MDNS
if(PROTOCORE_ENABLE_MDNS)
  protocore_module(pc_network_drivers_application_mdns_service_mdns_service
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/mdns_service/mdns_service.c"
    PRIVATE
      pc_mmgr_protostr
      pc_mmgr_rawmemcpy
      pc_mmgr_secure
      pc_network_drivers_network_dns_dns_wire
      pc_network_drivers_physical_physical
      pc_network_drivers_transport_udp_server_server
  )
endif()

# PROTOCORE_ENABLE_MDNS_ADAPTIVE
if(PROTOCORE_ENABLE_MDNS_ADAPTIVE)
  protocore_module(pc_network_drivers_application_mdns_adaptive_mdns_adaptive
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/mdns_adaptive/mdns_adaptive.c"
    PRIVATE
      pc_network_drivers_application_mdns_service_mdns_service
      pc_network_drivers_physical_physical
      pc_server_clock_clock
      pc_services_radio_promisc_promisc
      pc_shared_pcap_pcap
  )
endif()

# PROTOCORE_ENABLE_MELSEC
if(PROTOCORE_ENABLE_MELSEC)
  protocore_module(pc_services_fieldbus_melsec_melsec
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/melsec/melsec.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_MLKEM
if(PROTOCORE_ENABLE_MLKEM)
  protocore_module(pc_crypto_pqc_mlkem
    SOURCES "${PROTOCORE_ROOT}/src/crypto/pqc/mlkem.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_crypto_hash_sha3
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_MMS
if(PROTOCORE_ENABLE_MMS)
  protocore_module(pc_services_energy_mms_mms
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/mms/mms.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_MNT
if(PROTOCORE_ENABLE_MNT)
  protocore_module(pc_server_storage_mnt
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/mnt.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_MODBUS_MASTER
if(PROTOCORE_ENABLE_MODBUS_MASTER)
  protocore_module(pc_services_fieldbus_modbus_modbus_master
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/modbus/modbus_master.c"
  )
endif()

# PROTOCORE_ENABLE_MODBUS_RTU
if(PROTOCORE_ENABLE_MODBUS_RTU)
  protocore_module(pc_services_fieldbus_modbus_modbus
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/modbus/modbus.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_core_proto_handler
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_MPR121
if(PROTOCORE_ENABLE_MPR121)
  protocore_module(pc_server_peripherals_mpr121_mpr121
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/mpr121/mpr121.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_server_clock_clock
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_MQTT
if(PROTOCORE_ENABLE_MQTT)
  protocore_module(pc_services_iot_mqtt_mqtt
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/mqtt/mqtt.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_mmgr_span
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_client_client
      pc_server_clock_clock
      pc_shared_utf8_utf8
  )
endif()

# PROTOCORE_ENABLE_MQTT_SN
if(PROTOCORE_ENABLE_MQTT_SN)
  protocore_module(pc_services_iot_mqtt_mqtt_sn
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/mqtt/mqtt_sn.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_MSGPACK
if(PROTOCORE_ENABLE_MSGPACK)
  protocore_module(pc_network_drivers_presentation_codec_msgpack_msgpack
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/msgpack/msgpack.c"
    PUBLIC
      pc_mmgr_span
      pc_network_drivers_presentation_codec_codec
    PRIVATE
      pc_mmgr_bytes
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_MTCONNECT
if(PROTOCORE_ENABLE_MTCONNECT)
  protocore_module(pc_services_machine_tool_mtconnect_mtconnect
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/mtconnect/mtconnect.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_NATS
if(PROTOCORE_ENABLE_NATS)
  protocore_module(pc_services_iot_nats_nats
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/nats/nats.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_NEMA_TS2
if(PROTOCORE_ENABLE_NEMA_TS2)
  protocore_module(pc_services_transportation_nema_ts2_nema_ts2
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/nema_ts2/nema_ts2.c"
    PRIVATE
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_NETADAPT
if(PROTOCORE_ENABLE_NETADAPT)
  protocore_module(pc_server_net_netadapt_netadapt
    SOURCES "${PROTOCORE_ROOT}/src/server/net/netadapt/netadapt.c"
  )
endif()

# PROTOCORE_ENABLE_NMEA2000
if(PROTOCORE_ENABLE_NMEA2000)
  protocore_module(pc_services_timing_position_nmea2000_nmea2000
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/nmea2000/nmea2000.c"
    PUBLIC
      pc_services_fieldbus_j1939_j1939
      pc_shared_can_can
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_NRF24
if(PROTOCORE_ENABLE_NRF24)
  protocore_module(pc_services_radio_nrf24_nrf24
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/nrf24/nrf24.c"
  )
endif()

# PROTOCORE_ENABLE_NTCIP
if(PROTOCORE_ENABLE_NTCIP)
  protocore_module(pc_services_transportation_ntcip_ntcip
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/ntcip/ntcip.c"
  )
endif()

# PROTOCORE_ENABLE_NTP
if(PROTOCORE_ENABLE_NTP)
  protocore_module(pc_network_drivers_application_ntp_service_ntp_service
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/ntp_service/ntp_service.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_network_drivers_application_ntp_ntp
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_shared_http_date_http_date
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_NTP_SERVER
if(PROTOCORE_ENABLE_NTP_SERVER)
  protocore_module(pc_network_drivers_application_ntp_server_ntp_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/ntp_server/ntp_server.c"
    PUBLIC
      pc_network_drivers_application_ntp_ntp
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_services_timing_position_time_source_time_source
  )
endif()

# PROTOCORE_ENABLE_NTRIP_CASTER
if(PROTOCORE_ENABLE_NTRIP_CASTER)
  protocore_module(pc_services_timing_position_gnss_gnss_survey
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/gnss/gnss_survey.c"
    PUBLIC
      pc_services_timing_position_nmea0183_nmea0183
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
  protocore_module(pc_services_timing_position_gnss_ntrip_caster
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/gnss/ntrip_caster.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
  protocore_module(pc_services_timing_position_gnss_ntrip_caster_listener
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/gnss/ntrip_caster_listener.c"
    PUBLIC
      pc_services_timing_position_gnss_ntrip_caster
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_core_proto_handler
  )
  protocore_module(pc_services_timing_position_gnss_rtcm3
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/gnss/rtcm3.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_NTS
if(PROTOCORE_ENABLE_NTS)
  protocore_module(pc_network_drivers_application_nts_nts
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/nts/nts.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_OAUTH2
if(PROTOCORE_ENABLE_OAUTH2)
  protocore_module(pc_services_security_oauth2_oauth2
    SOURCES "${PROTOCORE_ROOT}/src/services/security/oauth2/oauth2.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_presentation_codec_json_json
      pc_services_net_http_client_http_client
      pc_shared_hex_hex
  )
endif()

# PROTOCORE_ENABLE_OCIT
if(PROTOCORE_ENABLE_OCIT)
  protocore_module(pc_services_transportation_ocit_ocit
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/ocit/ocit.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_OIDC
if(PROTOCORE_ENABLE_OIDC)
  protocore_module(pc_services_security_oidc_oidc
    SOURCES "${PROTOCORE_ROOT}/src/services/security/oidc/oidc.c"
    PRIVATE
      pc_crypto_asymmetric_rsa
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_presentation_codec_base64_base64
  )
endif()

# PROTOCORE_ENABLE_OPCUA
if(PROTOCORE_ENABLE_OPCUA)
  protocore_module(pc_services_opcua_opcua
    SOURCES "${PROTOCORE_ROOT}/src/services/opcua/opcua.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_core_proto_handler
  )
endif()

# PROTOCORE_ENABLE_OPCUA_CLIENT
if(PROTOCORE_ENABLE_OPCUA_CLIENT)
  protocore_module(pc_services_opcua_opcua_client_opcua_client
    SOURCES "${PROTOCORE_ROOT}/src/services/opcua/opcua_client/opcua_client.c"
    PUBLIC
      pc_services_opcua_opcua
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_OPENADR
if(PROTOCORE_ENABLE_OPENADR)
  protocore_module(pc_services_energy_openadr_openadr
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/openadr/openadr.c"
    PRIVATE
      pc_mmgr_membuild
  )
endif()

# PROTOCORE_ENABLE_OTA_ROLLBACK
if(PROTOCORE_ENABLE_OTA_ROLLBACK)
  protocore_module(pc_server_update_ota_rollback
    SOURCES "${PROTOCORE_ROOT}/src/server/update/ota_rollback.c"
    PRIVATE
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_PACKML
if(PROTOCORE_ENABLE_PACKML)
  protocore_module(pc_services_machine_tool_packml_packml
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/packml/packml.c"
    PRIVATE
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_PARTITION_MONITOR
if(PROTOCORE_ENABLE_PARTITION_MONITOR)
  protocore_module(pc_server_storage_partition_monitor_partition_monitor
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/partition_monitor/partition_monitor.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protostr
  )
  protocore_module(pc_server_storage_partition_monitor_partition_monitor_routes
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/partition_monitor/partition_monitor_routes.c"
    PRIVATE
      pc_server_storage_partition_monitor_partition_monitor
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_PCA9685
if(PROTOCORE_ENABLE_PCA9685)
  protocore_module(pc_server_peripherals_pca9685_pca9685
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/pca9685/pca9685.c"
    PRIVATE
      pc_mmgr_secure
      pc_server_clock_clock
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_PMBUS
if(PROTOCORE_ENABLE_PMBUS)
  protocore_module(pc_server_peripherals_pmbus
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/pmbus.c"
    PRIVATE
      pc_server_peripherals_smbus
  )
endif()

# PROTOCORE_ENABLE_PN532
if(PROTOCORE_ENABLE_PN532)
  protocore_module(pc_server_peripherals_pn532_pn532
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/pn532/pn532.c"
  )
endif()

# PROTOCORE_ENABLE_POLY1305
if(PROTOCORE_ENABLE_POLY1305)
  protocore_module(pc_crypto_mac_poly1305
    SOURCES "${PROTOCORE_ROOT}/src/crypto/mac/poly1305.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_POWERLINK
if(PROTOCORE_ENABLE_POWERLINK)
  protocore_module(pc_services_fieldbus_powerlink_powerlink
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/powerlink/powerlink.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_POWER_MGMT
if(PROTOCORE_ENABLE_POWER_MGMT)
  protocore_module(pc_server_core_power_mgmt
    SOURCES "${PROTOCORE_ROOT}/src/server/core/power_mgmt.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
  )
endif()

# PROTOCORE_ENABLE_PQC_KEX
if(PROTOCORE_ENABLE_PQC_KEX)
  protocore_module(pc_network_drivers_presentation_http_http3_tls13_msg
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http3/tls13_msg.c"
    PRIVATE
      pc_crypto_asymmetric_ed25519
      pc_crypto_pqc_mlkem
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
  protocore_module(pc_network_drivers_presentation_ssh_client_client
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/client/client.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_crypto_asymmetric_bignum
      pc_crypto_asymmetric_curve25519
      pc_crypto_asymmetric_ecdsa
      pc_crypto_asymmetric_ed25519
      pc_crypto_hash_sha256
      pc_crypto_pqc_mlkem
      pc_crypto_pqc_sntrup761
      pc_crypto_rng_rng
      pc_mmgr_arena
      pc_mmgr_bytes
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_rawmemcpy
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_auth_auth
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_ssh
      pc_network_drivers_presentation_ssh_transport_ssh_kexhash
      pc_network_drivers_presentation_ssh_transport_ssh_rsa
      pc_network_drivers_presentation_ssh_transport_transport
      pc_network_drivers_transport_tcp_client_client
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_shared_log_log
  )
  protocore_module(pc_network_drivers_presentation_ssh_transport_transport
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/transport.c"
    PUBLIC
      pc_crypto_aead_aesgcm
      pc_crypto_aead_chachapoly
      pc_crypto_asymmetric_bignum
      pc_crypto_cipher_aes256ctr
      pc_crypto_hash_sha256
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_common
      pc_network_drivers_presentation_ssh_transport_phase_machine
    PRIVATE
      pc_crypto_asymmetric_curve25519
      pc_crypto_asymmetric_ecdsa
      pc_crypto_asymmetric_ed25519
      pc_crypto_asymmetric_rsa
      pc_crypto_ct_eq
      pc_crypto_mac_hmac_sha256
      pc_crypto_mac_hmac_sha512
      pc_crypto_pqc_mlkem
      pc_crypto_pqc_sntrup761
      pc_crypto_rng_rng
      pc_mmgr_bytes
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_ssh_auth_auth
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_ssh
      pc_network_drivers_presentation_ssh_transport_comp
      pc_network_drivers_presentation_ssh_transport_extension
      pc_network_drivers_presentation_ssh_transport_ssh_kexhash
      pc_network_drivers_presentation_ssh_transport_ssh_rsa
      pc_network_drivers_presentation_ssh_transport_zlib
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_PREEMPT_QUEUE
if(PROTOCORE_ENABLE_PREEMPT_QUEUE)
  protocore_module(pc_server_core_preempt_queue
    SOURCES "${PROTOCORE_ROOT}/src/server/core/preempt_queue.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_secure
  )
  protocore_module(pc_server_core_worker
    SOURCES "${PROTOCORE_ROOT}/src/server/core/worker.c"
    PUBLIC
      pc_server_core_preempt_queue
    PRIVATE
      pc_mmgr_arena
      pc_mmgr_plaintext
      pc_mmgr_ring
  )
endif()

# PROTOCORE_ENABLE_PROFIBUS
if(PROTOCORE_ENABLE_PROFIBUS)
  protocore_module(pc_services_fieldbus_profibus_profibus
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/profibus/profibus.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_PROFINET
if(PROTOCORE_ENABLE_PROFINET)
  protocore_module(pc_services_fieldbus_profinet_profinet
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/profinet/profinet.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_PROMISC
if(PROTOCORE_ENABLE_PROMISC)
  protocore_module(pc_services_radio_promisc_promisc
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/promisc/promisc.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_network_drivers_physical_physical
      pc_network_drivers_physical_radio_power
      pc_shared_pcap_pcap
  )
endif()

# PROTOCORE_ENABLE_PROVISIONING
if(PROTOCORE_ENABLE_PROVISIONING)
  protocore_module(pc_server_core_provisioning_service_provisioning_service
    SOURCES "${PROTOCORE_ROOT}/src/server/core/provisioning_service/provisioning_service.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_application_web_assets
      pc_network_drivers_physical_physical
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_shared_hex_hex
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_PROXY_PROTOCOL
if(PROTOCORE_ENABLE_PROXY_PROTOCOL)
  protocore_module(pc_network_drivers_transport_proxy_protocol_proxy_protocol
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/proxy_protocol/proxy_protocol.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_PSRAM_POOL
if(PROTOCORE_ENABLE_PSRAM_POOL)
  protocore_module(pc_mmgr_psram_pool
    SOURCES "${PROTOCORE_ROOT}/src/mmgr/psram_pool.c"
  )
endif()

# PROTOCORE_ENABLE_PTP
if(PROTOCORE_ENABLE_PTP)
  protocore_module(pc_network_drivers_application_ptp_ptp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/ptp/ptp.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_RADIO_POWER
if(PROTOCORE_ENABLE_RADIO_POWER)
  protocore_module(pc_network_drivers_physical_radio_power
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/physical/radio_power.c"
    PUBLIC
      pc_network_drivers_physical_physical
    PRIVATE
      pc_mmgr_plaintext
  )
endif()

# PROTOCORE_ENABLE_RADIO_SNIFF
if(PROTOCORE_ENABLE_RADIO_SNIFF)
  protocore_module(pc_services_radio_radio_sniff_radio_sniff
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/radio_sniff/radio_sniff.c"
    PRIVATE
      pc_mmgr_endian
      pc_shared_pcap_pcap
  )
endif()

# PROTOCORE_ENABLE_RANGE
if(PROTOCORE_ENABLE_RANGE)
  protocore_module(pc_network_drivers_application_http_range
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/http_range.c"
    PRIVATE
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_RAWL2
if(PROTOCORE_ENABLE_RAWL2)
  protocore_module(pc_services_fieldbus_rawl2_rawl2
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/rawl2/rawl2.c"
    PRIVATE
      pc_mmgr_protomem
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_RCWL0516
if(PROTOCORE_ENABLE_RCWL0516)
  protocore_module(pc_server_peripherals_rcwl0516_rcwl0516
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/rcwl0516/rcwl0516.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_REDIS
if(PROTOCORE_ENABLE_REDIS)
  protocore_module(pc_services_iot_redis_resp_redis_resp
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/redis_resp/redis_resp.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_RELAY
if(PROTOCORE_ENABLE_RELAY)
  protocore_module(pc_server_net_relay_relay
    SOURCES "${PROTOCORE_ROOT}/src/server/net/relay/relay.c"
    PRIVATE
      pc_mmgr_protomem
  )
  protocore_module(pc_server_net_relay_relay_listener
    SOURCES "${PROTOCORE_ROOT}/src/server/net/relay/relay_listener.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_physical_radio_power
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_client_client
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_core_proto_handler
      pc_server_net_relay_relay
  )
endif()

# PROTOCORE_ENABLE_RNG
if(PROTOCORE_ENABLE_RNG)
  protocore_module(pc_crypto_rng_rng
    SOURCES "${PROTOCORE_ROOT}/src/crypto/rng/rng.c"
    PRIVATE
      pc_crypto_cipher_chacha20
      pc_crypto_crypto_opt
      pc_mmgr_protomem
      pc_mmgr_secure
  )
endif()

# PROTOCORE_ENABLE_ROAMING
if(PROTOCORE_ENABLE_ROAMING)
  protocore_module(pc_network_drivers_datalink_roaming
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/datalink/roaming.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_ROBOTICS
if(PROTOCORE_ENABLE_ROBOTICS)
  protocore_module(pc_services_opcua_models_robotics_robotics
    SOURCES "${PROTOCORE_ROOT}/src/services/opcua/models/robotics/robotics.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
      pc_services_opcua_opcua
  )
endif()

# PROTOCORE_ENABLE_RSA
if(PROTOCORE_ENABLE_RSA)
  protocore_module(pc_crypto_asymmetric_rsa
    SOURCES "${PROTOCORE_ROOT}/src/crypto/asymmetric/rsa.c"
    PRIVATE
      pc_crypto_asymmetric_bignum
      pc_crypto_crypto_opt
      pc_crypto_ct_eq
      pc_crypto_hash_sha256
      pc_crypto_hash_sha512
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_RTC
if(PROTOCORE_ENABLE_RTC)
  protocore_module(pc_server_peripherals_rtc_rtc
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/rtc/rtc.c"
    PRIVATE
      pc_mmgr_secure
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_S7COMM
if(PROTOCORE_ENABLE_S7COMM)
  protocore_module(pc_services_fieldbus_s7comm_s7comm
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/s7comm/s7comm.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SAFETY_SCL
if(PROTOCORE_ENABLE_SAFETY_SCL)
  protocore_module(pc_services_machine_tool_safety_scl_safety_scl
    SOURCES "${PROTOCORE_ROOT}/src/services/machine_tool/safety_scl/safety_scl.c"
  )
endif()

# PROTOCORE_ENABLE_SCPI
if(PROTOCORE_ENABLE_SCPI)
  protocore_module(pc_services_instrumentation_scpi_scpi
    SOURCES "${PROTOCORE_ROOT}/src/services/instrumentation/scpi/scpi.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_SDI12
if(PROTOCORE_ENABLE_SDI12)
  protocore_module(pc_server_peripherals_sdi12_sdi12
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/sdi12/sdi12.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_SEN0192
if(PROTOCORE_ENABLE_SEN0192)
  protocore_module(pc_server_peripherals_sen0192_sen0192
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/sen0192/sen0192.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_SENML
if(PROTOCORE_ENABLE_SENML)
  protocore_module(pc_services_iot_senml_senml
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/senml/senml.c"
    PUBLIC
      pc_network_drivers_presentation_codec_codec
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_span
      pc_network_drivers_presentation_codec_json_json
  )
endif()

# PROTOCORE_ENABLE_SEP2
if(PROTOCORE_ENABLE_SEP2)
  protocore_module(pc_services_energy_sep2_sep2
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/sep2/sep2.c"
    PRIVATE
      pc_mmgr_membuild
  )
endif()

# PROTOCORE_ENABLE_SERCOS
if(PROTOCORE_ENABLE_SERCOS)
  protocore_module(pc_services_fieldbus_sercos_sercos
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/sercos/sercos.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SHA1
if(PROTOCORE_ENABLE_SHA1)
  protocore_module(pc_crypto_hash_sha1
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/sha1.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SHA256
if(PROTOCORE_ENABLE_SHA256)
  protocore_module(pc_crypto_hash_sha256
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/sha256.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SHA3
if(PROTOCORE_ENABLE_SHA3)
  protocore_module(pc_crypto_hash_sha3
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/sha3.c"
    PRIVATE
      pc_crypto_crypto_opt
  )
endif()

# PROTOCORE_ENABLE_SHA384
if(PROTOCORE_ENABLE_SHA384)
  protocore_module(pc_crypto_hash_sha384
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/sha384.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SHA512
if(PROTOCORE_ENABLE_SHA512)
  protocore_module(pc_crypto_hash_sha512
    SOURCES "${PROTOCORE_ROOT}/src/crypto/hash/sha512.c"
    PRIVATE
      pc_crypto_crypto_opt
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SHT3X
if(PROTOCORE_ENABLE_SHT3X)
  protocore_module(pc_server_peripherals_sht3x_sht3x
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/sht3x/sht3x.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_secure
      pc_server_clock_clock
      pc_server_peripherals_i2c
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_SIGFOX
if(PROTOCORE_ENABLE_SIGFOX)
  protocore_module(pc_services_radio_sigfox_sigfox
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/sigfox/sigfox.c"
  )
endif()

# PROTOCORE_ENABLE_SIMATIC
if(PROTOCORE_ENABLE_SIMATIC)
  protocore_module(pc_services_fieldbus_simatic_simatic
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/simatic/simatic.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SLEEP_SCHED
if(PROTOCORE_ENABLE_SLEEP_SCHED)
  protocore_module(pc_server_core_sleep_sched
    SOURCES "${PROTOCORE_ROOT}/src/server/core/sleep_sched.c"
  )
endif()

# PROTOCORE_ENABLE_SMB
if(PROTOCORE_ENABLE_SMB)
  protocore_module(pc_network_drivers_application_smb_ntlm
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/smb/ntlm.c"
    PRIVATE
      pc_crypto_hash_md
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_mmgr_span
  )
  protocore_module(pc_network_drivers_application_smb_ntlmssp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/smb/ntlmssp.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_application_smb_smb2
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/smb/smb2.c"
    PRIVATE
      pc_crypto_aead_aes128gcm
      pc_crypto_aead_aesccm
      pc_crypto_aead_aesgcm
      pc_crypto_hash_sha512
      pc_crypto_kdf_kdf
      pc_crypto_mac_aes_cmac
      pc_crypto_mac_hmac_sha256
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_secure
  )
  protocore_module(pc_network_drivers_application_smb_smb_client
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/smb/smb_client.c"
    PUBLIC
      pc_network_drivers_application_smb_smb2
    PRIVATE
      pc_crypto_rng_rng
      pc_mmgr_protomem
      pc_network_drivers_application_smb_ntlm
      pc_network_drivers_application_smb_ntlmssp
      pc_network_drivers_application_smb_spnego
  )
  protocore_module(pc_network_drivers_application_smb_spnego
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/smb/spnego.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SMBUS
if(PROTOCORE_ENABLE_SMBUS)
  protocore_module(pc_server_peripherals_smbus
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/smbus.c"
    PRIVATE
      pc_mmgr_secure
      pc_server_peripherals_i2c
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_SMTP
if(PROTOCORE_ENABLE_SMTP)
  protocore_module(pc_services_net_smtp_smtp
    SOURCES "${PROTOCORE_ROOT}/src/services/net/smtp/smtp.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_presentation_codec_base64_base64
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_client_client
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_SNMP
if(PROTOCORE_ENABLE_SNMP)
  protocore_module(pc_services_net_snmp_snmp_agent
    SOURCES "${PROTOCORE_ROOT}/src/services/net/snmp/snmp_agent.c"
    PUBLIC
      pc_services_net_snmp_snmp_ber
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_transport_udp_server_server
      pc_server_clock_clock
      pc_services_net_snmp_snmp_v3
  )
  protocore_module(pc_services_net_snmp_snmp_ber
    SOURCES "${PROTOCORE_ROOT}/src/services/net/snmp/snmp_ber.c"
  )
endif()

# PROTOCORE_ENABLE_SNMP_TRAP
if(PROTOCORE_ENABLE_SNMP_TRAP)
  protocore_module(pc_services_net_snmp_snmp_notify
    SOURCES "${PROTOCORE_ROOT}/src/services/net/snmp/snmp_notify.c"
    PUBLIC
      pc_services_net_snmp_snmp_ber
    PRIVATE
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_transport_udp_client_client
      pc_server_clock_clock
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_SNMP_V3
if(PROTOCORE_ENABLE_SNMP_V3)
  protocore_module(pc_services_net_snmp_snmp_crypto
    SOURCES "${PROTOCORE_ROOT}/src/services/net/snmp/snmp_crypto.c"
    PRIVATE
      pc_crypto_cipher_aes_sbox
      pc_crypto_hash_sha256
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
  )
  protocore_module(pc_services_net_snmp_snmp_v3
    SOURCES "${PROTOCORE_ROOT}/src/services/net/snmp/snmp_v3.c"
    PUBLIC
      pc_services_net_snmp_snmp_notify
    PRIVATE
      pc_crypto_mac_hmac_sha256
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_transport_udp_client_client
      pc_server_clock_clock
      pc_services_net_snmp_snmp_agent
      pc_services_net_snmp_snmp_ber
      pc_services_net_snmp_snmp_crypto
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_SNP
if(PROTOCORE_ENABLE_SNP)
  protocore_module(pc_services_fieldbus_snp_snp
    SOURCES "${PROTOCORE_ROOT}/src/services/fieldbus/snp/snp.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SNTRUP761
if(PROTOCORE_ENABLE_SNTRUP761)
  protocore_module(pc_crypto_pqc_sntrup761
    SOURCES "${PROTOCORE_ROOT}/src/crypto/pqc/sntrup761.c"
    PRIVATE
      pc_crypto_hash_sha512
      pc_crypto_rng_rng
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_SOCKPOOL
if(PROTOCORE_ENABLE_SOCKPOOL)
  protocore_module(pc_server_net_sockpool_sockpool
    SOURCES "${PROTOCORE_ROOT}/src/server/net/sockpool/sockpool.c"
  )
endif()

# PROTOCORE_ENABLE_SOUTHBOUND
if(PROTOCORE_ENABLE_SOUTHBOUND)
  protocore_module(pc_services_southbound_southbound
    SOURCES "${PROTOCORE_ROOT}/src/services/southbound/southbound.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_SPARKPLUG
if(PROTOCORE_ENABLE_SPARKPLUG)
  protocore_module(pc_services_iot_sparkplug_sparkplug
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/sparkplug/sparkplug.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_services_iot_protobuf_protobuf
  )
endif()

# PROTOCORE_ENABLE_SPA_ROUTER
if(PROTOCORE_ENABLE_SPA_ROUTER)
  protocore_module(pc_server_web_spa_router_spa_router
    SOURCES "${PROTOCORE_ROOT}/src/server/web/spa_router/spa_router.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_SQLITE
if(PROTOCORE_ENABLE_SQLITE)
  protocore_module(pc_services_storage_sqlite_sqlite_format
    SOURCES "${PROTOCORE_ROOT}/src/services/storage/sqlite/sqlite_format.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_SSE
if(PROTOCORE_ENABLE_SSE)
  protocore_module(pc_network_drivers_presentation_http_sse_sse
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/sse/sse.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_mmgr_span
      pc_network_drivers_transport_tcp_protocol_protocol
  )
  protocore_module(pc_network_drivers_session_sse_sse
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/session/sse/sse.c"
    PRIVATE
      pc_network_drivers_presentation_http_sse_sse
  )
endif()

# PROTOCORE_ENABLE_SSH_CLIENT
if(PROTOCORE_ENABLE_SSH_CLIENT)
  protocore_module(pc_network_drivers_presentation_ssh_app_client
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/app/client.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_client_client
    PRIVATE
      pc_crypto_asymmetric_ed25519
      pc_mmgr_arena
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_transport_transport
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_shared_log_log
  )
endif()

# PROTOCORE_ENABLE_SSH_SCP
if(PROTOCORE_ENABLE_SSH_SCP)
  protocore_module(pc_network_drivers_session_scp_scp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/session/scp/scp.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_session_scp_ssh_scp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/session/scp/ssh_scp.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_session_scp_scp
      pc_network_drivers_session_session
      pc_server_storage_filesystem
  )
endif()

# PROTOCORE_ENABLE_SSH_SFTP
if(PROTOCORE_ENABLE_SSH_SFTP)
  protocore_module(pc_network_drivers_application_sftp_sftp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/sftp/sftp.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_shared_time_compat_time_compat
  )
  protocore_module(pc_network_drivers_application_sftp_ssh_sftp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/sftp/ssh_sftp.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_application_sftp_sftp
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_session_session
      pc_server_storage_filesystem
  )
  protocore_module(pc_network_drivers_presentation_ssh_app_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/app/server.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_connection_connection
    PRIVATE
      pc_mmgr_bytes
      pc_mmgr_protomem
  )
  protocore_module(pc_network_drivers_presentation_ssh_connection_connection
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/connection/connection.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_mmgr_bytes
      pc_mmgr_endian
      pc_mmgr_plaintext
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_presentation_ssh_app_server
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_server_server
      pc_network_drivers_presentation_ssh_ssh
      pc_network_drivers_presentation_ssh_transport_transport
      pc_network_drivers_transport_tcp_tcp
      pc_shared_ip_ip
      pc_shared_log_log
  )
endif()

# PROTOCORE_ENABLE_SSH_ZLIB
if(PROTOCORE_ENABLE_SSH_ZLIB)
  protocore_module(pc_network_drivers_presentation_ssh_auth_auth
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/auth/auth.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_crypto_asymmetric_ecdsa
      pc_crypto_asymmetric_ed25519
      pc_mmgr_bytes
      pc_mmgr_endian
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_transport_comp
      pc_network_drivers_presentation_ssh_transport_ssh_rsa
      pc_network_drivers_presentation_ssh_transport_transport
      pc_server_clock_clock
  )
  protocore_module(pc_network_drivers_presentation_ssh_network_network
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/network/network.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_network_drivers_presentation_ssh_auth_auth
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_ssh
      pc_network_drivers_presentation_ssh_transport_comp
      pc_network_drivers_presentation_ssh_transport_transport
      pc_network_drivers_transport_tcp_client_client
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_server_clock_clock
      pc_server_core_proto_handler
      pc_server_core_worker
  )
  protocore_module(pc_network_drivers_presentation_ssh_server_server
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/server/server.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_auth_auth
      pc_network_drivers_presentation_ssh_connection_connection
      pc_network_drivers_presentation_ssh_network_network
      pc_network_drivers_presentation_ssh_ssh
      pc_network_drivers_presentation_ssh_transport_comp
      pc_network_drivers_presentation_ssh_transport_transport
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_server_server
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_server_core_proto_handler
  )
  protocore_module(pc_network_drivers_presentation_ssh_transport_comp
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/comp.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_mmgr_secure
      pc_network_drivers_presentation_ssh_transport_inflate
      pc_network_drivers_presentation_ssh_transport_zlib
  )
  protocore_module(pc_network_drivers_presentation_ssh_transport_inflate
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/inflate.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_mmgr_protomem
      pc_network_drivers_presentation_codec_deflate_rfc1951
  )
  protocore_module(pc_network_drivers_presentation_ssh_transport_zlib
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/ssh/transport/zlib.c"
    PUBLIC
      pc_network_drivers_presentation_ssh_common
    PRIVATE
      pc_mmgr_bitio
      pc_mmgr_protomem
      pc_network_drivers_presentation_codec_deflate_rfc1951
  )
endif()

# PROTOCORE_ENABLE_STATS
if(PROTOCORE_ENABLE_STATS)
  protocore_module(pc_server_io_response
    SOURCES "${PROTOCORE_ROOT}/src/server/io/response.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_application_web_assets
      pc_network_drivers_presentation_http_http
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_shared_hex_hex
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_STATSD
if(PROTOCORE_ENABLE_STATSD)
  protocore_module(pc_services_iot_statsd_statsd
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/statsd/statsd.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_udp_client_client
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_STOMP
if(PROTOCORE_ENABLE_STOMP)
  protocore_module(pc_services_iot_stomp_stomp
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/stomp/stomp.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_STREAM_BODY
if(PROTOCORE_ENABLE_STREAM_BODY)
  protocore_module(pc_network_drivers_presentation_http_http_parser_http_parser
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/http_parser/http_parser.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_SUNSPEC
if(PROTOCORE_ENABLE_SUNSPEC)
  protocore_module(pc_services_energy_sunspec_sunspec
    SOURCES "${PROTOCORE_ROOT}/src/services/energy/sunspec/sunspec.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_SYSLOG
if(PROTOCORE_ENABLE_SYSLOG)
  protocore_module(pc_services_net_syslog_syslog
    SOURCES "${PROTOCORE_ROOT}/src/services/net/syslog/syslog.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_udp_client_client
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_TELEMETRY
if(PROTOCORE_ENABLE_TELEMETRY)
  protocore_module(pc_services_iot_telemetry_telemetry
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/telemetry/telemetry.c"
  )
endif()

# PROTOCORE_ENABLE_TELNET
if(PROTOCORE_ENABLE_TELNET)
  protocore_module(pc_network_drivers_presentation_telnet_telnet
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/telnet/telnet.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_server_core_proto_handler
  )
  protocore_module(pc_server_protocore_builtins
    SOURCES "${PROTOCORE_ROOT}/src/server/protocore_builtins.c"
    PRIVATE
      pc_network_drivers_presentation_presentation
      pc_network_drivers_presentation_ssh_server_server
      pc_network_drivers_presentation_telnet_telnet
      pc_server_core_proto_handler
      pc_services_fieldbus_modbus_modbus
      pc_services_opcua_opcua
  )
endif()

# PROTOCORE_ENABLE_THREAD
if(PROTOCORE_ENABLE_THREAD)
  protocore_module(pc_services_radio_thread_thread
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/thread/thread.c"
    PRIVATE
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_TIME_SOURCE
if(PROTOCORE_ENABLE_TIME_SOURCE)
  protocore_module(pc_services_timing_position_time_source_time_source
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/time_source/time_source.c"
    PRIVATE
      pc_shared_http_date_http_date
  )
endif()

# PROTOCORE_ENABLE_TLS
if(PROTOCORE_ENABLE_TLS)
  protocore_module(pc_network_drivers_transport_tcp_protocol_protocol
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/transport/tcp/protocol/protocol.c"
    PUBLIC
      pc_network_drivers_transport_tcp_evt
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_rawmemcpy
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_net_addr_net_addr
      pc_network_drivers_transport_tcp_common
      pc_network_drivers_transport_tcp_lower_lower
      pc_network_drivers_transport_tcp_server_server
      pc_server_clock_clock
      pc_server_core_worker
  )
endif()

# PROTOCORE_ENABLE_TLS_POLICY
if(PROTOCORE_ENABLE_TLS_POLICY)
  protocore_module(pc_server_security_tls_policy_tls_policy
    SOURCES "${PROTOCORE_ROOT}/src/server/security/tls_policy/tls_policy.c"
  )
endif()

# PROTOCORE_ENABLE_TOTP
if(PROTOCORE_ENABLE_TOTP)
  protocore_module(pc_services_security_totp_totp
    SOURCES "${PROTOCORE_ROOT}/src/services/security/totp/totp.c"
    PRIVATE
      pc_crypto_hash_sha1
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_mmgr_span
  )
endif()

# PROTOCORE_ENABLE_TRACE_CAPTURE
if(PROTOCORE_ENABLE_TRACE_CAPTURE)
  protocore_module(pc_server_signaling_trace_capture
    SOURCES "${PROTOCORE_ROOT}/src/server/signaling/trace_capture.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_UBX
if(PROTOCORE_ENABLE_UBX)
  protocore_module(pc_services_timing_position_ubx_ubx
    SOURCES "${PROTOCORE_ROOT}/src/services/timing_position/ubx/ubx.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_UDP_TELEMETRY
if(PROTOCORE_ENABLE_UDP_TELEMETRY)
  protocore_module(pc_services_iot_udp_telemetry_udp_telemetry
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/udp_telemetry/udp_telemetry.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_network_drivers_transport_udp_client_client
      pc_shared_ip_ip
  )
endif()

# PROTOCORE_ENABLE_UMATI
if(PROTOCORE_ENABLE_UMATI)
  protocore_module(pc_services_opcua_models_umati_umati
    SOURCES "${PROTOCORE_ROOT}/src/services/opcua/models/umati/umati.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protostr
      pc_services_opcua_opcua
  )
endif()

# PROTOCORE_ENABLE_UPLOAD
if(PROTOCORE_ENABLE_UPLOAD)
  protocore_module(pc_network_drivers_application_upload_service_upload_service
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/upload_service/upload_service.c"
    PRIVATE
      pc_mmgr_protoframe
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_server_storage_mnt
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_UTMC
if(PROTOCORE_ENABLE_UTMC)
  protocore_module(pc_services_transportation_utmc_utmc
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/utmc/utmc.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_VL53L0X
if(PROTOCORE_ENABLE_VL53L0X)
  protocore_module(pc_server_peripherals_vl53l0x_vl53l0x
    SOURCES "${PROTOCORE_ROOT}/src/server/peripherals/vl53l0x/vl53l0x.c"
    PRIVATE
      pc_mmgr_secure
      pc_server_peripherals_i2c
  )
endif()

# PROTOCORE_ENABLE_VXI11
if(PROTOCORE_ENABLE_VXI11)
  protocore_module(pc_services_instrumentation_vxi11_vxi11
    SOURCES "${PROTOCORE_ROOT}/src/services/instrumentation/vxi11/vxi11.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_WAL
if(PROTOCORE_ENABLE_WAL)
  protocore_module(pc_services_storage_wal_wal
    SOURCES "${PROTOCORE_ROOT}/src/services/storage/wal/wal.c"
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
  protocore_module(pc_services_storage_wal_wal_store
    SOURCES "${PROTOCORE_ROOT}/src/services/storage/wal/wal_store.c"
    PUBLIC
      pc_services_storage_wal_wal
    PRIVATE
      pc_mmgr_endian
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_WAMP
if(PROTOCORE_ENABLE_WAMP)
  protocore_module(pc_services_iot_wamp_wamp
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/wamp/wamp.c"
    PRIVATE
      pc_network_drivers_presentation_codec_json_json
  )
endif()

# PROTOCORE_ENABLE_WAVE
if(PROTOCORE_ENABLE_WAVE)
  protocore_module(pc_services_transportation_wave_wave
    SOURCES "${PROTOCORE_ROOT}/src/services/transportation/wave/wave.c"
    PRIVATE
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_WEARLEVEL
if(PROTOCORE_ENABLE_WEARLEVEL)
  protocore_module(pc_server_storage_wearlevel
    SOURCES "${PROTOCORE_ROOT}/src/server/storage/wearlevel.c"
  )
endif()

# PROTOCORE_ENABLE_WEBDAV
if(PROTOCORE_ENABLE_WEBDAV)
  protocore_module(pc_network_drivers_application_webdav_webdav
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/application/webdav/webdav.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_shared_hex_hex
  )
  protocore_module(pc_server_io_webdav_handler
    SOURCES "${PROTOCORE_ROOT}/src/server/io/webdav_handler.c"
    PRIVATE
      pc_crypto_rng_rng
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_network_drivers_application_webdav_webdav
      pc_network_drivers_presentation_http_http
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_network_drivers_presentation_http_route_http_route
      pc_network_drivers_transport_tcp_tcp
      pc_server_clock_clock
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_WEBHOOK
if(PROTOCORE_ENABLE_WEBHOOK)
  protocore_module(pc_services_net_webhook_webhook
    SOURCES "${PROTOCORE_ROOT}/src/services/net/webhook/webhook.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_services_net_http_client_http_client
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_WEBSOCKET
if(PROTOCORE_ENABLE_WEBSOCKET)
  protocore_module(pc_network_drivers_presentation_http_websocket_websocket
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/http/websocket/websocket.c"
    PUBLIC
      pc_network_drivers_presentation_codec_deflate_deflate
      pc_network_drivers_presentation_codec_inflate_inflate
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_mmgr_secure
      pc_mmgr_span
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_shared_utf8_utf8
  )
  protocore_module(pc_network_drivers_presentation_presentation
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/presentation.c"
    PUBLIC
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_network_drivers_session_session
      pc_network_drivers_transport_tcp_evt
    PRIVATE
      pc_mmgr_protostr
      pc_network_drivers_presentation_http_http2_h2_server
      pc_network_drivers_presentation_http_sse_sse
      pc_network_drivers_presentation_http_websocket_websocket
      pc_network_drivers_session_sse_sse
      pc_network_drivers_session_ws_ws
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_server_core_proto_handler
  )
  protocore_module(pc_network_drivers_session_ws_ws
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/session/ws/ws.c"
    PRIVATE
      pc_network_drivers_presentation_http_websocket_websocket
  )
  protocore_module(pc_server_io_websocket_sse
    SOURCES "${PROTOCORE_ROOT}/src/server/io/websocket_sse.c"
    PRIVATE
      pc_crypto_hash_sha1
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_mmgr_span
      pc_network_drivers_presentation_codec_base64_base64
      pc_network_drivers_presentation_http_http_parser_http_parser
      pc_network_drivers_presentation_http_sse_sse
      pc_network_drivers_presentation_http_websocket_websocket
      pc_network_drivers_session_sse_sse
      pc_network_drivers_session_ws_ws
      pc_network_drivers_transport_tcp_protocol_protocol
      pc_network_drivers_transport_tcp_tcp
  )
endif()

# PROTOCORE_ENABLE_WEB_TERMINAL
if(PROTOCORE_ENABLE_WEB_TERMINAL)
  protocore_module(pc_server_web_web_terminal_web_terminal
    SOURCES "${PROTOCORE_ROOT}/src/server/web/web_terminal/web_terminal.c"
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_secure
      pc_network_drivers_application_web_assets
      pc_shared_mime_mime
  )
endif()

# PROTOCORE_ENABLE_WIFI_SNIFFER
if(PROTOCORE_ENABLE_WIFI_SNIFFER)
  protocore_module(pc_services_radio_wifi_sniffer_wifi_sniffer
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/wifi_sniffer/wifi_sniffer.c"
    PRIVATE
      pc_mmgr_plaintext
      pc_mmgr_protomem
      pc_server_clock_clock
      pc_services_radio_promisc_promisc
      pc_shared_pcap_pcap
  )
endif()

# PROTOCORE_ENABLE_WISUN
if(PROTOCORE_ENABLE_WISUN)
  protocore_module(pc_services_radio_wisun_wisun
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/wisun/wisun.c"
    PUBLIC
      pc_shared_ip_ip
    PRIVATE
      pc_mmgr_membuild
      pc_mmgr_protomem
  )
endif()

# PROTOCORE_ENABLE_WS_CLIENT
if(PROTOCORE_ENABLE_WS_CLIENT)
  protocore_module(pc_services_net_ws_client_ws_client
    SOURCES "${PROTOCORE_ROOT}/src/services/net/ws_client/ws_client.c"
    PRIVATE
      pc_crypto_hash_sha1
      pc_crypto_rng_rng
      pc_mmgr_membuild
      pc_mmgr_protomem
      pc_mmgr_protostr
      pc_mmgr_secure
      pc_mmgr_span
      pc_network_drivers_presentation_codec_base64_base64
      pc_network_drivers_tls_tls
      pc_network_drivers_transport_tcp_client_client
      pc_server_clock_clock
  )
endif()

# PROTOCORE_ENABLE_WS_DEFLATE
if(PROTOCORE_ENABLE_WS_DEFLATE)
  protocore_module(pc_network_drivers_presentation_codec_deflate_deflate
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/deflate/deflate.c"
    PRIVATE
      pc_mmgr_bitio
      pc_mmgr_protomem
      pc_network_drivers_presentation_codec_deflate_rfc1951
  )
  protocore_module(pc_network_drivers_presentation_codec_inflate_inflate
    SOURCES "${PROTOCORE_ROOT}/src/network_drivers/presentation/codec/inflate/inflate.c"
    PRIVATE
      pc_mmgr_protomem
      pc_network_drivers_presentation_codec_deflate_rfc1951
  )
endif()

# PROTOCORE_ENABLE_XMPP
if(PROTOCORE_ENABLE_XMPP)
  protocore_module(pc_services_iot_xmpp_xmpp
    SOURCES "${PROTOCORE_ROOT}/src/services/iot/xmpp/xmpp.c"
    PRIVATE
      pc_mmgr_protomem
      pc_mmgr_protostr
  )
endif()

# PROTOCORE_ENABLE_ZIGBEE
if(PROTOCORE_ENABLE_ZIGBEE)
  protocore_module(pc_services_radio_zigbee_zigbee
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/zigbee/zigbee.c"
    PRIVATE
      pc_shared_crc_crc
  )
endif()

# PROTOCORE_ENABLE_ZWAVE
if(PROTOCORE_ENABLE_ZWAVE)
  protocore_module(pc_services_radio_zwave_zwave
    SOURCES "${PROTOCORE_ROOT}/src/services/radio/zwave/zwave.c"
  )
endif()
