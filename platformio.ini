[platformio]
default_envs = esp32dev

[env:esp32dev]
; Unpinned, tracking the default branch, so development happens on the current
; toolchain rather than a frozen one.
;
; The registry's espressif32 cannot deliver that: 7.0.1 still hard-pins
; framework-arduinoespressif32 ~3.20017.0, which is Arduino core 2.0.17, IDF 4.4.7
; and gcc 8.4. Its sdk covers four dies (esp32, s2, s3, c3) and none of c5, c6,
; c61, h2 or p4. pioarduino carries Arduino core 3.x, IDF 5.x, gcc 14.2 and every
; die, so the radio and peripheral work can build against the framework this
; project actually ships on.
;
; mbedtls goes v2 to v3 with core 3.x; ssh_rsa.c guards on MBEDTLS_VERSION_MAJOR
; and builds either way. See docs/TODO.md "Build / toolchain".
platform = https://github.com/pioarduino/platform-espressif32.git
board = esp32dev
framework = arduino
monitor_speed = 115200

; src/ is C11 (docs/SRC_LAW.md section 0): _Atomic and static_assert do not exist below it. The
; framework builder appends -std=gnu99 to CFLAGS after everything in build_flags, and gcc takes the
; last -std it is given, so naming gnu11 alone is overridden and the flag has to be removed first.
; C++ is untouched: -std=gnu++11 lands in CXXFLAGS, and only the C flag is replaced here.
; library.json states the same pair, for a consumer that builds this as a dependency.
build_unflags = -std=gnu99
build_flags = -std=gnu11

; >>> GENERATED TEST ENVS - do not edit below; edit test/test_matrix.json and run test/gen_test_envs.py >>>
; Single source of truth: test/test_matrix.json  (326 native envs, 2 stack bases)

; Shared flags for all native environments
[native_base]
platform = native
build_flags =
    ; src/ is C11 (docs/SRC_LAW.md section 0).
    -std=c11
    ; strnlen is POSIX 2008, not ISO C11, and -std=c11 asks glibc for ISO C alone - so <string.h>
    ; declares strlen and not strnlen. Ban #1 (docs/SRCBANNED.md) requires strnlen everywhere, so
    ; without this the whole tree compiles it as an implicit declaration returning int, and every
    ; `size_t n = strnlen(...)` truncates through 32 bits with nothing but a warning. Naming the
    ; POSIX level is what makes the bounded string functions visible; the language stays C11.
    -D_POSIX_C_SOURCE=200809L
    ; src/ contains no throw / try / catch (no-heap, no-stdlib, deterministic - and the ESP32
    ; target builds without exceptions anyway), so this only strips what the host toolchain would
    ; add on its own. It matters for coverage: with exceptions on, g++ emits an unwind edge at
    ; every call to a function that is not noexcept, and gcov counts each one as a BRANCH. Those
    ; edges are unreachable by construction in a codebase that never throws, so they are pure
    ; noise in the branch numbers and they make 100% branch coverage unattainable - wamp.cpp alone
    ; carried 114 of them (280 branches -> 166, and 57 of its 84 "uncovered" branches vanished).
    ; Keep this here rather than only in the coverage run so the tested build and the measured
    ; build are the same build.
    -fno-exceptions
    -I test/mocks
    -I test/support
    -I src
    ; core_setup/ sits beside src/, not inside it, so the board profile an include names as
    ; core_setup/board_profiles/... resolves from the repo root rather than from src/.
    -I .
    ; PROTOCORE_HOST is NOT defined here. board_profiles/pc_platform.h derives it from the vendor
    ; axis: nothing on a native build matches a vendor, so its else-arm defines it. Passing it on
    ; the command line as well made a second source of truth that the vendor axis could not
    ; libm is a separate library to the C driver. g++ pulled it in behind libstdc++, so nothing here
    ; ever named it; gcc does not, and every env whose sources call sin / cos / sqrt / atan2 / fabs
    ; fails at the link with an undefined reference instead. It sits in the shared block because
    ; which envs reach a math call is a property of their sources, not of their configuration.
    -lm
; The filesystem the device actually runs, so a host test asserts against real directory
; semantics rather than a hand-rolled tree that agrees with itself. Only the envs whose sources
; include lfs.h build it - the dependency finder does not compile what nothing reaches for.
lib_deps =
    anurag3301/littlefs@^2.11.6
test_build_src = yes

; CANopen (CiA 301) message codec (services/fieldbus/canopen): NMT, SYNC, heartbeat, EMCY,
; PDO, and expedited SDO read/write/abort + the COB-ID classifier, over the shared CAN
; frame (shared_primitives/can.h). Pure host tests.
[env:native_canopen]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CANOPEN=1
build_src_filter =
    +<services/fieldbus/canopen/canopen.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_canopen

; CiA 402 / IEC 61800-7-201 drive profile (services/fieldbus/cia402): the Statusword power-state
; decode (mask/value table), the Controlword commands + enable sequence, Statusword flags, the
; CANopen SDO setters (Controlword / Modes / target position-velocity-torque) and cyclic PDO
; pack/unpack. Layers on services/canopen. Pure host tests.
[env:native_cia402]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CIA402=1
    -DPC_ENABLE_CANOPEN=1
build_src_filter =
    +<services/fieldbus/cia402/cia402.c>
    +<services/fieldbus/canopen/canopen.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_cia402

; PID control law (services/system/control): the P/I/D terms, output clamping, anti-windup by
; conditional integration, derivative-on-measurement (no setpoint kick) + optional low-pass,
; feed-forward, the batched multi-axis update, and the inline control-law primitives (clamp /
; deadband / slew / low-pass). Pure single-precision float; host tests.
[env:native_control]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CONTROL=1
build_src_filter =
    +<services/system/control/control.c>
    +<mmgr/protomem.c>
test_filter =
    unit/system/test_control

; Log-structured hash key-value store on the WAL (services/storage/dbm): put/get/delete with an
; in-RAM open-addressed index and value data appended to the write-ahead log, plus index rebuild by
; replaying the log on open. Pure host tests over a RAM-backed WalDev, incl. overwrite, tombstone
; resurrection, persistence across a remount (with and without checkpoint), collisions, index-full
; fail-closed, key/value bounds, and a max-size value round-trip.
[env:native_dbm]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAL=1
    -DPC_ENABLE_DBM=1
build_src_filter =
    +<services/storage/wal/wal.c>
    +<services/storage/wal/wal_store.c>
    +<services/storage/dbm/dbm.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_dbm

; Local JSON document store on the WAL (services/storage/docstore): JSON documents addressed by id,
; stored via dbm on the write-ahead log, plus top-level field queries (find documents whose JSON field
; equals a value) via the zero-heap JSON reader. Pure host tests over a RAM-backed WalDev, incl.
; put/get/delete, string/int/bool field finds, persistence + query across a remount, and early stop.
[env:native_docstore]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAL=1
    -DPC_ENABLE_DBM=1
    -DPC_ENABLE_DOCSTORE=1
build_src_filter =
    +<services/storage/wal/wal.c>
    +<services/storage/wal/wal_store.c>
    +<services/storage/dbm/dbm.c>
    +<services/storage/docstore/docstore.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_docstore

; CNC DNC drip-feed (services/machine_tool/dnc): the EIA RS-244 <-> ISO/ASCII tape-code translation
; (odd-parity EIA table), ISO even parity, G-code block framing with '%' rewind-stop and leader
; runout, XON/XOFF flow-control state, a streaming block reassembling decoder, and the dnc_stream
; drip-feed engine (leader / markers / blocks over a send/recv seam, paced on reverse-channel
; XON/XOFF). Pure host tests, incl. an odd-parity + exact-inverse table guardrail, full encode ->
; decode program round-trips for both tape codes, and a scripted mock controller that decodes the
; streamed program back and exercises the XOFF/XON pause-resume path.
[env:native_dnc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DNC=1
build_src_filter =
    +<services/machine_tool/dnc/dnc.c>
    +<services/machine_tool/dnc/dnc_stream.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_dnc
    protocols/machine_tool/test_dnc_stream

; FTP client wire codec (services/file_transfer/ftp, RFC 959 + RFC 2428): the control-command
; builders (generic verb + PORT + EPRT), the single/multi-line 3-digit reply parser, and the
; PASV / EPSV data-address decoders. Pure host tests with authentic reply strings captured from a
; live FTP server (multiline greeting + FEAT, PASV/EPSV tuples).
[env:native_ftp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FTP=1
build_src_filter =
    +<services/file_transfer/ftp/ftp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/file_transfer/test_ftp

; HTTP Cache-Control helpers (services/web/httpcache, RFC 9111 + 8246 + 5861): the
; structured directive builder + first-class origin presets (immutable asset / shared / no-store /
; revalidatable), the tolerant directive parser (case-insensitive, quoted deltas, unknown ignored),
; and the RFC 9111 freshness-lifetime precedence. Pure host tests incl. a build->parse round-trip.
[env:native_httpcache]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_CACHE=1
build_src_filter =
    +<services/web/httpcache/httpcache.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_httpcache

; CDN edge-cache engine (services/web/edge_cache): the pure freshness/validator core (response
; header-field access, HTTP-date parsing over IMF-fixdate / RFC 850 / asctime, RFC 9111 lifetime +
; Expires-Date + heuristic + corrected age, the canonical cache key + SHA-256 digest + Vary secondary
; key, and the L1 LRU/TTL store + storeability rules), plus the async origin-fetch engine (completion
; detection over Content-Length / chunked / connection-close, and the state machine over a mock transport
; seam), plus the shared single-range `Range: bytes=...` parser (http_parse_byte_range) that backs serving
; 206 Partial Content windows from a cached body. Pure host tests.
[env:native_edge_cache]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_CACHE=1
    -DPC_ENABLE_HTTP_CLIENT=1
    -DPC_ENABLE_EDGE_CACHE=1
    -DPC_ENABLE_RANGE=1
build_src_filter =
    +<services/web/httpcache/httpcache.c>
    +<services/net/http_client/http_client.c>
    +<services/web/edge_cache/edge_cache.c>
    +<services/web/edge_cache/edge_fetch.c>
    +<network_drivers/application/http_range.c>
    +<crypto/hash/sha256.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/web/test_edge_cache
    protocols/web/test_edge_fetch

; CDN edge-cache L2 SD-persistence tier (services/web/edge_cache/edge_cache_sd): the entry <->
; dbm-value serialization roundtrip (all response metadata, Vary variants, binary and max-size bodies),
; the spill/promote/del/purge API over a dbm on a RAM-backed WalDev, the L1 store's on_evict write-back
; hook, the storeability gates (no-validator and oversize entries stay L1-only), reboot survival via
; remount + index replay, and that a shared dbm's foreign values are never touched. PC_DBM_VAL_MAX is
; sized below a full entry so the oversize-stays-L1 path is exercised. Pure host tests.
[env:native_edge_cache_sd]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAL=1
    -DPC_ENABLE_DBM=1
    -DPC_DBM_VAL_MAX=1024
    -DPC_ENABLE_HTTP_CACHE=1
    -DPC_ENABLE_HTTP_CLIENT=1
    -DPC_ENABLE_EDGE_CACHE=1
build_src_filter =
    +<services/storage/wal/wal.c>
    +<services/storage/wal/wal_store.c>
    +<services/storage/dbm/dbm.c>
    +<services/web/httpcache/httpcache.c>
    +<services/net/http_client/http_client.c>
    +<services/web/edge_cache/edge_cache.c>
    +<services/web/edge_cache/edge_cache_sd.c>
    +<crypto/hash/sha256.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/web/test_edge_cache_sd

; CDN edge-cache mesh sibling-cache codec (services/web/edge_cache/edge_mesh): the request/response
; wire frames (build + tri-state accumulating parse over partial reads, magic/version/opcode validation),
; the freshness-carrying entry frame (the shared edge_sd body plus a timing trailer) round-tripped
; byte-exact, RFC 9111 age propagation (a peer's corrected age transfers so the receiver stays fresh for
; the remaining lifetime and its Age keeps growing), and the async peer-query engine reaching HIT / MISS /
; FAILED (timeout, peer close, malformed) over a scripted mock transport seam. Pure host tests.
[env:native_edge_mesh]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_CACHE=1
    -DPC_ENABLE_HTTP_CLIENT=1
    -DPC_ENABLE_EDGE_CACHE=1
    -DPC_ENABLE_EDGE_MESH=1
build_src_filter =
    +<services/web/httpcache/httpcache.c>
    +<services/net/http_client/http_client.c>
    +<services/web/edge_cache/edge_cache.c>
    +<services/web/edge_cache/edge_cache_sd.c>
    +<services/web/edge_cache/edge_mesh.c>
    +<crypto/hash/sha256.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/web/test_edge_mesh

; Shared no-stdlib primitives (shared_primitives): the base-10 pc_strtol/strtoul/strtof
; number parsers (numparse.h), the strict RFC 3629 UTF-8 validator (utf8.h), and the parameterized
; Rocksoft/Williams CRC engine (crc.h). Every preset is asserted against its published cataloge check
; value (the CRC of "123456789"), and the engine is then diffed against the hand-rolled CRC loops the
; in-tree services used to carry - X-25, CCITT-FALSE, ARC, MODBUS, DNP, CRC-32, ESP3 CRC-8 and the
; Sensirion CRC-8 - across every length 0..64. Those loops are interop-proven against real peers, so
; the 13 services now calling the engine are proven byte-identical to the code they retired rather
; than merely plausible; the transcriptions here are what still pins that behaviour. Plus the streaming
; split, the reflect flags, width masking, and single-bit-flip detection. Header-only; pure host tests.
[env:native_primitives]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/shared_primitives/test_primitives
    unit/shared_primitives/test_crc

; IP address core (network_drivers/network/pc_ip): RFC 4291 IPv4/IPv6 text parsing,
; RFC 5952 canonical formatting (:: zero-compression, v4-mapped), and scope classification
; (loopback / link-local / private-ULA / multicast / global). Pure host tests.
[env:native_ip]
extends = native_base
build_src_filter =
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/shared_primitives/test_ip

; Unified double-ended server arena (network_drivers/session/pc_arena): first-fit
; persistent end (bottom, individual free + coalesce + boundary shrink) + bump scratch end
; (top, mark/release/reset) sharing a floating middle; fail-closed at the boundary. Host tests.
[env:native_arena]
extends = native_base
build_src_filter =
    +<mmgr/arena.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_arena

; Modern SSH crypto KATs (curve25519-sha256 KEX + ssh-ed25519 host key / client auth):
; SHA-512 (FIPS 180-4), X25519 (RFC 7748), Ed25519 (RFC 8032). Vectors are the published RFC
; test vectors + tool-generated digests, grounding the impl against the standards. Pure host tests.
[env:native_ssh_ed25519]
extends = native_base
build_src_filter =
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/protomem.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
test_filter =
    unit/ssh/test_ssh_ed25519

; SSH client-to-server resumable INFLATE (ssh_inflate): decompresses OpenSSH's per-packet
; Z_PARTIAL_FLUSH zlib stream across packets with a 32 KB context-takeover window. Verified against
; golden vectors produced by real zlib (windowBits 15, Z_PARTIAL_FLUSH per packet; tools/crypto/gen_ssh_inflate_vectors.py),
; covering fixed/dynamic/stored blocks, cross-packet back-references, and empty/tiny payloads. Pure host tests.
[env:native_ssh_inflate]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_ZLIB=1
build_src_filter =
    +<network_drivers/presentation/ssh/transport/ssh_inflate.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_inflate

; Data-driven external crypto known-answer tests: HMAC-SHA256/512, AEAD_AES_128_GCM,
; X25519, and Ed25519 verify from Project Wycheproof (including its adversarial edge cases),
; plus HKDF-SHA256 Extract (RFC 5869), the ChaCha20 block (RFC 8439 2.4.2), and Poly1305
; (RFC 8439 2.5.2). Vectors are vendored as auditable JSON (test/vectors) and compiled to a
; table by tools/crypto/gen_crypto_vectors.py. Pure host tests.
[env:native_crypto_kat]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/mac/poly1305.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/crypto/test_crypto_kat

; Wi-Fi promiscuous capture helpers (services/radio/promisc): the pure 802.11 MAC header parser
; (to/from-DS src/dst/bssid resolution, QoS, WDS 4-address, control frames, malformed
; rejection) and libpcap (DLT_IEEE802_11) framing. Pure host tests.
[env:native_promisc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PROMISC=1
build_src_filter =
    +<services/radio/promisc/promisc.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_promisc

; CAN listen-only capture framing (server/signaling/bus_capture): can_to_socketcan() building the
; 16-byte Linux SocketCAN frame (big-endian can_id, EFF/RTR flags, length, data) and the
; DLT_CAN_SOCKETCAN libpcap link type. Pure host tests.
[env:native_bus_capture]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_BUS_CAPTURE=1
build_src_filter =
    +<server/signaling/bus_capture.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_bus_capture

; SAE J1939 codec (services/fieldbus/j1939): 29-bit id encode/decode (PDU1 + PDU2), single-frame
; messages, Request PGN, Address Claimed + NAME, and the Transport Protocol (BAM + TP.DT)
; reassembler, over the shared CAN frame. Pure host tests.
[env:native_j1939]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_J1939=1
build_src_filter =
    +<services/fieldbus/j1939/j1939.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_j1939

; DeviceNet link-adaptation codec (services/fieldbus/devicenet): the 4-group 11-bit CAN id,
; explicit-message header octet, single-frame explicit messages, and the fragmentation
; reassembler (CIP over CAN; the CIP body is built with the cip codec). Pure host tests.
[env:native_devicenet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DEVICENET=1
build_src_filter =
    +<services/fieldbus/devicenet/devicenet.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_devicenet

; NMEA 2000 codec (services/timing_position/nmea2000): single-frame messages plus the Fast Packet
; transport (frame count, build, reassembly), built on the J1939 id codec (implied). Pure
; host tests.
[env:native_nmea2000]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NMEA2000=1
build_src_filter =
    +<services/timing_position/nmea2000/nmea2000.c>
    +<services/fieldbus/j1939/j1939.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_nmea2000

; Wired M-Bus codec (services/fieldbus/mbus): the ACK / short / long frame builders + parser
; (start/stop, doubled length, 8-bit sum checksum) and the EN 13757-3 variable-data record
; walker (DIF/VIF, DIFE/VIFE chains, LVAR). Pure host tests.
[env:native_mbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MBUS=1
build_src_filter =
    +<services/fieldbus/mbus/mbus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_mbus

; IEC 60870-5-101/-104 codec (services/energy/iec60870): the -104 APCI (I/S/U), the ASDU
; header + 3-octet IOA, and the -101 FT1.2 fixed/variable link frames (sum checksum). Pure
; host tests.
[env:native_iec60870]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IEC60870=1
build_src_filter =
    +<services/energy/iec60870/iec60870.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_iec60870

; SDI-12 sensor-bus codec (services/peripherals/sdi12): the command builders, the measurement
; response parser (atttn), the data-value splitter, and the SDI-12 CRC (compute/encode/verify).
; Pure host tests.
[env:native_sdi12]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SDI12=1
build_src_filter =
    +<services/peripherals/sdi12/sdi12.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_sdi12

; DMX512 + RDM lighting codec (services/peripherals/dmx): the DMX512 slot packet (build/get) and the
; RDM (ANSI E1.20) packet build/parse with 48-bit UIDs and the 16-bit additive checksum. Pure
; host tests.
[env:native_dmx]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DMX=1
build_src_filter =
    +<services/peripherals/dmx/dmx.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_dmx

; NMEA 0183 sentence codec (services/timing_position/nmea0183): the XOR checksum, sentence build, parse
; (field splitting, talker/type, checksum validation) against the canonical GGA vector, and the
; field-value helpers. Pure host tests.
[env:native_nmea0183]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NMEA0183=1
build_src_filter =
    +<services/timing_position/nmea0183/nmea0183.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_nmea0183

; UBX (u-blox binary GNSS protocol) codec (services/timing_position/ubx): B5 62 framing, 8-bit Fletcher checksum,
; build/poll/parse, and the streaming NMEA+UBX demultiplexer. KATs are the published MON-VER /
; CFG-PRT poll frames. Pure host tests.
[env:native_ubx]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_UBX=1
build_src_filter =
    +<services/timing_position/ubx/ubx.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_ubx

; PTP / IEEE 1588-2008 (PTPv2) message codec + slave clock math (network_drivers/application/ptp): the 34-octet common
; header, 10-octet timestamp, Sync/Delay_Req/Follow_Up/Delay_Resp/Announce build+parse, and the
; offset/mean-path-delay calculation from t1..t4. Pure host tests.
[env:native_ptp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PTP=1
build_src_filter =
    +<network_drivers/application/ptp/ptp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_ptp

; Wi-Fi roaming decision layer (network_drivers/network/roaming): the pure policy that fuses the current RSSI, a
; candidate neighbour list, and an optional 802.11v BTM hint into a roam/stay decision (target BSSID +
; channel + reason), with a hysteresis margin and a disassoc-imminent override. Pure host tests.
[env:native_roaming]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ROAMING=1
build_src_filter =
    +<network_drivers/datalink/roaming.c>
    +<mmgr/protomem.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
test_filter =
    unit/datalink/test_roaming

; IO-Link (SDCI) data-link message codec (services/fieldbus/iolink): the MC / CKT / CKS control
; octets and the SDCI checksum (seed 0x52 + the 8->6 compression of IO-Link spec A.1.6), with a
; hand-computed known-answer vector. Pure host tests.
[env:native_iolink]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IOLINK=1
build_src_filter =
    +<services/fieldbus/iolink/iolink.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_iolink

; Layers 4-6 stack: transport + session + presentation + the standalone parser, no app layer. Base env for the suites that build on it; it runs no suite of its own.
; These suites predate the RFC 7230 §5.4 Host rule and feed bare HTTP/1.1
; request lines to exercise parser mechanics; Host enforcement is verified
; separately in env:native_compliance.
[native_stack_l46]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<mmgr/protostr.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>

; test_transport against the native_stack_l46 stack.
[env:native_transport]
extends = native_stack_l46
test_filter =
    integration/transport/test_transport

; test_presentation against the native_stack_l46 stack.
[env:native_presentation]
extends = native_stack_l46
test_filter =
    integration/presentation/test_presentation

; test_session against the native_stack_l46 stack.
[env:native_session]
extends = native_stack_l46
test_filter =
    integration/presentation/test_session

; test_http_parser against the native_stack_l46 stack.
[env:native_http_parser]
extends = native_stack_l46
test_filter =
    unit/http/test_http_parser

; test_websocket against the native_stack_l46 stack.
[env:native_websocket]
extends = native_stack_l46
test_filter =
    integration/http/test_websocket

; test_sse against the native_stack_l46 stack.
[env:native_sse]
extends = native_stack_l46
build_flags =
    ${native_stack_l46.build_flags}
    -DPC_ENABLE_SSE=1
test_filter =
    integration/http/test_sse

; test_base64 against the native_stack_l46 stack.
[env:native_base64]
extends = native_stack_l46
test_filter =
    unit/codec/test_base64

; Transport observability (PC_ENABLE_OBSERVABILITY): the pc_conn_on_event
; hook, by-reason counters, the live CONN_CLOSING gauge, and that the real lwIP
; callbacks (recv FIN / error / timeout / local close / backpressure) drive the
; right counter. Pure L4 core - host-tested.
[env:native_observability]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OBSERVABILITY=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_observability

; base64 scalar constant-time decode fallback (PC_BASE64_SWAR=0): classify one character at a
; time instead of the default SWAR four-per-word path. Runs the same RFC 4648 vectors, both alphabets,
; malformed-input rejection, capacity guard, and round-trip fuzz as the SWAR path (covered by the same
; test_base64 in env:native, where SWAR is the default) - proving the two decoders are byte-identical.
[env:native_base64_scalar]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_BASE64_SWAR=0
build_src_filter =
    +<network_drivers/presentation/codec/base64/base64.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_base64

; DiffServ QoS marking (PC_ENABLE_DIFFSERV): the DSCP->TOS encode (DSCP << 2, ECN 0),
; the server-wide and UDP DSCP defaults (set/get, 6-bit mask), the per-connection setter
; (pc_conn_set_dscp writes pcb->tos on the host path), and the per-listener override
; (pc_listen_set_dscp incl. the UNSET sentinel + mask + unknown-port miss). Compiles the
; flag-guarded transport paths no other env builds. Pure L4 core - host-tested.
[env:native_diffserv]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DIFFSERV=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<network_drivers/transport/diffserv.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_diffserv

; Accept-time connection gates with their flags ON (PC_ENABLE_ACCEPT_THROTTLE
; / PER_IP_THROTTLE / IP_ALLOWLIST): the global fixed-window throttle, the per-source-IP
; bucket table (independent budgets, window rollover, the millis() wrap, bounded LRU
; eviction), and the CIDR source-IP allowlist. Compiles the flag-guarded accept-callback
; paths that no other env builds. Budgets overridden small so the boundaries are explicit.
[env:native_accept_gate]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_ACCEPT_THROTTLE=1
    -DPC_ENABLE_PER_IP_THROTTLE=1
    -DPC_ENABLE_IP_ALLOWLIST=1
    -DPC_ACCEPT_THROTTLE_MAX=3
    -DPC_ACCEPT_THROTTLE_WINDOW_MS=1000
    -DPC_PER_IP_THROTTLE_MAX=2
    -DPC_PER_IP_THROTTLE_WINDOW_MS=1000
    -DPC_PER_IP_THROTTLE_SLOTS=4
    -DPC_IP_ALLOWLIST_SLOTS=4
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<mmgr/protostr.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_accept_gate

; Parser streaming-body hook (OTA) - exercises http_parser with PC_ENABLE_OTA=1
; using a mock sink (no ESP32 Update dependency).
[env:native_ota]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_OTA=1
build_src_filter =
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_http_ota

; Provisioning form-field parser - the only host-testable part of the captive
; portal (softAP / lwIP UDP / NVS are ESP32-only and compiled out here).
[env:native_prov]
extends = native_base
build_src_filter =
    +<services/system/provisioning_service/provisioning_service.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/system/test_provisioning

; SSH crypto layer (native software paths only, no mbedtls dependency); channels multiplexed (PC_SSH_MAX_CHANNELS=3) to exercise routing; SFTP/SCP subsystem routing on (MNT satisfies the guard - the file-transfer servers reach storage through the mount, not through FILE_SERVING)
[env:native_ssh]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_SSH_MAX_CHANNELS=3
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_MNT=1
    -DPC_ENABLE_SSH_SFTP=1
    -DPC_ENABLE_SSH_SCP=1
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    unit/ssh/test_ssh_crypto
    integration/ssh/test_ssh_transport
    integration/ssh/test_ssh_auth
    integration/ssh/test_ssh_channel
    integration/ssh/test_ssh_server
extra_scripts =
    pre:test/ensure_test_keys.py

; SSH keyboard-interactive auth (RFC 4256) built with PC_ENABLE_SSH_KEYBOARD_INTERACTIVE=1: the server sends one non-echoed Password prompt (INFO_REQUEST) and verifies the INFO_RESPONSE via the password callback. Also re-runs test_ssh_auth to confirm the password/publickey methods are unaffected with keyboard-interactive advertised.
[env:native_ssh_kbdint]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_KEYBOARD_INTERACTIVE=1
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    integration/ssh/test_ssh_kbdint
    integration/ssh/test_ssh_auth
extra_scripts =
    pre:test/ensure_test_keys.py

; mlkem768x25519-sha256 SSH hybrid KEX (draft-ietf-sshm-mlkem-hybrid-kex) end to end: the
; full SSH transport built with PC_ENABLE_PQC_KEX=1 plus the ML-KEM-768 / SHA-3 core. Verifies the
; hybrid is negotiated, then runs a real server-side key exchange and checks it as a conforming client
; would - ML-KEM Decaps (an independent reference) + X25519, K = SHA256(K_PQ || K_CL), the string-K
; exchange hash and KDF, and the ed25519 signature over H.
[env:native_ssh_pqc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_SSH_MAX_CHANNELS=3
    -DPC_ENABLE_PQC_KEX=1
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<crypto/hash/sha3.c>
    +<crypto/pqc/mlkem.c>
    +<crypto/pqc/sntrup761.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    integration/ssh/test_ssh_pqc
extra_scripts =
    pre:test/ensure_test_keys.py

; SSH built with password auth disabled (publickey-only hardening)
[env:native_ssh_hardened]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_SSH_ALLOW_PASSWORD=0
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    integration/ssh/test_ssh_hardening
extra_scripts =
    pre:test/ensure_test_keys.py

; SSH wired through the real transport/session layers (PROTO_SSH byte-pump)
[env:native_ssh_conn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<network_drivers/presentation/ssh/connection/ssh_conn.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    integration/ssh/test_ssh_conn
extra_scripts =
    pre:test/ensure_test_keys.py

; SFTP protocol v3 wire codec (network_drivers/application/sftp): the SSH_FXP_* request reader + response builders
; (VERSION / STATUS / HANDLE / DATA / ATTRS / NAME), the ATTRS blob encode/decode round-trip (size / perms /
; mtime + the flag word, skipping unknown/extended fields), packet length-framing (need-more vs
; malformed/too-big), and the ls -l longname formatter. Pure host tests (no fs / no SSH).
[env:native_ssh_sftp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_SFTP=1
    -DPC_ENABLE_MNT=1
build_src_filter =
    +<network_drivers/application/sftp/sftp.c>
    +<mmgr/frame.c>
    +<server/filesystem/filesystem.c>
    +<server/filesystem/mnt.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_sftp

; SCP (RCP) protocol wire codec (network_drivers/application/scp): parse an `scp -t/-f <path>` exec command into its
; sink/source role + target, parse + build the `C<mode> <size> <name>` control line (octal mode, decimal
; size, name; malformed lines rejected), and the ack byte constants. Pure host tests.
[env:native_scp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_SCP=1
    -DPC_ENABLE_MNT=1
build_src_filter =
    +<network_drivers/application/scp/scp.c>
    +<server/filesystem/filesystem.c>
    +<server/filesystem/mnt.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_scp

; Full HTTP/1.1 server stack through Layer 7. Base env for the suites that build on it; it runs no suite of its own.
[native_stack_http]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DBODY_BUF_SIZE=512
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_STATS=1
    -DPC_ENABLE_METRICS=1
    -DPC_ENABLE_ETAG=1
    -DPC_ENABLE_WEB_TERMINAL=1
    -DPC_HTTP_EMIT_DATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<protocore.c>
    +<network_drivers/application/file_serving/file_serving.c>
    +<network_drivers/application/http_range.c>
    +<network_drivers/presentation/http/auth/auth.c>
    +<server/response.c>
    +<server/websocket_sse.c>
    +<server/regex.c>
    +<server/middleware.c>
    +<server/webdav_handler.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<network_drivers/application/web_assets.c>
    +<services/web/web_terminal/web_terminal.c>
    +<network_drivers/application/ntp_service/ntp_service.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<network_drivers/presentation/http/route/http_route.c>
    +<network_drivers/network/network.c>
    +<network_drivers/network/dns/dns.c>
    +<server/signaling/signaling.c>
    +<server/filesystem/filesystem.c>
    +<server/filesystem/mnt.c>
    +<mmgr/protostr.c>
    +<mmgr/secure.c>
    +<mmgr/protomem.c>
    +<network_drivers/presentation/http/http.c>

; test_auth against the native_stack_http stack.
[env:native_auth]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_AUTH=1
test_filter =
    integration/http/test_auth

; test_file_serving against the native_stack_http stack.
[env:native_file_serving]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_FILE_SERVING=1
test_filter =
    integration/filesystem/test_file_serving

; test_multipart against the native_stack_http stack.
[env:native_multipart]
extends = native_stack_http
test_filter =
    integration/codec/test_multipart

; test_dispatch against the native_stack_http stack.
[env:native_dispatch]
extends = native_stack_http
test_filter =
    integration/fieldbus/test_dispatch

; test_application against the native_stack_http stack.
[env:native_application]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_FILE_SERVING=1
test_filter =
    integration/misc/test_application

; test_response_headers against the native_stack_http stack. Carries PC_ENABLE_NTP because the
; Date-header cases drive the clock through the NTP client's own entry points, and with it the UDP
; listener the portable client binds its reply port on.
[env:native_response_headers]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_NTP=1
build_src_filter =
    ${native_stack_http.build_src_filter}
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<mmgr/protomem.c>
test_filter =
    integration/application/test_response_headers

; test_form_params against the native_stack_http stack.
[env:native_form_params]
extends = native_stack_http
test_filter =
    integration/transport/test_form_params

; test_path_params against the native_stack_http stack.
[env:native_path_params]
extends = native_stack_http
test_filter =
    integration/transport/test_path_params

; test_digest_auth against the native_stack_http stack.
[env:native_digest_auth]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_AUTH=1
test_filter =
    integration/http/test_digest_auth

; test_digest_vectors against the native_stack_http stack.
[env:native_digest_vectors]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_AUTH=1
test_filter =
    unit/http/test_digest_vectors

; test_template against the native_stack_http stack.
[env:native_template]
extends = native_stack_http
test_filter =
    integration/transport/test_template

; test_middleware against the native_stack_http stack.
[env:native_middleware]
extends = native_stack_http
test_filter =
    integration/transport/test_middleware

; test_chunked against the native_stack_http stack.
[env:native_chunked]
extends = native_stack_http
test_filter =
    integration/shared_primitives/test_chunked

; test_json against the native_stack_http stack.
[env:native_json]
extends = native_stack_http
test_filter =
    unit/codec/test_json

; test_iface against the native_stack_http stack.
[env:native_iface]
extends = native_stack_http
test_filter =
    integration/transport/test_iface

; test_regex against the native_stack_http stack.
[env:native_regex]
extends = native_stack_http
test_filter =
    integration/transport/test_regex

; test_web_terminal against the native_stack_http stack.
[env:native_web_terminal]
extends = native_stack_http
test_filter =
    integration/transport/test_web_terminal

; test_defer against the native_stack_http stack.
[env:native_defer]
extends = native_stack_http
test_filter =
    integration/session/test_defer

; WebDAV request handler over a directory-capable FS mock (recursive COPY/MOVE/DELETE)
[env:native_webdav_handler]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DBODY_BUF_SIZE=512
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_WEBDAV=1
    -DPC_ENABLE_FILE_SERVING=1
    -DPC_ENABLE_WEB_TERMINAL=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<protocore.c>
    +<network_drivers/network/network.c>
    +<network_drivers/network/dns/dns.c>
    +<network_drivers/application/file_serving/file_serving.c>
    +<network_drivers/application/http_range.c>
    +<network_drivers/presentation/http/auth/auth.c>
    +<server/response.c>
    +<server/websocket_sse.c>
    +<server/regex.c>
    +<server/middleware.c>
    +<server/webdav_handler.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<network_drivers/application/web_assets.c>
    +<services/web/web_terminal/web_terminal.c>
    +<network_drivers/application/webdav/webdav.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<server/filesystem/filesystem.c>
    +<server/filesystem/mnt.c>
    +<network_drivers/presentation/http/route/http_route.c>
    +<mmgr/protomem.c>
    +<server/signaling/signaling.c>
    +<mmgr/protostr.c>
    +<mmgr/secure.c>
    +<network_drivers/presentation/http/http.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
test_filter =
    integration/http/test_webdav_handler

; Build-flag reporter (diag() / PC_ENABLE_DIAG). The gated diag() entry point
; and its DIAG_DOC frame spec are enabled by no other env, so this one keeps them
; compiled + exercised in CI. Reuses the full app src via the native_stack_http
; base.
[env:native_diag]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_DIAG=1
build_src_filter =
    ${native_stack_http.build_src_filter}
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_diag

; SNMP ASN.1 BER codec (the version-agnostic base for the SNMP agent). Pure
; codec - no lwIP/UDP - so it unit-tests on the host.
[env:native_snmp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SNMP=1
build_src_filter =
    +<services/net/snmp/snmp_ber.c>
    +<services/net/snmp/snmp_agent.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_snmp_ber
    protocols/iot/test_snmp_agent

; SNMPv3 USM layer: auth (HMAC-SHA-256), privacy (AES-128-CFB), engine discovery,
; timeliness. Pulls in the SHA-256/HMAC primitives from the SSH crypto layer.
[env:native_snmp_v3]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SNMP=1
    -DPC_ENABLE_SNMP_V3=1
    -DPC_ENABLE_SNMP_TRAP=1
build_src_filter =
    +<services/net/snmp/snmp_ber.c>
    +<services/net/snmp/snmp_agent.c>
    +<services/net/snmp/snmp_crypto.c>
    +<services/net/snmp/snmp_v3.c>
    +<services/net/snmp/snmp_notify.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/iot/test_snmp_v3

; Telnet server (RFC 854 IAC negotiation + line editing) wired through the real
; transport ring buffer; output checked via the tcp_write capture mock.
[env:native_telnet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TELNET=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<network_drivers/presentation/telnet/telnet.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    integration/session/test_telnet

; CoAP server (RFC 7252) message codec + resource dispatch. Pure core - no
; lwIP/UDP - so it unit-tests on the host (udp_transport provides host stubs).
[env:native_coap]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_COAP=1
    -DPC_ENABLE_COAP_BLOCK=1
    -DPC_COAP_BLOCK_SZX_MAX=2
    -DPC_COAP_BLOCK1_MAX=128
build_src_filter =
    +<services/iot/coap/coap.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_coap

; CoAP with resource observation (RFC 7641) enabled. Compiles + runs test_coap
; with PC_ENABLE_COAP_OBSERVE on, so the Observe-gated code (request Observe
; recording, the observer registry, pc_coap_notify) is actually built in CI instead
; of bit-rotting (it is enabled by no other env).
[env:native_coap_observe]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_COAP=1
    -DPC_ENABLE_COAP_BLOCK=1
    -DPC_COAP_BLOCK_SZX_MAX=2
    -DPC_COAP_BLOCK1_MAX=128
    -DPC_ENABLE_COAP_OBSERVE=1
build_src_filter =
    +<services/iot/coap/coap.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_coap

; WebDAV server core (RFC 4918): method classification, header parsing, XML
; escaping, and the 207 Multi-Status builder. Pure core - no FS/sockets - so it
; unit-tests on the host. The FS-backed handling is HW-tested on a real LittleFS.
[env:native_webdav]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WEBDAV=1
    -DPC_ENABLE_FILE_SERVING=1
build_src_filter =
    +<network_drivers/application/webdav/webdav.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_webdav

; Modbus TCP slave core + RTU framing (Modbus Application Protocol): the data
; model + MBAP/PDU codec + the RTU ADU codec (CRC16 + [addr][PDU][CRC]). Pure core
; - the transports are ARDUINO-gated - so it unit-tests on the host from modbus.c alone.
[env:native_modbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MODBUS=1
    -DPC_ENABLE_MODBUS_RTU=1
build_src_filter =
    +<services/fieldbus/modbus/modbus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_modbus

; CloudEvents v1.0 envelope (services/iot/cloudevents): the structured-JSON builder
; (over the JSON writer) + the binary-mode ce-* header reader. Pure host tests.
[env:native_cloudevents]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CLOUDEVENTS=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<services/iot/cloudevents/cloudevents.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/shared_primitives/test_cloudevents

; Redis RESP2/RESP3 codec (services/iot/redis_resp): the zero-heap command encoder + the
; cursor reply parser (RESP2 simple/error/integer/bulk/array/nil plus RESP3 null/boolean/double/big
; number/bulk error/verbatim/map/set/push). Pure host tests against Redis spec vectors.
[env:native_redis]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_REDIS=1
build_src_filter =
    +<services/iot/redis_resp/redis_resp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_redis_resp

; SQLite3 on-disk file-format reader (services/storage/sqlite): the 100-byte database header, the
; b-tree page header, the record varint, and record serial types, parsed by hand. Pure host tests
; against a real database built by the sqlite3 CLI (page-1 header + b-tree header + first-cell varints)
; plus varint and serial-type spec vectors.
[env:native_sqlite]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SQLITE=1
build_src_filter =
    +<services/storage/sqlite/sqlite_format.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_sqlite

; STOMP 1.2 frame codec (services/iot/stomp): the zero-heap frame builder (command +
; escaped headers + NUL body) + the non-mutating parser (command/header slices/body,
; honoring content-length) + escape/unescape. Pure host tests.
[env:native_stomp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_STOMP=1
build_src_filter =
    +<services/iot/stomp/stomp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_stomp

; MQTT-SN v1.2 wire codec (services/iot/mqtt/mqtt_sn): the zero-heap message builders
; (CONNECT/REGISTER/PUBLISH/SUBSCRIBE/PINGREQ/DISCONNECT/SEARCHGW) + the Length+MsgType
; header parser (1- and 3-octet forms) + typed payload parsers. Pure host tests.
[env:native_mqtt_sn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MQTT_SN=1
build_src_filter =
    +<services/iot/mqtt/mqtt_sn.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_mqtt_sn

; Flow-record export codec (services/net/flow_export): NetFlow v5 fixed header/record
; builders + the NetFlow v9 / IPFIX template-then-data cursor (length/count patching,
; v9 4-octet padding). Pure host tests with wire-byte assertions.
[env:native_flow_export]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FLOW_EXPORT=1
build_src_filter =
    +<services/net/flow_export/flow_export.c>
    +<mmgr/protomem.c>
test_filter =
    unit/net/test_flow_export

; Protocol Buffers wire codec (services/iot/protobuf): the zero-heap streaming writer
; (varint / ZigZag / fixed32 / fixed64 / length-delimited) + the cursor reader, host-tested
; against the spec vectors. Pure host tests.
[env:native_protobuf]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PROTOBUF=1
build_src_filter =
    +<services/iot/protobuf/protobuf.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_protobuf

; Preempting work queue (network_drivers/session/preempt_queue), v5 real-time ingest: FIFO order,
; urgent-to-front, fail-closed when full, high-water, and the hand-off from a post to the lane
; task's handler. The DMA lane is driven end to end by the host DMA driver (mmgr/dma): a fed
; transfer completes where the ISR would fire, its callback posts the bytes onto the lane, and the
; lane's task does the work. The post and the task run on the host through the keyed queue mock;
; only the interrupt itself and the preempt latency need hardware.
[env:native_preempt_queue]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PREEMPT_QUEUE=1
    -DPC_PQ_DEPTH=4
    -DPC_PQ_ITEM_SIZE=16
    -DPC_ENABLE_DMA=1
    -DPC_DMA_BUF_SIZE=8
    -DPC_DMA_CHANNELS=2
build_src_filter =
    +<network_drivers/session/preempt_queue.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/dma.c>
    +<mmgr/protomem.c>
test_filter =
    unit/session/test_preempt_queue

; DMA peripheral ingest / egress simulator (mmgr/dma), v5 hardware ingest: an
; ingress feed surfaces as RX completion events, a full buffer ping-pongs and re-arms,
; egress DMA is captured, TX is one-in-flight fail-closed, and a loopback channel
; round-trips its own TX into RX. The preempt-queue hand-off is observable on the host
; through the keyed queue mock; only the interrupt that triggers it needs hardware.
[env:native_dma]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DMA=1
    -DPC_DMA_BUF_SIZE=8
    -DPC_DMA_CHANNELS=2
build_src_filter =
    +<mmgr/dma.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/mmgr/test_dma

; Pre/post-trigger sample-window assembler (server/signaling/trace_capture), v5 high-rate
; acquisition: a continuously-running pre-trigger ring, trigger() freezing it as the window's
; pre-trigger half, feed() filling the post-trigger half and firing the sink on completion,
; one capture in flight fail-closed (dropped triggers counted), samples fed before begin() /
; after end() counted dropped, and multiple sequential windows with a correct trace_id
; sequence. Pure host tests (pc_cycles() falls back to a coarse host stand-in; only ordering,
; not magnitude, is asserted for assembly_cycles).
[env:native_trace_capture]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TRACE_CAPTURE=1
    -DPC_TC_MAX_WINDOW_SAMPLES=32
build_src_filter =
    +<server/signaling/trace_capture.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_trace_capture

; AD9238 SPI configuration-port codec (services/peripherals/ad9238): the 16-bit instruction word
; (R/W + byte-count + 13-bit address) for single-byte register writes/reads, the device-update
; transfer transaction, and the input-validation fail-closed paths (null buffer, undersized
; cap, zero/oversize byte count, out-of-range address). Pure host tests - the SPI clocking and
; the per-register bit-field meanings are the datasheet's / the application's, not this codec's.
[env:native_ad9238]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_AD9238=1
build_src_filter =
    +<services/peripherals/ad9238/ad9238.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_ad9238

; Interface forwarding plane (network_drivers/network/forward), v5 bridge / router: default-deny,
; an ALLOW rule forwards, a DENY wins, multi-destination fan-out, no reflection to the
; source, the per-rule rate cap (host test clock), send-failure counting, the interface /
; rule table limits, the ingress ACL (byte-pattern permit/deny, first-match, default
; action), policy routing (byte-pattern -> egress, precedence + fail-closed), and the opt-in
; inspection hook. Pure host tests; the DMA-driven wiring is covered by native_dma over the same host queue.
[env:native_forward]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FORWARD=1
    -DPC_PHY_MAX_IFACES=4
    -DPC_FWD_MAX_RULES=4
    -DPC_FWD_MAX_ACL=4
    -DPC_FWD_MAX_ROUTES=4
    -DPC_FWD_INSPECT=1
build_src_filter =
    +<network_drivers/network/forward/forward.c>
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_forward

; Radio / wireless gateway bridge (services/net/gateway), v5 southbound-to-northbound: an
; uplink envelopes a received frame (src address / port / rssi / seq) and publishes it,
; fail-closed on no sink / unknown port / rate cap / sink refusal; a downlink routes to the
; port transmit callback; the topic helper formats <prefix>/<port>/<addr>; port table limits.
; Pure host tests; the DMA + FORWARD-lane ingest is covered by native_dma and native_forward.
[env:native_gateway]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GATEWAY=1
    -DPC_GW_MAX_PORTS=4
build_src_filter =
    +<services/net/gateway/gateway.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_gateway

; LoRa codec + SX127x driver (services/radio/lora), v5 radio plugin: the RadioHead 4-byte
; header parse/build, and the SX127x register protocol (init / send / tx-done / set-rx /
; recv) exercised against a mock chip - a register file plus a FIFO with the auto-
; incrementing address pointer. Pure host tests; the RF link needs the module.
[env:native_lora]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LORA=1
build_src_filter =
    +<services/radio/lora/lora.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/radio/test_lora

; nRF24L01+ driver (services/radio/nrf24), v5 radio plugin: the Nordic SPI command protocol
; (STATUS shifted out first, W/R_REGISTER, W_TX/R_RX_PAYLOAD, write-1-to-clear) exercised
; against a mock chip - init / send (padded to the static width) / tx-done / set-rx / recv
; (pipe-addressed). Pure host tests; the RF link needs the module.
[env:native_nrf24]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NRF24=1
    -DPC_NRF24_PAYLOAD=8
build_src_filter =
    +<services/radio/nrf24/nrf24.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/radio/test_nrf24

; EnOcean ESP3 serial codec (services/radio/enocean), v5 radio plugin: the CRC-8 (poly 0x07)
; against known answers, a build -> parse round trip, malformed framing (bad sync / header
; CRC / data CRC), incomplete telegrams, over-length rejection, and resynchronisation.
; Pure host tests.
[env:native_enocean]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ENOCEAN=1
    -DPC_ENOCEAN_MAX_DATA=16
build_src_filter =
    +<services/radio/enocean/enocean.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_enocean

; PN532 NFC frame codec (services/peripherals/pn532), v5 radio plugin: the normal-information-frame
; build/parse against the documented GetFirmwareVersion command + response frames (LEN/LCS +
; DCS checksums), a round trip, malformed framing (bad preamble / start / LCS / DCS), the ACK
; frame, and over-length rejection. Pure host tests.
[env:native_pn532]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PN532=1
    -DPC_PN532_MAX_DATA=8
build_src_filter =
    +<services/peripherals/pn532/pn532.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_pn532

; Sigfox modem AT-command codec (services/radio/sigfox), v5 radio plugin: the AT$SF uplink
; command (uppercase hex encoding of the payload), its bounds (12-byte cap, output cap), and
; the OK / ERROR / PENDING response classification. Pure host tests.
[env:native_sigfox]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SIGFOX=1
build_src_filter =
    +<services/radio/sigfox/sigfox.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_sigfox

; Z-Wave Serial API frame codec (services/radio/zwave), v5 radio plugin: the data-frame
; build/parse against the documented GetVersion request (01 03 00 15 E9), the XOR checksum, a
; round trip, malformed framing (bad SOF / checksum / length), incomplete frames, and the ACK
; / NAK / CAN control bytes. Pure host tests.
[env:native_zwave]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ZWAVE=1
    -DPC_ZWAVE_MAX_DATA=16
build_src_filter =
    +<services/radio/zwave/zwave.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_zwave

; Zigbee EZSP / ASH framing codec (services/radio/zigbee), v5 radio plugin: the CRC-16/CCITT
; and the encoded RST frame against their documented values (C0 38 BC 7E), an encode -> decode
; round trip, the byte-stuffing of reserved bytes, and malformed framing (bad CRC, dangling
; escape, buffer-too-small, no flag). Pure host tests.
[env:native_zigbee]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ZIGBEE=1
    -DPC_ZIGBEE_MAX_DATA=32
build_src_filter =
    +<services/radio/zigbee/zigbee.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_zigbee

; Thread spinel / HDLC-lite codec (services/radio/thread), v5 radio plugin: the FCS
; (CRC-16/X-25) against its catalog check value (0x906E), an encode -> decode round trip, the
; byte-stuffing of reserved bytes, malformed framing (bad FCS, dangling escape,
; buffer-too-small, no flag), the spinel command layer (packed-uint KATs, command build/parse
; through HDLC), and the property registry + typed value semantics (reader/writer round trip
; across every spinel datatype, little-endian wire layout, PROTOCOL_VERSION / CAPS decode,
; UTF8 + length-prefixed data, bounds/overflow latching, prop + status name lookup). Pure host tests.
[env:native_thread]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_THREAD=1
    -DPC_THREAD_MAX_DATA=64
build_src_filter =
    +<services/radio/thread/thread.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_thread

; UDP transport multicast receive (network_drivers/transport/udp.c): joining an IPv4
; multicast group by dotted-quad, rejecting a non-multicast or malformed group, delivering a group
; datagram to the registered handler, and leaving the group (releasing the listener slot). Drives the
; host UDP mock's inject seam.
[env:native_udp_transport]
extends = native_base
build_src_filter =
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_udp_transport

; The TCP path a stack drives, run on the host against the pcb driver in test/mocks. This is the only env that compiles the marshaled
; arm of tcp_conn.c - every send, output, close, abort and recved the target routes through
; pc_tcp_marshal - and the only one that drives a segment through the callbacks listener_accept_cb
; installed on the pcb rather than naming lowlevel_recv_cb directly.
[env:native_tcp]
extends = native_base
build_src_filter =
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/diffserv.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_tcp

; PC_WORKER_COUNT=2 over the stack path, which nothing else compiles: native_tcp is
; single-worker and takes the other arm of listener_enqueue, so the multi-worker arm - the per-worker queue routing and the wake that
; follows it - had no witness in the matrix at all.
[env:native_workers_stack]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_WORKER_COUNT=2
build_src_filter =
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/diffserv.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_workers

; The UDP path a stack drives, run on the host against the pcb driver in test/mocks. Covers udp_listener + udp_client against the
; host pcb driver: the receive ring, both send paths reaching the wire in the call that makes them,
; NetAddr carrying v4 and v6 both ways, the pbuf pool refusing, and the datagrams the core handed to the stack read back as a DLT_RAW libpcap
; capture with synthesized IP and UDP headers (test/mocks/pc_net_pcap.h).
[env:native_udp]
extends = native_base
build_src_filter =
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_udp

; WAMP messaging codec (services/iot/wamp): the JSON-array message builders (HELLO /
; SUBSCRIBE / PUBLISH / CALL / REGISTER / YIELD / GOODBYE over JsonWriter) + the positional
; array parser (type / ids / URIs, nesting-aware). Pure host tests.
[env:native_wamp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAMP=1
build_src_filter =
    +<services/iot/wamp/wamp.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_wamp

; SunSpec Modbus model codec (services/energy/sunspec): the map writer (marker / model
; headers / points / end model) + the model-chain walker + typed point readers
; (u16 / i16 / u32 / i32 / string). Pure host tests.
[env:native_sunspec]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SUNSPEC=1
build_src_filter =
    +<services/energy/sunspec/sunspec.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_sunspec

; IEEE C37.118.2 synchrophasor frame codec (services/energy/c37118): CRC-CCITT, the frame
; builder + Command frame, and the CRC-validating parser (type / ids / timestamp / payload).
; Pure host tests.
[env:native_c37118]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_C37118=1
build_src_filter =
    +<services/energy/c37118/c37118.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_c37118

; DNP3 (IEEE 1815) data-link frame codec (services/energy/dnp3): CRC-16/DNP, the frame
; builder (0x0564 header + CRC'd 16-octet data blocks) and the CRC-validating, de-blocking
; parser. Pure host tests.
[env:native_dnp3]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DNP3=1
build_src_filter =
    +<services/energy/dnp3/dnp3.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_dnp3

; gRPC-Web message framing codec (services/iot/grpcweb): the 5-octet length-prefixed
; message frame builder + the 0x80 trailers frame (grpc-status / grpc-message) + the frame
; parser. Pure host tests.
[env:native_grpcweb]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GRPC_WEB=1
build_src_filter =
    +<services/iot/grpcweb/grpcweb.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_grpcweb

; OMA LwM2M TLV codec (services/iot/lwm2m): the writer (raw + int / bool / string /
; float value helpers, 8-/16-bit ids, inline / 8-/16-/24-bit lengths) + the cursor reader
; + integer value decoding. Pure host tests.
[env:native_lwm2m_tlv]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LWM2M=1
build_src_filter =
    +<services/iot/lwm2m/lwm2m_tlv.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_lwm2m_tlv

; Omron FINS frame codec (services/fieldbus/fins): the command builder + Memory Area Read
; convenience + the command / response parsers (10-octet header, MRC/SRC, MRES/SRES end
; code). Pure host tests.
[env:native_fins]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FINS=1
build_src_filter =
    +<services/fieldbus/fins/fins.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_fins

; Omron Host Link (C-mode) frame codec (services/fieldbus/hostlink): the FCS (XOR), the ASCII
; command builder (@UU + header + text + FCS + *CR), and the FCS-validating parser +
; end-code reader. Pure host tests.
[env:native_hostlink]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HOSTLINK=1
build_src_filter =
    +<services/fieldbus/hostlink/hostlink.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_hostlink

; SCPI / IEEE 488.2 instrument-control codec (services/instrumentation/scpi): the command builder
; (:-hierarchy header + params + terminator), the response parsers (numeric NR1/NR2/NR3,
; boolean, quoted string, definite/indefinite arbitrary block), the STB/ESR/ESE/SRE +
; error-queue status model, and the SCPI short/long-form header matcher. Pure host tests.
[env:native_scpi]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SCPI=1
    -DUNITY_INCLUDE_DOUBLE
build_src_filter =
    +<services/instrumentation/scpi/scpi.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/instrumentation/test_scpi

; HiSLIP (High-Speed LAN Instrument Protocol, IVI-6.1) message codec (services/instrumentation/hislip):
; the fixed 16-byte header build/parse (HS prologue + type + control + 32-bit param + 64-bit
; payload length, big-endian), the Initialize / AsyncInitialize handshake helpers, and the
; Data / DataEND SCPI-payload framing with message ids. Pure host tests.
[env:native_hislip]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HISLIP=1
build_src_filter =
    +<services/instrumentation/hislip/hislip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/instrumentation/test_hislip

; VXI-11 (TCP/IP Instrument Protocol) codec over ONC RPC / XDR (services/instrumentation/vxi11): the XDR
; write/read helpers (4-byte-aligned, big-endian, length-prefixed opaque/string), the ONC-RPC
; record-marking + CALL/REPLY framing with AUTH_NONE, the portmapper GETPORT call, and the
; DEVICE_CORE procedures (create_link / device_write / device_read / device_readstb /
; destroy_link). Pure host tests with byte-exact vectors.
[env:native_vxi11]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_VXI11=1
build_src_filter =
    +<services/instrumentation/vxi11/vxi11.c>
    +<mmgr/protomem.c>
test_filter =
    unit/instrumentation/test_vxi11

; GPIB-over-LAN (Prologix-style) controller command codec (services/instrumentation/gpib): the ++ command
; builders (addr / mode / read / eoi / eos / spoll / clr / trg / ver), the data-line escaping
; (leading ESC before CR/LF/ESC/+ in instrument data), the command-vs-data classifier, and the
; response parsers (address, serial-poll status byte, version). Pure host tests.
[env:native_gpib]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GPIB=1
build_src_filter =
    +<services/instrumentation/gpib/gpib.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/instrumentation/test_gpib

; Haas Machine Data Collection (MDC) Q-command codec (services/machine_tool/haas_mdc): the ?Q query
; builders (Q100 serial, Q101 software version, Q102 model, Q104 mode, Q300 power-on time, Q500
; program + run status + parts counter, Q600 <var> macro/system-variable read) and the parser for the
; >-wrapped comma-delimited responses plus the unprompted DPRNT(...) lines. Pure host tests.
[env:native_haas_mdc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HAAS_MDC=1
build_src_filter =
    +<services/machine_tool/haas_mdc/haas_mdc.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_haas_mdc

; PackML / OMAC packaging-machine state model (services/machine_tool/packml), ISA-TR88.00.02: the pure
; 17-state transition engine (command / state-complete / execute-complete + command validity) and the
; owned PackTags service (state advance, production counters, unit-mode rules, machine speed, and the
; state/reset timers). Pure host tests.
[env:native_packml]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PACKML=1
build_src_filter =
    +<services/machine_tool/packml/packml.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_packml

; Heidenhain LSV/2 telegram codec (services/machine_tool/lsv2): the framer (4-byte big-endian payload-length
; prefix + 4-char mnemonic + payload), the typed request builders (login A_LG / logout A_LO,
; null-terminated-filename file commands, R_RI run-info with a 2-byte big-endian selector), and the
; response readers (T_OK, the T_ER/T_BD 2-byte error-class + error-code, data replies, stream
; re-framing). Byte-exact vectors cross-checked against pyLSV2. Pure host tests.
[env:native_lsv2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LSV2=1
build_src_filter =
    +<services/machine_tool/lsv2/lsv2.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_lsv2

; IKEv2 (RFC 7296) message + payload codec (services/security/ikev2): the 28-octet IKE header, the generic
; payload chain walker, the SA -> proposal -> transform tree (incl. the key-length attribute), and the
; KE / Nonce / Notify / Delete / IDi-IDr / AUTH / CERT / TSi-TSr / SK-envelope build + parse. Byte-exact
; golden vectors generated by scapy's IKEv2 codec (key-length attribute verified against scapy's
; decoder). Pure host tests.
[env:native_ikev2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IKEV2=1
build_src_filter =
    +<services/security/ikev2/ikev2.c>
    +<services/security/ikev2/ikev2_natt.c>
    +<crypto/hash/sha1.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<crypto/hash/sha512.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<../core_setup/hal/host/host_nvs.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_ikev2
    unit/security/test_ikev2_natt
extra_scripts =
    pre:test/ensure_test_keys.py

; ESP (RFC 4303) packet transform with AES-256-GCM (RFC 4106) - the IPsec datapath crypto core
; (services/system/esp): encapsulate a payload into SPI|Seq|IV|AES-GCM(payload|pad|padlen|nexthdr)|ICV and
; verify + decapsulate it, with the SPI|Seq AAD + salt||IV nonce. Gated on the IKEv2 feature. Pure host
; tests cross-checked against a Python AES-256-GCM reference.
[env:native_esp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IKEV2=1
build_src_filter =
    +<services/system/esp/esp.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<mmgr/arena.c>
    +<mmgr/plaintext.c>
    +<mmgr/secure.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/system/test_esp

; IPsec Security Policy Database + Security Association Database (RFC 4301, services/system/esp/ipsec_db):
; ordered first-match-wins SPD policy lookup over source/destination/protocol/port selector ranges,
; an SPD selector built from an IKEv2-negotiated TSi/TSr pair, and SPI-keyed SAD add/find/remove with
; per-SA outbound sequence allocation + inbound anti-replay window. Pure host-testable datapath logic.
[env:native_ipsec_db]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IKEV2=1
build_src_filter =
    +<services/system/esp/ipsec_db.c>
    +<services/system/esp/esp.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<mmgr/arena.c>
    +<mmgr/plaintext.c>
    +<mmgr/secure.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/system/test_ipsec_db

; SenML (RFC 8428) pack builder (services/iot/senml): the SenML-JSON encoder (over the
; JSON writer) + the SenML-CBOR encoder (over the CBOR writer, integer labels), integral
; numbers emitted as integers. Pure host tests.
[env:native_senml]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SENML=1
build_src_filter =
    +<services/iot/senml/senml.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<network_drivers/presentation/codec/cbor/cbor.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_senml

; Allen-Bradley DF1 full-duplex frame codec (services/fieldbus/df1): BCC + CRC-16/ARC, the
; frame builder with DLE byte-stuffing, and the validating, un-stuffing parser. Vectors from
; AB pub. 1770-6.5.16. Pure host tests.
[env:native_df1]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DF1=1
build_src_filter =
    +<services/fieldbus/df1/df1.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_df1

; Siemens SIMATIC serial (services/fieldbus/simatic): 3964R block framing (DLE-double + XOR BCC)
; + the 3964R link state machine (STX/DLE handshake, NAK/QVZ retry, ZVZ timeout, priority
; arbitration) + RK512 SEND/FETCH/reaction telegram build/parse (big-endian words). Pure host tests.
[env:native_simatic]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SIMATIC=1
build_src_filter =
    +<services/fieldbus/simatic/simatic.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_simatic

; TPKT (RFC 1006) + COTP X.224 class-0 frame codec (services/fieldbus/cotp): the TPKT
; envelope, the COTP Data TPDU + Connection Request builders, and the COTP parser. The
; ISO-on-TCP foundation under S7comm / IEC 61850 MMS. Pure host tests.
[env:native_cotp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_COTP=1
build_src_filter =
    +<services/fieldbus/cotp/cotp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_cotp

; Siemens S7comm PDU codec (services/fieldbus/s7comm): the Setup Communication + Read Var
; request builders, the header parser, and the response data-item reader (length-in-bits +
; even padding). Constants per the Wireshark dissector. Pure host tests.
[env:native_s7comm]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_S7COMM=1
build_src_filter =
    +<services/fieldbus/s7comm/s7comm.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_s7comm

; Mitsubishi MELSEC MC binary 3E codec (services/fieldbus/melsec): the batch-read request
; builder (little-endian, subheader 0x5000, command 0x0401, device code + 24-bit head device)
; + the 0xD000 response parser. Pure host tests.
[env:native_melsec]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MELSEC=1
build_src_filter =
    +<services/fieldbus/melsec/melsec.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_melsec

; Beckhoff ADS / AMS codec (services/fieldbus/ads): the AMS/TCP + AMS-header request builders
; (little-endian, target-before-source addressing, cmd id + state flags + cbData + invoke id) for
; Read/Write/ReadWrite/ReadState/ReadDeviceInfo/WriteControl/Add+DeleteNotification + the response
; parsers incl. the DeviceNotification stamp/sample walk. TwinCAT PC-based control. Pure host tests.
[env:native_ads]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ADS=1
build_src_filter =
    +<services/fieldbus/ads/ads.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_ads

; FANUC FOCAS Ethernet codec (services/machine_tool/focas): the big-endian frame envelope
; (magic/version/type/length) + open/close handshake, the generic command request (6-octet function
; selector + five i32 args), and the response parsers (echoed selector + FOCAS return code + data),
; incl. ODBSYS SysInfo, the alarm bitmask, and the 8-octet data/base^exp value encoding. FANUC CNC
; data over TCP 8193, reverse-engineered vs diohpix/pyfanuc. Pure host tests.
[env:native_focas]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FOCAS=1
build_src_filter =
    +<services/machine_tool/focas/focas.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_focas

; FANUC Stream Motion / option J519 UDP codec (services/machine_tool/fanuc_j519): the robot
; counterpart to FOCAS. The 8-octet little-endian header, all six packets (Start/Motion/Stop/Request
; PC->robot, Robot Status/Ack robot->PC), the 9-axis IEEE-754 binary32 pose/joint/current blocks, and
; the 20-entry threshold tables - byte-checked at every field offset against the public Wireshark
; dissector, plus symmetric build->parse round trips. Includes the length guards that separate the two
; packets sharing a type code per direction (Start 8 vs Status 132, Request 16 vs Ack 184). No heap,
; no stdlib. Pure host tests.
[env:native_fanuc_j519]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FANUC_J519=1
build_src_filter =
    +<services/machine_tool/fanuc_j519/fanuc_j519.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_fanuc_j519

; Post-quantum hybrid KEX primitives (network_drivers/presentation/pqc): the
; Keccak/SHA-3/SHAKE sponge (FIPS 202) and ML-KEM-768 Encaps (FIPS 203) - the responder half of the
; mlkem768x25519-sha256 (SSH) and X25519MLKEM768 (TLS 1.3) hybrid key exchanges - plus the full
; Streamlined NTRU Prime sntrup761 KEM (keypair/enc/dec) for sntrup761x25519-sha512@openssh.com.
; SHA-3/SHAKE digests pinned from NIST FIPS 202 vectors; ML-KEM Encaps byte-checked against a kyber-py
; vector; sntrup761 decapsulation checked against an OpenSSH-reference KAT plus a keypair/enc/dec
; round-trip. Pure host tests.
[env:native_pqc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PQC_KEX=1
build_src_filter =
    +<crypto/hash/sha3.c>
    +<crypto/pqc/mlkem.c>
    +<crypto/pqc/sntrup761.c>
    +<crypto/hash/sha512.c>
    +<mmgr/protomem.c>
test_filter =
    unit/pqc/test_pqc_sha3
    unit/pqc/test_pqc_mlkem
    unit/pqc/test_pqc_sntrup761

; Interface bridge pure core (services/net/iface_bridge): the user-defined address:port -> bus
; rule table (register / find / dedup / capacity, keyed by port+proto with the full pc_ip bind address
; preserved) and the write-then-read transaction frame codec (big-endian write_len/read_len header,
; partial-frame handling, build/parse round-trip). Bus I/O + listener are ESP32-only. Pure host tests.
[env:native_iface_bridge]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_IFACE_BRIDGE=1
build_src_filter =
    +<services/net/iface_bridge/iface_bridge.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_iface_bridge

; RTCM 3.x framing + station-reference codec (services/timing_position/gnss/rtcm3), the pure core of the
; GNSS RTK base / NTRIP caster: the transport frame (0xD3 preamble, 10-bit length, CRC-24Q), MSB-first
; bit I/O, and 1005/1006 Stationary Antenna Reference Point encode/decode. Frames verified byte-for-byte
; against pyrtcm. Pure host tests.
[env:native_rtcm3]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTRIP_CASTER=1
build_src_filter =
    +<services/timing_position/gnss/rtcm3.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_rtcm3

; GNSS survey-in core (services/timing_position/gnss/gnss_survey): the exact WGS84 geodetic<->ECEF
; transform (matched against pyproj EPSG:4979->EPSG:4978), the shifted-origin position averager with a
; 3-D accuracy estimate, and the GGA->geodetic fold. Pure host tests.
[env:native_gnss_survey]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTRIP_CASTER=1
    -DPC_ENABLE_NMEA0183=1
    -DUNITY_INCLUDE_DOUBLE
build_src_filter =
    +<services/timing_position/gnss/gnss_survey.c>
    +<services/timing_position/nmea0183/nmea0183.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_gnss_survey

; NTRIP caster protocol codec (services/timing_position/gnss/ntrip_caster): rover request parsing
; (mountpoint, NTRIP 1.0/2.0 version, HTTP Basic auth), the stream-accept / error responses, and the RTCM
; source table (STR records + ENDSOURCETABLE with a self-consistent Content-Length). Pure host tests.
[env:native_ntrip_caster]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTRIP_CASTER=1
build_src_filter =
    +<services/timing_position/gnss/ntrip_caster.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_ntrip_caster

; BACnet/IP BVLC + NPDU codec (services/fieldbus/bacnet): the BVLC envelope (type 0x81,
; function, length) + the NPDU header (version + NPCI control + optional DNET/DADR + hop
; count) builders and parsers, slicing the APDU. ASHRAE 135. Pure host tests.
[env:native_bacnet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_BACNET=1
build_src_filter =
    +<services/fieldbus/bacnet/bacnet.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_bacnet

; EtherNet/IP encapsulation codec (services/fieldbus/enip): the 24-octet header, RegisterSession
; + SendRRData builders (Common Packet Format), and the SendRRData reply extractor. Constants
; per the Wireshark ENIP dissector. Pure host tests.
[env:native_enip]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ENIP=1
build_src_filter =
    +<services/fieldbus/enip/enip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_enip

; AMQP 0-9-1 frame codec (services/iot/amqp): the protocol header, the frame + method
; builders, the heartbeat, and the frame/method parsers (type/channel/size/payload/0xCE).
; Pure host tests.
[env:native_amqp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_AMQP=1
build_src_filter =
    +<services/iot/amqp/amqp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_amqp

; CIP message codec (services/fieldbus/cip): the EPATH logical-segment builder, the request
; builders (Get_Attribute_Single), and the response parser (service / status / data). Rides
; an EtherNet/IP Unconnected Data item. Pure host tests.
[env:native_cip]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CIP=1
build_src_filter =
    +<services/fieldbus/cip/cip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_cip

; NATS client protocol codec (services/iot/nats): the CONNECT / PUB / SUB / UNSUB / PING /
; PONG builders + the inbound MSG / INFO / PING / +OK / -ERR parser (subject/sid/reply/payload).
; Pure host tests.
[env:native_nats]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NATS=1
build_src_filter =
    +<services/iot/nats/nats.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_nats

; HAProxy PROXY protocol codec (services/net/proxy_protocol): the v1 (text) + v2 (binary)
; TCP/IPv4 header builders and the unified parser (recover the real client IP behind a load
; balancer). Pure host tests.
[env:native_proxy_protocol]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PROXY_PROTOCOL=1
build_src_filter =
    +<services/net/proxy_protocol/proxy_protocol.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/net/test_proxy_protocol

; Sparkplug B codec (services/iot/sparkplug): the topic builder + the Metric / Payload
; protobuf serializers (over the protobuf codec). Field numbers per the Eclipse Tahu proto.
; Pure host tests.
[env:native_sparkplug]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SPARKPLUG=1
build_src_filter =
    +<services/iot/sparkplug/sparkplug.c>
    +<services/iot/protobuf/protobuf.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_sparkplug

; Modbus master codec + scanner (services/fieldbus/modbus/modbus_master): build read
; requests, parse responses; host-tested as a round-trip against the slave codec.
[env:native_modbus_master]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MODBUS=1
    -DPC_ENABLE_MODBUS_MASTER=1
build_src_filter =
    +<services/fieldbus/modbus/modbus.c>
    +<services/fieldbus/modbus/modbus_master.c>
    +<mmgr/protomem.c>
test_filter =
    integration/fieldbus/test_modbus_master

; OTA rollback decision (server/update/ota_rollback): pure decision matrix host-tested;
; the esp_ota commit/rollback are ESP32-only.
[env:native_ota_rollback]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OTA_ROLLBACK=1
build_src_filter =
    +<server/update/ota_rollback.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_ota_rollback

; TOTP two-factor (services/security/totp): HMAC-SHA1 HOTP/TOTP + base32, host-tested
; against the RFC 6238 vectors (builds on the software SHA-1).
[env:native_totp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TOTP=1
build_src_filter =
    +<services/security/totp/totp.c>
    +<crypto/hash/sha1.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_totp

; Webhook / IFTTT builders (services/net/webhook): IFTTT URL + value1/2/3 JSON payload,
; host-tested. Firing is via the http_client (ESP32).
[env:native_webhook]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WEBHOOK=1
build_src_filter =
    +<services/net/webhook/webhook.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/net/test_webhook

; WiFi radio power controls (network_drivers/physical/radio_power): modem-sleep mode names
; host-tested; the apply/readback are ESP32-only (esp_wifi).
[env:native_radio_power]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RADIO_POWER=1
build_src_filter =
    +<network_drivers/physical/radio_power.c>
    +<network_drivers/physical/physical.c>
    +<mmgr/protomem.c>
test_filter =
    unit/physical/test_radio_power

; The portable DNS resolver (network_drivers/network/dns/dns_resolver, RFC 1035) on the build
; where PC_HAS_VENDOR_DNS_RESOLVER is 0: the A-record question it writes, the answer it reads back -
; walking past a CNAME and following the compression pointers a real response uses - and the replies it
; refuses on a wrong id, a set RCODE, a question rather than an answer, or no A record at all. Plus the
; dotted-quad fast path, the nameserver setter, and the classifier that rejects a loopback or multicast
; answer as a rebinding indicator.
[env:native_dns_resolver]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DNS_RESOLVER=1
build_src_filter =
    +<network_drivers/network/dns/dns_resolver.c>
    +<network_drivers/network/dns/dns_wire.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/network/test_dns_resolver

; Tamper-evident hash-chained audit log (services/security/audit_log). Pure - the chain is
; identical on host and ESP32 - so it unit-tests on the host; SHA-256 comes from
; crypto/sha256 (software path on native), the timestamp from the pluggable clock.
[env:native_audit_log]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_AUDIT_LOG=1
build_src_filter =
    +<services/security/audit_log/audit_log.c>
    +<crypto/hash/sha256.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
test_filter =
    unit/security/test_audit_log

; OIDC RS256 ID-token verifier (services/security/oidc). Pure relying-party verification -
; the signature check is real RSA modexp (ssh_rsa + ssh_bignum), so it host-tests
; against openssl-signed RS256 vectors with the matching JWKS public key.
[env:native_oidc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OIDC=1
build_src_filter =
    +<services/security/oidc/oidc.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<crypto/hash/sha256.c>
    +<crypto/hash/sha512.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_oidc
extra_scripts =
    pre:test/ensure_test_keys.py

; GraphQL query subset (services/iot/graphql) - pure parser + executor, host-tested
; with a demo resolver. No heap; bounds are compile-time.
[env:native_graphql]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GRAPHQL=1
build_src_filter =
    +<services/iot/graphql/graphql.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_graphql

; ESP-NOW peer messaging (services/radio/espnow) - the envelope codec + peer registry
; are host-tested here; the esp_now radio binding is ESP32-only. No heap.
[env:native_espnow]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ESPNOW=1
build_src_filter =
    +<services/radio/espnow/espnow.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_espnow

; OAuth2 token-endpoint client (services/security/oauth2) - the form-body builder + JSON
; token-response parser are host-tested (the parser reuses the JSON reader); the
; HTTP exchange is ESP32-only. No heap, no stdlib.
[env:native_oauth2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OAUTH2=1
build_src_filter =
    +<services/security/oauth2/oauth2.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_oauth2

; OPC UA Binary increment 1 (services/fieldbus/opcua) - the type codec, UACP framing, and
; Hello/Acknowledge handshake are host-tested here; the TCP server (opcua_rx) is
; ESP32-only. No heap, no stdlib.
[env:native_opcua]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPCUA=1
build_src_filter =
    +<services/fieldbus/opcua/opcua.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_opcua

[env:native_opcua_client]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_OPCUA_CLIENT=1
build_src_filter =
    +<services/fieldbus/opcua/opcua.c>
    +<services/fieldbus/opcua_client/opcua_client.c>
    +<mmgr/protomem.c>
test_filter =
    integration/fieldbus/test_opcua_client

; umati / OPC UA for Machine Tools (OPC 40501-1) MachineTool model (services/machine_tool/umati) - the
; Browse hierarchy + the Read resolver over a bound UmatiMachineTool are host-tested here.
; Requires the OPC UA codec. No heap, no stdlib.
[env:native_umati]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_UMATI=1
build_src_filter =
    +<services/fieldbus/opcua/opcua.c>
    +<services/machine_tool/umati/umati.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_umati

; OPC UA for Robotics (OPC 40010-1) MotionDeviceSystem model (services/machine_tool/robotics) - the Browse
; hierarchy + the Read resolver over a bound RoboticsMotionDeviceSystem, including the parametric
; Axes, are host-tested here. Requires the OPC UA codec. No heap, no stdlib.
[env:native_robotics]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_ROBOTICS=1
build_src_filter =
    +<services/fieldbus/opcua/opcua.c>
    +<services/machine_tool/robotics/robotics.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_robotics

; EUROMAP 77 (OPC 40077) IMM_MES_Interface model (services/machine_tool/euromap77) - OPC UA for injection
; molding machines. The Browse hierarchy + the Read resolver over a bound EmImm, including the
; UInt64 production counters, are host-tested here. Requires the OPC UA codec. No heap, no stdlib.
[env:native_euromap77]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_EUROMAP77=1
build_src_filter =
    +<services/fieldbus/opcua/opcua.c>
    +<services/machine_tool/euromap77/euromap77.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_euromap77

; Adversarial / pentest harness - run SEPARATELY (`pio test -e native_pentest`),
; NOT part of run_tests.sh. Fuzzes the untrusted-input parsers (HTTP, multipart,
; CoAP, SNMP+BER, Modbus, CBOR, MessagePack, JSON, base64/base32, the SQLite
; on-disk file-format reader, the Redis RESP reply decoder, the OPC UA Binary
; message parsers, the SMB2 client response parsers + the SPNEGO DER walker +
; NTLMSSP CHALLENGE, the DNC block decoder, the FTP client reply parsers, and the
; industrial client response parsers FINS / MELSEC / CIP / EtherNet-IP / DF1, the
; BACnet/IP + ISO-transport TPKT/COTP frame parsers, the IEEE C37.118
; synchrophasor + FINS command parsers, the JWT HS256 token verifier, the
; DirectNET / CC-Link / AMQP / MMS frame parsers, the DDS/RTPS UDP submessage
; walker, the WebDAV (RFC 4918) PROPPATCH body walker + Destination-header
; percent-decoder, and the HPACK HTTP/2 + QPACK HTTP/3 header-block decoders) with malformed/oversized/partial/random/mutated input and asserts bounded
; buffers + fail-closed + termination (no crash, no over-read, no hang). Pairs with
; the live driver penetration_testing/pc_pentest.py. See docs/PENTEST.md.
[env:native_pentest]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MODBUS=1
    -DPC_ENABLE_MODBUS_MASTER=1
    -DPC_ENABLE_TOTP=1
    -DPC_ENABLE_MULTIPART=1
    -DPC_ENABLE_CBOR=1
    -DPC_ENABLE_MSGPACK=1
    -DPC_ENABLE_COAP=1
    -DPC_ENABLE_COAP_BLOCK=1
    -DPC_COAP_BLOCK_SZX_MAX=2
    -DPC_COAP_BLOCK1_MAX=128
    -DPC_ENABLE_SNMP=1
    -DPC_ENABLE_SQLITE=1
    -DPC_ENABLE_REDIS=1
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_GRAPHQL=1
    -DPC_ENABLE_DNS_SERVER=1
    -DPC_ENABLE_DNP3=1
    -DPC_ENABLE_STOMP=1
    -DPC_ENABLE_SMB=1
    -DPC_ENABLE_DNC=1
    -DPC_ENABLE_FTP=1
    -DPC_ENABLE_FINS=1
    -DPC_ENABLE_MELSEC=1
    -DPC_ENABLE_CIP=1
    -DPC_ENABLE_ENIP=1
    -DPC_ENABLE_DF1=1
    -DPC_ENABLE_BACNET=1
    -DPC_ENABLE_COTP=1
    -DPC_ENABLE_C37118=1
    -DPC_ENABLE_JWT=1
    -DPC_ENABLE_DIRECTNET=1
    -DPC_ENABLE_CCLINK=1
    -DPC_ENABLE_AMQP=1
    -DPC_ENABLE_MMS=1
    -DPC_ENABLE_DDS=1
    -DPC_ENABLE_WEBDAV=1
    -DPC_ENABLE_HTTP2=1
    -DPC_ENABLE_HTTP3=1
    -DPC_ENABLE_FILE_SERVING=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http2/hpack.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<network_drivers/presentation/codec/json/json.c>
    +<network_drivers/presentation/codec/cbor/cbor.c>
    +<network_drivers/presentation/codec/msgpack/msgpack.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<services/fieldbus/modbus/modbus.c>
    +<services/fieldbus/modbus/modbus_master.c>
    +<services/security/totp/totp.c>
    +<services/iot/coap/coap.c>
    +<services/net/snmp/snmp_ber.c>
    +<services/net/snmp/snmp_agent.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<crypto/hash/sha1.c>
    +<services/storage/sqlite/sqlite_format.c>
    +<services/iot/redis_resp/redis_resp.c>
    +<services/fieldbus/opcua/opcua.c>
    +<services/iot/graphql/graphql.c>
    +<network_drivers/network/dns/dns_server.c>
    +<network_drivers/network/dns/dns_wire.c>
    +<services/energy/dnp3/dnp3.c>
    +<services/iot/stomp/stomp.c>
    +<network_drivers/application/smb/smb2.c>
    +<network_drivers/application/smb/spnego.c>
    +<network_drivers/application/smb/ntlmssp.c>
    +<network_drivers/application/smb/ntlm.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/aead/aesccm.c>
    +<crypto/hash/md.c>
    +<services/machine_tool/dnc/dnc.c>
    +<services/file_transfer/ftp/ftp.c>
    +<services/fieldbus/fins/fins.c>
    +<services/fieldbus/melsec/melsec.c>
    +<services/fieldbus/cip/cip.c>
    +<services/fieldbus/enip/enip.c>
    +<services/fieldbus/df1/df1.c>
    +<services/fieldbus/bacnet/bacnet.c>
    +<services/fieldbus/cotp/cotp.c>
    +<services/energy/c37118/c37118.c>
    +<services/security/jwt/jwt.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/kdf/kdf.c>
    +<crypto/mac/aes_cmac.c>
    +<services/fieldbus/directnet/directnet.c>
    +<services/fieldbus/cclink/cclink.c>
    +<services/iot/amqp/amqp.c>
    +<services/energy/mms/mms.c>
    +<services/iot/dds/dds.c>
    +<network_drivers/application/webdav/webdav.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_pentest

; HTTP/1.1 keep-alive (persistent connections): full server built with
; PC_ENABLE_KEEPALIVE=1; a small per-connection request cap makes the
; fairness-bound test fast. Drives requests through the real transport ring +
; tcp_write capture mock.
[env:native_keepalive]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_KEEPALIVE=1
    -DPC_KEEPALIVE_MAX_REQUESTS=3
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<protocore.c>
    +<network_drivers/network/network.c>
    +<network_drivers/network/dns/dns.c>
    +<network_drivers/application/file_serving/file_serving.c>
    +<network_drivers/application/http_range.c>
    +<network_drivers/presentation/http/auth/auth.c>
    +<server/response.c>
    +<server/websocket_sse.c>
    +<server/regex.c>
    +<server/middleware.c>
    +<server/webdav_handler.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<network_drivers/presentation/http/route/http_route.c>
    +<mmgr/protomem.c>
    +<server/signaling/signaling.c>
    +<mmgr/protostr.c>
    +<mmgr/secure.c>
    +<network_drivers/presentation/http/http.c>
    +<server/filesystem/mnt.c>
test_filter =
    integration/server/test_keepalive

; HTTP Range requests / 206 Partial Content (RFC 7233): full server built with
; PC_ENABLE_RANGE=1, serving a real littlefs volume through the mount seam and
; reading the responses back off the tcp_write capture.
[env:native_range]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_RANGE=1
    -DPC_ENABLE_FILE_SERVING=1
    -DPC_ENABLE_KEEPALIVE=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<protocore.c>
    +<network_drivers/network/network.c>
    +<network_drivers/network/dns/dns.c>
    +<network_drivers/presentation/http/route/http_route.c>
    +<mmgr/protomem.c>
    +<server/signaling/signaling.c>
    +<server/filesystem/filesystem.c>
    +<server/filesystem/mnt.c>
    +<network_drivers/application/file_serving/file_serving.c>
    +<network_drivers/application/http_range.c>
    +<network_drivers/presentation/http/auth/auth.c>
    +<server/response.c>
    +<server/websocket_sse.c>
    +<server/regex.c>
    +<server/middleware.c>
    +<server/webdav_handler.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/secure.c>
    +<network_drivers/presentation/http/http.c>
test_filter =
    integration/server/test_range

; Syslog client (RFC 5424) line formatter. Pure formatter - no lwIP/UDP - so it
; unit-tests on the host (udp_transport provides host stubs for pc_udp_sendto).
[env:native_syslog]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SYSLOG=1
build_src_filter =
    +<services/net/syslog/syslog.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_syslog

; SMB2 client (network_drivers/application/smb, MS-SMB2 / MS-NLMP): the SMB2 wire codec (transport frame,
; sync header, NEGOTIATE, SESSION_SETUP, TREE_CONNECT/CREATE/CLOSE/READ/WRITE); the NTLM digests
; MD4 (RFC 1320) / MD5 (RFC 1321) / HMAC-MD5 (RFC 2104) KAT-verified vs the RFC vectors; the
; NTLMv2 response (NThash / NTOWFv2 / NTProofStr / SessionBaseKey) vs the MS-NLMP section 4.2
; worked example; the NTLMSSP message codec; the SPNEGO GSS-API DER wrapping (RFC 4178) verified
; byte-exact + independently vs openssl; and the smb_client dialogue engine (the full NEGOTIATE
; -> NTLMv2 SESSION_SETUP -> TREE_CONNECT -> CREATE -> CLOSE handshake) driven by a scripted mock
; SMB2 server over a send/recv seam. Pure host tests.
[env:native_smb]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SMB=1
build_src_filter =
    +<network_drivers/application/smb/smb2.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/aead/aesccm.c>
    +<crypto/hash/md.c>
    +<network_drivers/application/smb/ntlm.c>
    +<network_drivers/application/smb/ntlmssp.c>
    +<network_drivers/application/smb/spnego.c>
    +<network_drivers/application/smb/smb_client.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/kdf/kdf.c>
    +<crypto/mac/aes_cmac.c>
    +<crypto/hash/sha512.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
    +<crypto/rng/rng.c>
    +<crypto/cipher/chacha20.c>
test_filter =
    unit/smb/test_smb2
    unit/smb/test_smb_crypto
    unit/smb/test_ntlm
    unit/smb/test_ntlmssp
    unit/smb/test_spnego
    integration/smb/test_smb_client

; SMTP client (RFC 5321) dialogue engine (services/net/smtp/smtp_run): greeting/EHLO/AUTH
; LOGIN/MAIL/RCPT/DATA over a send/recv seam, with dot-stuffing + multi-line reply
; parsing. Driven by a scripted mock server on the host - no lwIP or TLS.
[env:native_smtp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SMTP=1
build_src_filter =
    +<services/net/smtp/smtp.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_smtp

; NTP/SNTP server (RFC 5905 server mode): the response codec (ntp_server_build_response) -
; version echo, mode/LI/stratum, origin-timestamp copy, reference/receive/transmit stamps,
; big-endian encoding, and the length guards - plus the UDP/123 bind and its handler, driven
; through the host stack's pcb so the reply that reaches the wire is the one a client would get.
[env:native_ntp_server]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTP_SERVER=1
    -DPC_ENABLE_TIME_SOURCE=1
build_src_filter =
    +<network_drivers/application/ntp_server/ntp_server.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<shared_primitives/ip.c>
    +<services/timing_position/time_source/time_source.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_ntp_server

; The SNTP client (network_drivers/application/ntp_service, RFC 4330), which is the client on
; every target: the mode-3 request it puts on the wire, the mode-4 reply it accepts, and the ones it
; refuses - a wrong mode, a kiss-o'-death stratum, an origin that does not echo the request's transmit
; stamp, and a pre-2021 clock. Plus the epoch advancing off the monotonic tick between syncs.
[env:native_ntp_service]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTP=1
build_src_filter =
    +<network_drivers/application/ntp_service/ntp_service.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<shared_primitives/ip.c>
    +<shared_primitives/http_date.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_ntp_service

; The portable mDNS / DNS-SD responder (network_drivers/application/mdns_service, RFC 6762 /
; RFC 6763) on the build where PC_HAS_VENDOR_MDNS is 0: joining 224.0.0.251:5353 through the UDP
; listener, and answering a query for the service-enumeration name, a service type, and an instance's
; SRV / TXT - each answer read back off the wire and decoded by the test's own label reader.
[env:native_mdns_service]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MDNS=1
build_src_filter =
    +<network_drivers/application/mdns_service/mdns_service.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<network_drivers/network/dns/dns_wire.c>
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<shared_primitives/ip.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_mdns_service

; The DNS name on the wire (network_drivers/network/dns/dns_wire, RFC 1035 sec 3.1 / 4.1.4):
; labels to a dotted string and back, compression pointers followed for an answer and refused for a
; question, the hop limit that makes a self-referential message terminate, the undefined label types,
; the label and destination bounds, and the case-insensitive compare names are defined to use.
[env:native_dns_wire]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DNS_SERVER=1
build_src_filter =
    +<network_drivers/network/dns/dns_wire.c>
    +<mmgr/protomem.c>
test_filter =
    unit/network/test_dns_wire

; Authoritative DNS server (network_drivers/network/dns/dns_server): the A-record response builder
; (QNAME parse, compressed A answer, NXDOMAIN, non-A query, header flags, malformed
; guards), the built-in name->IP table (add / case-insensitive lookup / clear), and begin() binding
; port 53 on the UDP listener, driven against the host pcb driver.
[env:native_dns_server]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DNS_SERVER=1
build_src_filter =
    +<network_drivers/network/dns/dns_server.c>
    +<network_drivers/network/dns/dns_wire.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/network/test_dns_server

; DS1307/DS3231 RTC conversions (services/peripherals/rtc): BCD time registers <-> Unix epoch in
; 24- and 12-hour encodings, leap years, clock-halt/century bit masks, range validation,
; and a round-trip over the 2000-2099 range. Pure - the I2C read/write is ESP32-only.
[env:native_rtc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RTC=1
build_src_filter =
    +<services/peripherals/rtc/rtc.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_rtc

; TCP relay / DNAT byte pump (services/net/relay): the bidirectional relay engine that
; publishes an internal host:port through the server. Tests cover a bidirectional transfer, the
; backpressure carry (a send that accepts partial writes), independent half-close with shutdown
; propagation, a large multi-step transfer (byte-exact), and a seam error - driven by two mock
; sockets over the send/recv seams. Pure host tests.
[env:native_relay]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RELAY=1
build_src_filter =
    +<services/net/relay/relay.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_relay

; LD2410 mmWave radar codec (services/peripherals/ld2410): decoding a basic and an engineering-mode
; report frame, rejecting malformed frames, the byte-by-byte stream reassembler (resync past
; noise, split feeds, absurd-length drop), the presence/distance helpers, and the exact bytes
; of the config-command encoders. Pure - the UART pump is ESP32-only.
[env:native_ld2410]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LD2410=1
build_src_filter =
    +<services/peripherals/ld2410/ld2410.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_ld2410

; IEC 61784-3 black-channel Safety Communication Layer primitives (services/machine_tool/safety_scl):
; the monitoring-counter state machine, the receive watchdog, and the fail-safe latch the four safety
; profiles compose. Covers each way a black channel misbehaves - corruption, loss, duplication,
; reordering, insertion - plus the watchdog (incl. a millis() rollover and the disabled case), the
; no-silent-reheal latch, counter wrapping at a narrow modulus, and the explicit re-establish. The
; CRC is the profile's, not this module's. Pure core on a synthetic clock. Pure host tests.
[env:native_safety_scl]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SAFETY_SCL=1
build_src_filter =
    +<services/machine_tool/safety_scl/safety_scl.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_safety_scl

; Waveshare HMMD 24GHz mmWave micro-motion radar codec (services/peripherals/hmmd): the LD2410-family
; little-endian framing, the report parse (detection flag, distance, all 16 gate energies), rejecting
; malformed frames, the byte-by-byte stream reassembler (resync past noise, split feeds, absurd-length
; drop), the exact bytes of the command encoders (open/close command mode, firmware 0x0000, serial
; 0x0011, config 0x0008, register 0x0002), and ACK decoding. Pure - the UART pump is ESP32-only.
[env:native_hmmd]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HMMD=1
build_src_filter =
    +<services/peripherals/hmmd/hmmd.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_hmmd

; RCWL-0516 Doppler presence sensor + the shared one-GPIO presence facade
; (services/peripherals/rcwl0516): the debounce that swallows comparator chatter, the hold that bridges the
; module's ~2s retrigger gaps into one continuous presence span, the consume-once edge event,
; wrap-safety across a millis() rollover, and the degenerate zero-debounce / zero-hold configs.
; Pure core driven by a synthetic clock - no GPIO, no real time. Pure host tests.
[env:native_rcwl0516]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RCWL0516=1
build_src_filter =
    +<services/peripherals/rcwl0516/rcwl0516.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_rcwl0516

; SEN0192 microwave motion sensor presence state machine (services/peripherals/sen0192): presence
; asserts on an active sample and holds for the configured window after the last active sample, clears
; after it, counts clear->present edges, and honors OUT polarity. Pure - the GPIO read is ESP32-only.
[env:native_sen0192]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SEN0192=1
build_src_filter =
    +<services/peripherals/sen0192/sen0192.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_sen0192

; MPR121 capacitive-touch codec (services/peripherals/mpr121): decoding the touch-status word into
; an electrode bitmask (masking proximity / over-current), the per-electrode touched test, the
; proximity / over-current flags, the 10-bit filtered/baseline word, and the exact bytes of the
; register bring-up sequence. Pure - the I2C read/write is ESP32-only.
[env:native_mpr121]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MPR121=1
build_src_filter =
    +<services/peripherals/mpr121/mpr121.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_mpr121

; Sensirion SHT3x temperature/humidity codec (services/peripherals/sht3x): the CRC-8 against the
; datasheet check value (0xBEEF -> 0x92), the raw-tick -> milli-unit temperature/humidity
; conversions at the range endpoints and a mid value, and the six-byte single-shot response
; parse (both CRCs checked, corrupt CRC rejected). Pure - the I2C transfer is ESP32-only.
[env:native_sht3x]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SHT3X=1
build_src_filter =
    +<services/peripherals/sht3x/sht3x.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_sht3x

; SMBus 3.1 Packet Error Code (services/peripherals/smbus): the address byte with its direction
; bit, the PEC over a write transaction and over a read transaction (which spans both halves and
; the repeated-start address), and that the checksum depends on the address and the direction as
; well as the payload. The PEC comes from the shared CRC engine, so each vector is checked against
; pc_crc(&PC_CRC8_SMBUS) directly rather than against a second implementation.
[env:native_smbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SMBUS=1
build_src_filter =
    +<services/peripherals/smbus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_smbus

; PMBus 1.3 numeric encodings (services/peripherals/pmbus): the VOUT_MODE format selector and its
; 5-bit signed exponent, the LINEAR11 11-bit signed mantissa and 5-bit signed exponent with decode
; and round trip, LINEAR16 against an exponent the part reports separately, the DIRECT coefficient
; form, and that a value the exponent pushes out of an int32 of micro-units is refused rather than
; wrapped.
[env:native_pmbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SMBUS=1
    -DPC_ENABLE_PMBUS=1
build_src_filter =
    +<services/peripherals/pmbus.c>
    +<services/peripherals/smbus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_pmbus

; End to end from the host harness through the library: a real peripheral driver is called, it
; goes through the real I2C / SPI bus owner, and the bytes it put on the wire are asserted. Covers
; the SHT3x measurement command and its CRC-checked reply, the PCA9685 five-byte channel write,
; the INA219 big-endian register order, the RTC register-pointer-then-burst read and its write,
; SMBus with and without the PEC, the little-endian SMBus word, a full bus scan, and that a device
; that does not acknowledge makes the driver report failure.
[env:native_bus_wire]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SHT3X=1
    -DPC_ENABLE_PCA9685=1
    -DPC_ENABLE_INA219=1
    -DPC_ENABLE_RTC=1
    -DPC_ENABLE_SMBUS=1
build_src_filter =
    +<services/peripherals/sht3x/sht3x.c>
    +<services/peripherals/pca9685/pca9685.c>
    +<services/peripherals/ina219/ina219.c>
    +<services/peripherals/rtc/rtc.c>
    +<services/peripherals/smbus.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/peripherals/test_bus_wire

; PCA9685 PWM/servo codec (services/peripherals/pca9685): the PRESCALE computation from a PWM
; frequency (with clamping), the per-channel register address, the servo pulse-width -> 12-bit
; count conversion (with clamping), and the 5-byte channel PWM write encoder (including the
; full-on/off flag). Pure - the I2C writes are ESP32-only.
[env:native_pca9685]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PCA9685=1
build_src_filter =
    +<services/peripherals/pca9685/pca9685.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_pca9685

; ADS1115 16-bit ADC codec (services/peripherals/ads1115): building the 16-bit config word for a
; single-shot single-ended reading (channel MUX, gain, data rate, start/mode/comparator bits,
; with out-of-range fallbacks), and converting a signed 16-bit sample to microvolts across the
; gain's full-scale range. Pure - the I2C transfer is ESP32-only.
[env:native_ads1115]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ADS1115=1
build_src_filter =
    +<services/peripherals/ads1115/ads1115.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_ads1115

; INA219 current/power codec (services/peripherals/ina219): decoding the bus-voltage register (bits
; [15:3], LSB 4 mV, status bits ignored) and the shunt-voltage register (signed, LSB 10 uV),
; computing the calibration register from the current LSB and shunt resistance, and scaling the
; raw current / power registers. Pure - the I2C transfer is ESP32-only.
[env:native_ina219]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_INA219=1
build_src_filter =
    +<services/peripherals/ina219/ina219.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_ina219

; HPACK header compression for HTTP/2 (RFC 7541): prefix-integer coding (App C.1), the
; Huffman string code (App B / C.4.1), the first-request decode with dynamic-table insertion
; (C.3.1), dynamic-table indexing + FIFO eviction + size updates, and the server encoder
; (static indexing + literal round-trip). Pure host codec.
[env:native_hpack]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP2=1
build_src_filter =
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http2/hpack.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_hpack

; HTTP/2 binary framing (network_drivers/presentation/http/http2/h2_frame, RFC 9113): the
; 9-byte frame header parse/write (24-bit length, reserved-bit masking), SETTINGS build + parse
; with validation, and the exact bytes of the SETTINGS-ACK / WINDOW_UPDATE / RST_STREAM / GOAWAY
; / PING-ACK / HEADERS / DATA builders. Pure host codec.
[env:native_h2frame]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP2=1
build_src_filter =
    +<network_drivers/presentation/http/http2/h2_frame.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_h2_frame

; HTTP/2 connection engine (network_drivers/presentation/http/http2/h2_conn, RFC 9113):
; initial SETTINGS on init, preface + client SETTINGS -> SETTINGS ACK, decoding a real
; HPACK-encoded request into the header callbacks, PING -> PING ACK, split-across-reads frame
; reassembly, and h2_conn_respond serializing a HEADERS + DATA response decoded back.
[env:native_h2conn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP2=1
build_src_filter =
    +<network_drivers/presentation/http/http2/h2_conn.c>
    +<network_drivers/presentation/http/http2/h2_frame.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http2/hpack.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/http/test_h2_conn

; QUIC variable-length integer codec (network_drivers/presentation/http/http3/quic_varint,
; RFC 9000 sec 16) - the foundational HTTP/3 primitive: the Appendix A.1 worked examples (1/2/4/8
; byte encodings), the non-minimal decode, length computation, and overflow / truncation guards.
[env:native_quic_varint]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_varint

; HTTP/3 framing (network_drivers/presentation/http/http3/h3_frame, RFC 9114 sec 7): the
; type+length varint header parse/write (incl. a multi-byte length), the DATA / HEADERS /
; SETTINGS / GOAWAY builders, the SETTINGS payload round-trip + reserved-id rejection, and the
; reserved HTTP/2 frame-type check. Pure host codec.
[env:native_h3frame]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/h3_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_h3_frame

; JWT (HS256) bearer-auth verification. Pure crypto/codec - reuses the SSH
; HMAC-SHA-256 + base64 primitives - so it unit-tests on the host.
[env:native_jwt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_JWT=1
build_src_filter =
    +<services/security/jwt/jwt.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_jwt

; Streaming file upload: POST body -> FS file via the parser streaming hook.
; Small BODY_BUF_SIZE exercises multi-chunk streaming; the mock FS captures the
; written bytes (test/mocks/FS.h write-capture).
[env:native_upload]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_UPLOAD=1
    -DBODY_BUF_SIZE=64
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<crypto/hash/sha256.c>
    +<protocore.c>
    +<network_drivers/network/network.c>
    +<network_drivers/network/dns/dns.c>
    +<network_drivers/application/file_serving/file_serving.c>
    +<network_drivers/application/http_range.c>
    +<network_drivers/presentation/http/auth/auth.c>
    +<server/response.c>
    +<server/websocket_sse.c>
    +<server/regex.c>
    +<server/middleware.c>
    +<server/webdav_handler.c>
    +<network_drivers/application/upload_service/upload_service.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<network_drivers/presentation/http/route/http_route.c>
    +<mmgr/protomem.c>
    +<server/filesystem/mnt.c>
    +<server/signaling/signaling.c>
    +<mmgr/protostr.c>
    +<mmgr/secure.c>
    +<network_drivers/presentation/http/http.c>
test_filter =
    integration/server/test_upload

; Outbound HTTP client: URL parser + request builder + response parser. Pure
; string logic (the lwIP/mbedTLS transport is ESP32-only) so it unit-tests host.
[env:native_http_client]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_CLIENT=1
build_src_filter =
    +<services/net/http_client/http_client.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_http_client

; RFC-compliance suite: builds with all enforcement at production defaults
; (PC_ENFORCE_HOST_HEADER=1) and exercises the strict behaviors.
[env:native_compliance]
extends = native_base
build_src_filter =
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_compliance

[env:native_mqtt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MQTT=1
build_src_filter =
    +<services/iot/mqtt/mqtt.c>
    +<server/clock/clock.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_mqtt

[env:native_ws_client]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WS_CLIENT=1
build_src_filter =
    +<services/net/ws_client/ws_client.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_ws_client

; The plaintext pool accessor (mmgr/plaintext): bump-allocate + reset semantics,
; alignment, and fail-closed exhaustion. Pure host tests - no lwIP/sockets - so it builds from
; the accessor and the pool mechanism alone.
[env:native_plaintext]
extends = native_base
build_src_filter =
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/secure.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_plaintext

[env:native_snmp_trap]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SNMP=1
    -DPC_ENABLE_SNMP_TRAP=1
build_src_filter =
    +<services/net/snmp/snmp_notify.c>
    +<services/net/snmp/snmp_ber.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_snmp_trap

; RFC 1951 INFLATE core (the WebSocket permessage-deflate decompressor). Pure
; codec - no sockets - so it unit-tests on the host against zlib-generated
; vectors (scratchpad/gen_vectors.py).
[env:native_inflate]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_inflate

; RFC 1951 DEFLATE core (the WebSocket permessage-deflate compressor). Pure codec
; - no sockets - so it unit-tests on the host by round-tripping through INFLATE
; (which test_inflate validates against zlib), proving the emitted streams are
; valid RFC 1951.
[env:native_deflate]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<network_drivers/presentation/codec/deflate/deflate.c>
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_deflate

; SSH server-to-client streaming compressor (zlib@openssh.com / zlib): a context-takeover
; DEFLATE stream (persistent sliding window across packets, sync-flush per packet, zlib
; wrapper). The whole session is one zlib stream, so concatenating packets and decoding via
; inflate_raw (validated vs Python zlib in test_inflate) proves cross-packet back-references
; resolve; packet-by-packet decodability under a reference zlib.decompressobj (OpenSSH's model)
; is cross-checked out-of-tree.
[env:native_ssh_zlib]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_ZLIB=1
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<network_drivers/presentation/ssh/transport/ssh_zlib.c>
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_zlib

; SSH s2c compression WIRING with the full SSH stack built with PC_ENABLE_SSH_ZLIB=1:
; the compression owner (ssh_comp) + its NEWKEYS / USERAUTH_SUCCESS activation + the packet-layer
; compress path in ssh_pkt_send. Drives the packet layer directly and reconstructs the continuous
; zlib stream from the framed packets, decoding via inflate_raw. Compile-verifies every #if
; PC_ENABLE_SSH_ZLIB block across transport / server / packet.
[env:native_ssh_comp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SSH=1
    -DPC_ENABLE_SSH_ZLIB=1
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/mac/hmac_sha512.c>
    +<crypto/cipher/chacha20.c>
    +<crypto/rng/rng.c>
    +<mmgr/protomem.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<crypto/cipher/aes256ctr.c>
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<network_drivers/presentation/ssh/transport/ssh_keymat.c>
    +<network_drivers/presentation/ssh/transport/ssh_dh.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<network_drivers/presentation/ssh/transport/ssh_packet.c>
    +<network_drivers/presentation/ssh/transport/ssh_comp.c>
    +<network_drivers/presentation/ssh/transport/ssh_zlib.c>
    +<network_drivers/presentation/ssh/transport/ssh_inflate.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<network_drivers/presentation/ssh/transport/ssh_transport.c>
    +<network_drivers/presentation/ssh/auth/ssh_auth.c>
    +<network_drivers/presentation/ssh/connection/ssh_channel.c>
    +<network_drivers/presentation/ssh/connection/ssh_flow_control.c>
    +<network_drivers/presentation/ssh/connection/ssh_server.c>
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<mmgr/secure.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<../core_setup/hal/host/host_nvs.c>
test_filter =
    integration/ssh/test_ssh_comp
extra_scripts =
    pre:test/ensure_test_keys.py

; WebSocket permessage-deflate (RFC 7692) inbound path wired through the real WS
; stack: handshake negotiation, the RSV1 frame path, and INFLATE delivery (with
; the table scratch borrowed from the shared arena). Runs test_websocket with the
; feature on (the deflate cases are #if-gated).
[env:native_ws_deflate]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENFORCE_HOST_HEADER=0
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_WEBSOCKET=1
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http_parser/http_parser.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/presentation/presentation.c>
    +<mmgr/protostr.c>
    +<network_drivers/session/session.c>
    +<server/proto_builtins.c>
    +<mmgr/plaintext.c>
    +<mmgr/arena.c>
    +<network_drivers/session/worker.c>
    +<crypto/hash/sha1.c>
    +<network_drivers/presentation/codec/base64/base64.c>
    +<network_drivers/presentation/http/websocket/websocket.c>
    +<network_drivers/presentation/http/sse/sse.c>
    +<network_drivers/presentation/codec/multipart/multipart.c>
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<network_drivers/presentation/codec/deflate/deflate.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_websocket

; Multi-source time fallback matrix (services/timing_position/time_source): priority-ordered
; query of user time sources with first-valid-wins fallback. Pure core - no
; sockets/RTC - so it unit-tests on the host with mock sources.
[env:native_time_source]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TIME_SOURCE=1
build_src_filter =
    +<services/timing_position/time_source/time_source.c>
    +<mmgr/protomem.c>
test_filter =
    unit/timing_position/test_time_source

; Typed NVS config store (services/storage/config_store): string/u32/blob round-trips,
; defaults, capacity, erase/clear - run against the host NVS backend that hal/nvs.h
; selects off-target (the ESP32 Preferences backend is compiled in only on-target).
[env:native_config_store]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CONFIG_STORE=1
build_src_filter =
    +<services/storage/config_store/config_store.c>
    +<../core_setup/hal/host/host_nvs.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_config_store

; MAC-derived device UUID (server/signaling/device_id): RFC 4122 v5 from a MAC via SHA-1.
; Pure core - the ESP32 MAC read is ARDUINO-gated - so it unit-tests on the host
; against Python uuid.uuid5 reference values.
[env:native_device_id]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DEVICE_ID=1
build_src_filter =
    +<server/signaling/device_id.c>
    +<crypto/hash/sha1.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_device_id

; Per-IP brute-force auth lockout (services/security/auth_lockout): exponential-backoff
; lockout state machine. Pure core (the millisecond clock is passed in), so it
; unit-tests on the host. PC_ENABLE_AUTH_LOCKOUT (and its required parent
; PC_ENABLE_AUTH) are set so the flag-guarded code compiles.
[env:native_auth_lockout]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_AUTH=1
    -DPC_ENABLE_AUTH_LOCKOUT=1
build_src_filter =
    +<services/security/auth_lockout/auth_lockout.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_auth_lockout

; Trusted-reverse-proxy forwarded-client resolver (services/security/forwarded_trust): a
; Forwarded / X-Forwarded-For client address is honored only when the real TCP peer
; is a configured trusted-upstream CIDR, else the TCP peer is kept. Pure (no
; sockets), so it host-tests; a direct/untrusted peer's spoofed header is never
; believed. PC_ENABLE_FORWARDED_TRUST (+ its required parents AUTH / AUTH_LOCKOUT)
; are set so the flag-guarded config compiles.
[env:native_forwarded_trust]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_AUTH=1
    -DPC_ENABLE_AUTH_LOCKOUT=1
    -DPC_ENABLE_FORWARDED_TRUST=1
build_src_filter =
    +<services/security/forwarded_trust/forwarded_trust.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_forwarded_trust

; Stateless HMAC-signed CSRF token (services/security/csrf): issue/verify with a fixed
; secret unit-tests on the host (PC_ENABLE_CSRF set). Pulls in the SSH
; HMAC-SHA256 + SHA-256 core.
[env:native_csrf]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CSRF=1
build_src_filter =
    +<mmgr/frame.c>
    +<services/security/csrf/csrf.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_csrf

; Telemetry math (services/iot/telemetry): moving-window stats, rate-of-change, and
; totalizer. Pure computation (synthetic clock passed in), so it unit-tests on
; the host.
[env:native_telemetry]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TELEMETRY=1
build_src_filter =
    +<services/iot/telemetry/telemetry.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_telemetry

; Dashboard widget-table JSON serializers (services/web/dashboard core). Pure logic;
; the route/SSE wiring (dashboard_routes.c) is excluded so this tests on the
; host without the server.
[env:native_dashboard]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DASHBOARD=1
    -DPC_ENABLE_SSE=1
build_src_filter =
    +<services/web/dashboard/dashboard.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/web/test_dashboard

; The layer 1 interface registry (network_drivers/physical, Physical.iface): an interface is
; an id, a kind and the callback that puts bytes on the wire, and a device carries several of mixed
; kind. Registration and its refusals (duplicate id, null send, table full), the readbacks, reset,
; row walking, and that send hands the callback the id and ctx it was registered with. Every
; forwarded byte leaves through this send, so it is covered here and not only through forward.
[env:native_phy_iface]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_PHY_MAX_IFACES=4
build_src_filter =
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<mmgr/protomem.c>
test_filter =
    unit/physical/test_iface

; Egress-interface reporting (network_drivers/physical). The esp_netif default-
; route lookups are ESP32-only; the pure interface-key classifier host-tests here.
[env:native_net_egress]
extends = native_base
build_src_filter =
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<mmgr/protomem.c>
test_filter =
    unit/physical/test_net_egress

; Outbound TCP client transport (network_drivers/transport/tcp/tcp_client.c), the pooled
; layer-4 peer of tcp.c used by http_client / mqtt / ws_client / relay / smtp / ssh
; port-forward (PC_NEED_CLIENT). The marshaled path is driven against the host pcb driver:
; the connect completes inline, sends land in the capture, and received bytes arrive through
; the recv callback the client armed. Covers the dial, the ring, the peer FIN and pool exhaustion.
[env:native_client]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_CLIENT=1
build_src_filter =
    +<network_drivers/transport/tcp.c>
    +<mmgr/secure.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/network/dns/dns_resolver.c>
    +<network_drivers/network/dns/dns_wire.c>
    +<network_drivers/network/dns/dns.c>
    +<network_drivers/network/network.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_client

; Flash partition-map monitor (services/storage/partition_monitor core): the kind
; classifier + JSON serializer host-test here; the esp_partition walk is ESP32-only.
[env:native_partition]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PARTITION_MONITOR=1
build_src_filter =
    +<services/storage/partition_monitor/partition_monitor.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_partition_monitor

; CBOR (RFC 8949) encoder (network_drivers/presentation/codec/cbor): a pure byte-output
; codec, host-tested against the RFC 8949 Appendix A vectors.
[env:native_cbor]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CBOR=1
build_src_filter =
    +<network_drivers/presentation/codec/cbor/cbor.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_cbor

; MessagePack encoder (network_drivers/presentation/codec/msgpack): a pure byte-output
; codec, host-tested against the spec encodings.
[env:native_msgpack]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MSGPACK=1
build_src_filter =
    +<network_drivers/presentation/codec/msgpack/msgpack.c>
    +<mmgr/protomem.c>
test_filter =
    unit/codec/test_msgpack

; GPIO pin-mapper / browser diag core (server/signaling/gpio_map): direction names, JSON
; serializer, control-POST parser, output guard - all pure and host-tested.
[env:native_gpio_map]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GPIO_MAP=1
build_src_filter =
    +<server/signaling/gpio_map.c>
    +<mmgr/frame.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_gpio_map

; UDP telemetry line builder (services/iot/udp_telemetry): InfluxDB line-protocol
; formatting, host-tested. The UDP cast is ESP32-only (#ifdef ARDUINO).
[env:native_udp_telemetry]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_UDP_TELEMETRY=1
build_src_filter =
    +<services/iot/udp_telemetry/udp_telemetry.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transport/test_udp_telemetry

; StatsD metrics client (services/iot/statsd): the pure line formatter (name:value|type,
; sample rate, DogStatsD tags) plus the count/gauge/timing/set emit helpers, whose sent
; bytes are captured through the transport UDP service's host seam.
[env:native_statsd]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_STATSD=1
build_src_filter =
    +<services/iot/statsd/statsd.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/transport/test_statsd

; Heap/stack guardrails (services/security/guardrails): threshold evaluator + JSON,
; host-tested. Live sampling is ESP32-only (#ifdef ARDUINO).
[env:native_guardrails]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GUARDRAILS=1
build_src_filter =
    +<services/security/guardrails/guardrails.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/security/test_guardrails

; Software watchdog / deadlock detection + safe-state (server/failsafe): the wrap-safe
; overdue predicate, the lifeline registry, fire-once-per-episode breach callback, and JSON.
; Pure host tests with a synthetic clock.
[env:native_failsafe]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FAILSAFE=1
build_src_filter =
    +<server/failsafe.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_failsafe

; Dynamic sleep-cycle scheduler (server/sleep_sched): the wrap-safe idle->sleep-window
; decision core with a doubling ramp clamped to a ceiling. Pure host tests, synthetic clock.
[env:native_sleep_sched]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SLEEP_SCHED=1
build_src_filter =
    +<server/sleep_sched.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_sleep_sched

; Application-layer signaling (server/signaling): the state bucket. Deposits go in from the loop
; (response class tallies, and the uptime + conn/listener bitmasks a tick already has in hand), a
; read hands back a copy rather than a window, and kill forwards a slot to the transport unchanged.
; The module holds no logic by design, so the tests assert exactly that. pc_conn_close is stubbed:
; the forward is what is under test, not lwIP.
[env:native_signaling]
extends = native_base
build_src_filter =
    +<server/signaling/signaling.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_signaling

; Flash wear-leveling slot selector (server/filesystem/wearlevel): least-worn pick (ties -> lowest
; index), saturating mark, and the wear-imbalance spread metric. Pure host tests - a 4000-write
; pick+mark loop must level every slot exactly.
[env:native_wearlevel]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WEARLEVEL=1
build_src_filter =
    +<server/filesystem/wearlevel.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_wearlevel

; Network adaptation decisions (services/net/netadapt): TCP receive-window sizing from the free
; heap (reserve + quarter-of-spare, clamped) and the DHCP->static-IP fallback trigger. Pure host tests.
[env:native_netadapt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NETADAPT=1
build_src_filter =
    +<services/net/netadapt/netadapt.c>
    +<mmgr/protomem.c>
test_filter =
    unit/net/test_netadapt

; DShot ESC throttle codec (services/peripherals/dshot): the 16-bit frame (11-bit value + telemetry +
; 4-bit nibble-xor CRC), the bidirectional inverted-CRC variant, decode/validate, and per-rate bit
; timing. Pure host tests with hand-computed vectors.
[env:native_dshot]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DSHOT=1
build_src_filter =
    +<services/peripherals/dshot/dshot.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_dshot

; HART / HART-IP codec (services/fieldbus/hart): the HART command frame (longitudinal XOR checksum,
; short + long addressing) build/parse and the 8-octet HART-IP message header. Pure host tests with
; hand-verified vectors (command-0 checksum 0x82).
[env:native_hart]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HART=1
build_src_filter =
    +<services/fieldbus/hart/hart.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_hart

; Network Time Security codec (network_drivers/application/nts, RFC 8915): the NTS-KE TLV records (build the
; standard request, parse a response) and the NTS NTP extension-field framing (unique id / cookie,
; RFC 7822 4-byte padding). Pure host tests with spec-derived vectors.
[env:native_nts]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTS=1
build_src_filter =
    +<network_drivers/application/nts/nts.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_nts

; DDS / RTPS framing codec (services/iot/dds): the 20-octet RTPS header (magic/version/vendor/
; guidPrefix) and the submessage TLV (id/flags/octetsToNextHeader, endianness flag), build + parse.
; Pure host tests with spec-derived vectors.
[env:native_dds]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DDS=1
build_src_filter =
    +<services/iot/dds/dds.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_dds

; XMPP stanza codec (services/iot/xmpp, RFC 6120): XML-escaped stream/message/presence/iq
; builders and the stanza-name + attribute readers. Pure host tests with exact-output vectors.
[env:native_xmpp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_XMPP=1
build_src_filter =
    +<services/iot/xmpp/xmpp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/iot/test_xmpp

; Raw L2 Ethernet frame codec (services/fieldbus/rawl2): Ethernet II + 802.1Q VLAN build/parse and the
; 802.3 FCS (CRC-32). Pure host tests (FCS check value 0xCBF43926).
[env:native_rawl2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RAWL2=1
build_src_filter =
    +<services/fieldbus/rawl2/rawl2.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_rawl2

; Single-page-app micro-routing (services/web/spa_router): the serve-file / serve-shell /
; passthrough decision from a request path (extension test + API prefix). Pure host tests.
[env:native_spa_router]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SPA_ROUTER=1
build_src_filter =
    +<services/web/spa_router/spa_router.c>
    +<mmgr/protomem.c>
test_filter =
    unit/web/test_spa_router

; IEC 61850 GOOSE publisher codec (services/energy/goose): the BER IECGoosePdu (gocbRef..allData,
; minimal-length INTEGERs with the positive leading-zero rule) + the GOOSE header + Ethernet frame
; (ethertype 0x88B8). Pure host tests with structural + spec-derived vectors.
[env:native_goose]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_GOOSE=1
build_src_filter =
    +<services/energy/goose/goose.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_goose

; MTConnect agent response codec (services/machine_tool/mtconnect, ANSI/MTC1.4): the incremental
; MTConnectStreams builder (header + Samples/Events/Condition), the MTConnectDevices probe (device
; model), the MTConnectAssets response (CuttingTool life cycle), the sample sequence cursor (a rolling
; observation ring replayed as a from/count long-poll window), and the MTConnectError document,
; XML-escaped. Pure host tests with structural assertions.
[env:native_mtconnect]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MTCONNECT=1
build_src_filter =
    +<services/machine_tool/mtconnect/mtconnect.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_mtconnect

; Write-ahead store for atomic buffer-to-flash storage (services/storage/wal): CRC32 record
; framing + crash-recovery replay (the atomicity core), plus the A/B superblock + checkpoint + mount
; layer over a block-device seam. Pure host tests, incl. the CRC-32 check vector, torn/truncated-tail
; recovery to the last good record, checkpoint durability, and superblock fallback.
[env:native_wal]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAL=1
build_src_filter =
    +<services/storage/wal/wal.c>
    +<services/storage/wal/wal_store.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_wal
    protocols/storage/test_wal_store

; SAE J2735 V2X codec (services/transportation/j2735): the ASN.1 UPER bit primitive layer (constrained
; INTEGER / BOOLEAN / bit fields) and the BSMcore block encode/decode. Pure host tests with
; hand-computed bit patterns (162-bit BSMcore -> 21 octets).
[env:native_j2735]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_J2735=1
build_src_filter =
    +<services/transportation/j2735/j2735.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_j2735

; NEMA TS 2 SDLC frame codec (services/transportation/nema_ts2): the traffic-cabinet bus frame
; ([address][control][frame-type][data][CRC-16/X-25]) build + validate. Pure host tests (CRC check
; value 0x906E).
[env:native_nema_ts2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NEMA_TS2=1
build_src_filter =
    +<services/transportation/nema_ts2/nema_ts2.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_nema_ts2

; GE Fanuc SNP serial frame codec (services/fieldbus/snp): the Series Ninety Protocol frame
; ([control][length][data][arithmetic-sum BCC]) build + validate. Pure host tests.
[env:native_snp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SNP=1
build_src_filter =
    +<services/fieldbus/snp/snp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_snp

; AutomationDirect DirectNET serial frame codec (services/fieldbus/directnet): the header (SOH +
; ASCII-hex slave/type/addr/blocks + ETB + LRC) and data (STX + data + ETX + LRC) frames build/parse.
; Pure host tests.
[env:native_directnet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DIRECTNET=1
build_src_filter =
    +<services/fieldbus/directnet/directnet.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_directnet

; IEEE 2030.5 (SEP 2.0) resource codec (services/energy/sep2): the DeviceCapability, EndDevice,
; and DERControl XML documents (urn:ieee:std:2030.5:ns), XML-escaped. Pure host tests.
[env:native_sep2]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SEP2=1
build_src_filter =
    +<services/energy/sep2/sep2.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_sep2

; PROFINET DCP frame codec (services/fieldbus/profinet): the 10-octet DCP header + option/suboption
; blocks (even-padding) build + parse/walk, for Identify/Set over raw L2 (ethertype 0x8892). Pure host
; tests with spec-derived vectors.
[env:native_profinet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PROFINET=1
build_src_filter =
    +<services/fieldbus/profinet/profinet.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_profinet

; NTCIP transportation object OIDs (services/transportation/ntcip): the NTCIP 1202 signal-controller +
; 1203 DMS object roots under 1.3.6.1.4.1.1206.4.2 and the OID builder (root + instance index), for the
; shipped SNMP agent. Pure host tests.
[env:native_ntcip]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_NTCIP=1
build_src_filter =
    +<services/transportation/ntcip/ntcip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_ntcip

; OpenADR 3.0 JSON codec (services/energy/openadr): the event (programID + eventName + interval
; payloads) and report (VEN reading) JSON documents build, with escaping + a no-stdlib 3-decimal
; formatter. Pure host tests.
[env:native_openadr]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OPENADR=1
build_src_filter =
    +<services/energy/openadr/openadr.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_openadr

; IEC 61850 MMS PDU codec (services/energy/mms): the BER confirmed-request/response Read PDUs
; (invokeID + read service + named ObjectName), build + parse. Pure host tests with structural vectors.
[env:native_mms]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MMS=1
build_src_filter =
    +<services/energy/mms/mms.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_mms

; CC-Link cyclic fieldbus frame codec (services/fieldbus/cclink): the frame ([station][command][bit
; data][word data][sum]) build + parse and the bit/word process-image accessors. Pure host tests.
[env:native_cclink]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CCLINK=1
build_src_filter =
    +<services/fieldbus/cclink/cclink.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_cclink

; Ethernet POWERLINK basic frame codec (services/fieldbus/powerlink): the EPL cyclic frames
; ([messageType][dest][source][payload]) - SoC/PReq/PRes/SoA - build + parse, over raw L2 (0x88AB).
; Pure host tests.
[env:native_powerlink]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_POWERLINK=1
build_src_filter =
    +<services/fieldbus/powerlink/powerlink.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_powerlink

; SERCOS III motion-bus codec (services/fieldbus/sercos): the MDT/AT telegram (type + phase + cycle
; + data) build + parse and the 16-bit IDN encode/decode (S/P + set + block). Pure host tests.
[env:native_sercos]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SERCOS=1
build_src_filter =
    +<services/fieldbus/sercos/sercos.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_sercos

; PROFIBUS-DP FDL telegram codec (services/fieldbus/profibus): the SD1 (no-data) + SD2 (variable
; data, LE/LEr + arithmetic-sum FCS) telegrams build + validate. Pure host tests with spec-derived
; vectors.
[env:native_profibus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PROFIBUS=1
build_src_filter =
    +<services/fieldbus/profibus/profibus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_profibus

; LonWorks / LON-IP network-variable codec (services/fieldbus/lonworks): the LonTalk NV PDU
; ([msg-code][14-bit selector][value]) build + parse and the SNVT_temp / SNVT_switch scalar encodings.
; Pure host tests.
[env:native_lonworks]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LONWORKS=1
build_src_filter =
    +<services/fieldbus/lonworks/lonworks.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_lonworks

; Modbus Plus HDLC token-bus codec (services/fieldbus/mbplus): the HDLC frame (7E addr ctrl payload
; CRC-16/X-25 7E) build + validate and the token-rotation ring helper. Pure host tests (CRC 0x906E).
[env:native_mbplus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MBPLUS=1
build_src_filter =
    +<services/fieldbus/mbplus/mbplus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_mbplus

; INTERBUS summation-frame codec (services/fieldbus/interbus): the summation frame (loopback +
; per-device 16-bit slices + CRC-16/CCITT FCS) assemble + disassemble. Pure host tests (FCS 0x29B1).
[env:native_interbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_INTERBUS=1
build_src_filter =
    +<services/fieldbus/interbus/interbus.c>
    +<mmgr/protomem.c>
test_filter =
    unit/fieldbus/test_interbus

; ICCP / TASE.2 (IEC 60870-6) Data_Value codec (services/energy/iccp): the StateQ (state + quality)
; and RealQ (scaled INTEGER + quality) indication-point BER structures with optional timestamp. Pure
; host tests with spec-derived vectors.
[env:native_iccp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ICCP=1
build_src_filter =
    +<services/energy/iccp/iccp.c>
    +<mmgr/protomem.c>
test_filter =
    unit/energy/test_iccp

; IEEE 1609 WAVE codec (services/transportation/wave): the 1609.3 WSMP header (version + P-encoded PSID +
; length) build + parse, the PSID p-encoding, and the 1609.2 secured-message envelope header. Pure host
; tests (carries J2735).
[env:native_wave]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WAVE=1
build_src_filter =
    +<services/transportation/wave/wave.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_wave

; UTMC common-database codec (services/transportation/utmc): the UTMCRequest (object id) and UTMCResponse
; (value + quality + timestamp) HTTP/XML documents build + the request-id parse, escaped. Pure host tests.
[env:native_utmc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_UTMC=1
build_src_filter =
    +<services/transportation/utmc/utmc.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_utmc

; OCIT-Outstations message codec (services/transportation/ocit): the object message
; ([msg-type][object-type][instance][data-type][value]) build + parse and the typed-value accessors.
; Pure host tests.
[env:native_ocit]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_OCIT=1
build_src_filter =
    +<services/transportation/ocit/ocit.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transportation/test_ocit

; ATC field-I/O interop snapshot (services/machine_tool/atc): serialize this device's field-I/O map
; as {"inputs":[...],"outputs":[...]} JSON for an ATC engine over HTTP, plus the output setter and
; value getter. Pure host tests.
[env:native_atc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_ATC=1
build_src_filter =
    +<services/machine_tool/atc/atc.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/machine_tool/test_atc

; Southbound protocol-driver framework (services/net/southbound): the bounded driver registry
; (register / find / clear / count) and the name-dispatched read/write/read_block/write_block facade,
; including capability + not-found handling and transport-error passthrough. Pure host tests.
[env:native_southbound]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SOUTHBOUND=1
build_src_filter =
    +<services/net/southbound/southbound.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/net/test_southbound

; Modbus-master southbound driver adapter (services/net/southbound/sb_modbus): binds the
; transport-agnostic Modbus TCP master codec into the southbound driver framework, so an app
; reads/writes register points by driver name through the one facade. Host-tested end to end
; against the real slave codec (pc_modbus_process_adu) over a mock transaction seam.
[env:native_sb_modbus]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SOUTHBOUND=1
    -DPC_ENABLE_MODBUS=1
    -DPC_ENABLE_MODBUS_MASTER=1
build_src_filter =
    +<services/net/southbound/southbound.c>
    +<services/net/southbound/sb_modbus.c>
    +<services/fieldbus/modbus/modbus.c>
    +<services/fieldbus/modbus/modbus_master.c>
    +<mmgr/protomem.c>
test_filter =
    integration/net/test_sb_modbus

; ESP32 panic / exception decoder (server/exc_decoder): parse a captured Guru Meditation
; dump (cause, register PC + EXCVADDR, backtrace PC:SP frames) into a structured ExcInfo and serialize it
; as JSON for a live /exception panel; tolerates missing fields + a trailing corruption marker. Pure host tests.
[env:native_exc_decoder]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_EXC_DECODER=1
build_src_filter =
    +<server/exc_decoder.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_exc_decoder

; HTTP delivery optimizations (services/file_transfer/http_delivery): the RFC 5861 stale-while-revalidate
; freshness decision + its Cache-Control builder, and the versioned service-worker precache manifest
; (including that a full PC_DELIVERY_PRECACHE_MAX manifest fits PC_DELIVERY_MANIFEST_BUF and that an
; oversized one fails closed rather than truncating). Byte-range serving is network_drivers/application/http_range.h's job,
; not this service's. Pure host tests.
[env:native_http_delivery]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP_DELIVERY=1
build_src_filter =
    +<services/file_transfer/http_delivery/http_delivery.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_http_delivery

; Hardware-health diagnostics (server/signaling/hw_health): power-rail voltage-drop logger (worst
; droop + sag/brownout counts), SPI-bus CRC audit with hysteretic clock backoff, GPIO short-circuit test
; (driven vs readback), and capacitor-leakage diag (measured vs expected RC decay). Pure host tests.
[env:native_hw_health]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HW_HEALTH=1
build_src_filter =
    +<server/signaling/hw_health.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_hw_health

; Adaptive mDNS beacon scheduling (network_drivers/application/mdns_adaptive): RF-contention backoff/recovery
; of the announce interval, the TTL/2 continuous-refresher cadence, the announce-due check, and the
; auto-sleep beacon (announce before a sleep that would lapse the record). Wrap-safe. Pure host tests.
[env:native_mdns_adaptive]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MDNS_ADAPTIVE=1
build_src_filter =
    +<network_drivers/application/mdns_adaptive/mdns_adaptive.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/application/test_mdns_adaptive

; Dynamic socket recycling (services/net/sockpool): a fixed LRU connection-slot pool - acquire
; (free slot, else recycle the least-recently-used and report the evicted id), touch, release, find, and
; in-use count. Pure host tests.
[env:native_sockpool]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_SOCKPOOL=1
build_src_filter =
    +<services/net/sockpool/sockpool.c>
    +<mmgr/protomem.c>
test_filter =
    unit/transport/test_sockpool

; Buffer placement policy + DMA ping-pong (services/storage/psram_pool): pc_psram_place picks
; DRAM vs PSRAM by size / DMA requirement / free-heap headroom (large-cold to PSRAM, small-hot + DMA to
; DRAM, leaving a DRAM reserve), plus the SPI DMA ping-pong double-buffer index bookkeeping. Pure host tests.
[env:native_psram_pool]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_PSRAM_POOL=1
build_src_filter =
    +<services/storage/psram_pool/psram_pool.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_psram_pool

; Dual-stack Happy Eyeballs selection (services/net/happy_eyeballs): RFC 6724 destination
; preference scoring, the candidate-list sort + RFC 8305 address-family interleave, and the Connection
; Attempt Delay gate, over the shipped pc_ip. Pure host tests.
[env:native_happy_eyeballs]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HAPPY_EYEBALLS=1
build_src_filter =
    +<services/net/happy_eyeballs/happy_eyeballs.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/net/test_happy_eyeballs

; 802.11 sniffer / traffic analyzer (services/radio/wifi_sniffer): decode an 802.11 MAC header
; (frame-control type/subtype + flags, ToDS/FromDS-dependent addresses), tally frames by type, the
; RSSI-hysteresis channel-agility roaming decision, the channel-hop dwell schedule (hop/wrap/sweep
; counting, clamping, millis-rollover safety), and the per-channel RSSI survey (best RSSI + BSSID per
; channel, out-of-range rejection, strongest-other-channel candidate feeding the roam decision).
; Pure host tests.
[env:native_wifi_sniffer]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WIFI_SNIFFER=1
build_src_filter =
    +<services/radio/wifi_sniffer/wifi_sniffer.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_wifi_sniffer

; Multi-interface egress selection (server/signaling/link_manager): a table of interfaces (kind +
; priority + up/down) with deterministic best-link-up selection, graceful escalation to a higher-priority
; interface, failover when it drops, and real-transition change detection. Pure host tests.
[env:native_link_manager]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LINK_MANAGER=1
build_src_filter =
    +<server/signaling/link_manager.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_link_manager

; CC1101 sub-GHz radio driver (services/radio/cc1101): the TI SPI header protocol (config
; registers, command strobes, status registers, TX/RX FIFO) - init/detect, variable-length send, TX-done,
; set-rx, packet recv with appended RSSI/LQI, and RSSI-to-dBm decode, against a mock chip. Host tests.
[env:native_cc1101]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CC1101=1
build_src_filter =
    +<services/radio/cc1101/cc1101.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/radio/test_cc1101

; FDC2114/2214 capacitance-to-digital field sensor (services/peripherals/fdc2214): the 28-bit data
; combine + error flags, the frequency scale (data/2^28 * fref), and the single-channel config-sequence
; builder. Pure codec host tests (the I2C binding is ESP32-only).
[env:native_fdc2214]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_FDC2214=1
build_src_filter =
    +<services/peripherals/fdc2214/fdc2214.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_fdc2214

; LDC1614 inductance-to-digital field sensor (services/peripherals/ldc1614): the 28-bit data combine +
; error flags, the frequency scale (data/2^28 * fref), and the single-channel config-sequence builder.
; Pure codec host tests (the I2C binding is ESP32-only).
[env:native_ldc1614]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LDC1614=1
build_src_filter =
    +<services/peripherals/ldc1614/ldc1614.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_ldc1614

; VL53L0X time-of-flight ranging codec (services/peripherals/vl53l0x): the range byte-pair combine to
; millimeters, the interrupt-status data-ready decode, and the device range-status validity check. Pure
; codec host tests (the I2C ranging binding is ESP32-only).
[env:native_vl53l0x]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_VL53L0X=1
build_src_filter =
    +<services/peripherals/vl53l0x/vl53l0x.c>
    +<mmgr/protomem.c>
test_filter =
    unit/peripherals/test_vl53l0x

; Receive-only radio channel sniffer -> pcap (services/radio/radio_sniff): the int->float32 RSSI
; encode, the pcap global header (DLT 802.15.4 TAP), and the per-frame TAP record (RSSI + channel TLVs +
; MAC frame) so a sniffed radio channel opens in Wireshark. Pure host tests.
[env:native_radio_sniff]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_RADIO_SNIFF=1
build_src_filter =
    +<services/radio/radio_sniff/radio_sniff.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_radio_sniff

; Bluetooth ATT codec + GATT bridge (services/radio/ble_gatt): build/parse the common ATT PDUs
; (read/write/notify/error, LE handles) and serialize a GATT characteristic table as JSON for the web
; stack. Pure host tests.
[env:native_ble_gatt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_BLE_GATT=1
build_src_filter =
    +<services/radio/ble_gatt/ble_gatt.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_ble_gatt

; TLS version negotiation + pinned cipher policy (services/security/tls_policy): the server-style
; version pick (highest supported not above the client's), the version name, cipher selection by server
; preference from the offered set, and the AEAD-suite classifier. Pure host tests.
[env:native_tls_policy]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TLS_POLICY=1
build_src_filter =
    +<services/security/tls_policy/tls_policy.c>
    +<mmgr/protomem.c>
test_filter =
    unit/tls/test_tls_policy

; Wi-SUN FAN border-router connector (services/radio/wisun): the CoAP client request builder (RFC
; 7252 header + Uri-Path options with extended-length + payload) and the FAN node registry (register / find
; / joined-count / JSON) over pc_ip. Pure host tests.
[env:native_wisun]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_WISUN=1
build_src_filter =
    +<services/radio/wisun/wisun.c>
    +<shared_primitives/ip.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/radio/test_wisun

; Rotating log ring + severity trap (server/logbuf): pure, fully host-tested.
[env:native_logbuf]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LOGBUF=1
build_src_filter =
    +<server/logbuf.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_logbuf

; SoC power governor (server/power_mgmt): the pure clock decision from load, die
; temperature and reset reason - load-based scaling, the thermal hysteresis that stops a part parked at
; the limit from oscillating (asserted by feeding the plan's own output back for 20 ticks and counting
; one transition), post-brownout recovery holding the floor even when busy and cool, the precedence
; between the three, and INT16_MIN meaning no sensor rather than ice-cold.
[env:native_power_mgmt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_POWER_MGMT=1
build_src_filter =
    +<server/power_mgmt.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_power_mgmt

; Removable-storage hot-swap safeties (services/storage/hotswap): the ABSENT/READY/FAULTED state
; machine - a run of consecutive I/O errors faults a volume while a single one does not, any success
; resets the run, the fail counter saturates instead of wrapping, probe attempts are rate-limited (and
; wrap-safe across a millis() rollover), present-but-unmountable stays absent, and a full
; removal/reinsertion cycle returns to a clean mount. Pure host tests with a synthetic clock.
[env:native_hotswap]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HOTSWAP=1
build_src_filter =
    +<services/storage/hotswap/hotswap.c>
    +<server/clock/clock.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_hotswap

; Abstract logging macros (shared_primitives/log.h) whose disabled levels are discarded by
; the preprocessor: built at PC_LOG_LEVEL_INFO so DEBUG is below the floor. Asserts that a discarded
; call reaches neither the sink nor the ring and never even evaluates its arguments, that INFO/WARN/ERROR
; emit with the right level, that the PC_LOG_LEVEL_* values agree with pc_log_level's, and the formatting
; edges (truncation, null format, empty message).
[env:native_log]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_LOGBUF=1
    -DPC_LOG_LEVEL=PC_LOG_LEVEL_INFO
build_src_filter =
    +<server/logbuf.c>
    +<shared_primitives/log.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/server/test_log

; Schema-driven config export/restore (services/storage/config_io) over the config store;
; round-trip host-tested against the in-memory backend.
[env:native_config_io]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_CONFIG_STORE=1
    -DPC_ENABLE_CONFIG_IO=1
build_src_filter =
    +<services/storage/config_store/config_store.c>
    +<services/storage/config_io/config_io.c>
    +<../core_setup/hal/host/host_nvs.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/storage/test_config_io

; Core-partitioning invariant at N=2 (PC_WORKER_COUNT=2): each worker reaps
; only its owned slots (check_timeouts ownership). The host queue is keyed by handle, so
; per-worker routing is checkable here rather than only on hardware.
[env:native_workers]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_WORKER_COUNT=2
build_src_filter =
    +<shared_primitives/ip.c>
    +<network_drivers/transport/tcp.c>
    +<network_drivers/transport/tcp/tcp_client.c>
    +<network_drivers/transport/tcp/tcp_conn.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/tcp/tcp_listener.c>
    +<network_drivers/session/worker.c>
    +<mmgr/arena.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_workers

; CodeQL coverage env: the full app compiled with every new feature flag ON so
; CodeQL traces the integration paths (CSRF / lockout / allowlist gates,
; permessage-deflate) AND the new service modules, which the per-feature envs scan
; only in isolation. Built (--without-testing) by the CodeQL workflow; one test
; in the filter is enough to pull in the whole src set.
[env:native_codeql]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_CSRF=1
    -DPC_ENABLE_AUTH=1
    -DPC_ENABLE_AUTH_LOCKOUT=1
    -DPC_ENABLE_IP_ALLOWLIST=1
    -DPC_ENABLE_WS_DEFLATE=1
    -DPC_ENABLE_TIME_SOURCE=1
    -DPC_ENABLE_CONFIG_STORE=1
    -DPC_ENABLE_DEVICE_ID=1
    -DPC_ENABLE_TELEMETRY=1
    -DPC_ENABLE_DASHBOARD=1
    -DPC_ENABLE_PARTITION_MONITOR=1
    -DPC_ENABLE_CBOR=1
    -DPC_ENABLE_MSGPACK=1
    -DPC_ENABLE_GPIO_MAP=1
    -DPC_ENABLE_UDP_TELEMETRY=1
    -DPC_ENABLE_GUARDRAILS=1
    -DPC_ENABLE_FAILSAFE=1
    -DPC_ENABLE_SLEEP_SCHED=1
    -DPC_ENABLE_WEARLEVEL=1
    -DPC_ENABLE_NETADAPT=1
    -DPC_ENABLE_DSHOT=1
    -DPC_ENABLE_HART=1
    -DPC_ENABLE_NTS=1
    -DPC_ENABLE_DDS=1
    -DPC_ENABLE_XMPP=1
    -DPC_ENABLE_RAWL2=1
    -DPC_ENABLE_SPA_ROUTER=1
    -DPC_ENABLE_GOOSE=1
    -DPC_ENABLE_MTCONNECT=1
    -DPC_ENABLE_J2735=1
    -DPC_ENABLE_NEMA_TS2=1
    -DPC_ENABLE_SNP=1
    -DPC_ENABLE_DIRECTNET=1
    -DPC_ENABLE_SEP2=1
    -DPC_ENABLE_PROFINET=1
    -DPC_ENABLE_NTCIP=1
    -DPC_ENABLE_OPENADR=1
    -DPC_ENABLE_MMS=1
    -DPC_ENABLE_CCLINK=1
    -DPC_ENABLE_POWERLINK=1
    -DPC_ENABLE_SERCOS=1
    -DPC_ENABLE_PROFIBUS=1
    -DPC_ENABLE_LONWORKS=1
    -DPC_ENABLE_MBPLUS=1
    -DPC_ENABLE_INTERBUS=1
    -DPC_ENABLE_ICCP=1
    -DPC_ENABLE_WAVE=1
    -DPC_ENABLE_UTMC=1
    -DPC_ENABLE_OCIT=1
    -DPC_ENABLE_ATC=1
    -DPC_ENABLE_SOUTHBOUND=1
    -DPC_ENABLE_EXC_DECODER=1
    -DPC_ENABLE_HTTP_DELIVERY=1
    -DPC_ENABLE_HW_HEALTH=1
    -DPC_ENABLE_MDNS_ADAPTIVE=1
    -DPC_ENABLE_SOCKPOOL=1
    -DPC_ENABLE_PSRAM_POOL=1
    -DPC_ENABLE_HAPPY_EYEBALLS=1
    -DPC_ENABLE_WIFI_SNIFFER=1
    -DPC_ENABLE_LINK_MANAGER=1
    -DPC_ENABLE_CC1101=1
    -DPC_ENABLE_FDC2214=1
    -DPC_ENABLE_LDC1614=1
    -DPC_ENABLE_VL53L0X=1
    -DPC_ENABLE_RADIO_SNIFF=1
    -DPC_ENABLE_BLE_GATT=1
    -DPC_ENABLE_TLS_POLICY=1
    -DPC_ENABLE_WISUN=1
    -DPC_ENABLE_LOGBUF=1
    -DPC_ENABLE_OTA_ROLLBACK=1
    -DPC_ENABLE_TOTP=1
    -DPC_ENABLE_WEBHOOK=1
    -DPC_ENABLE_RADIO_POWER=1
    -DPC_ENABLE_AUDIT_LOG=1
    -DPC_ENABLE_OIDC=1
    -DPC_ENABLE_MNT=1
    -DPC_ENABLE_GRAPHQL=1
    -DPC_ENABLE_ESPNOW=1
    -DPC_ENABLE_OAUTH2=1
    -DPC_ENABLE_OPCUA=1
    -DPC_ENABLE_OPCUA_CLIENT=1
    -DPC_ENABLE_WEBSOCKET=1
    -DPC_ENABLE_SSE=1
build_src_filter =
    +<shared_primitives/ip.c>
    ${native_stack_http.build_src_filter}
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<services/security/audit_log/audit_log.c>
    +<services/security/oidc/oidc.c>
    +<server/filesystem/mnt.c>
    +<services/iot/graphql/graphql.c>
    +<services/radio/espnow/espnow.c>
    +<services/security/oauth2/oauth2.c>
    +<services/fieldbus/opcua/opcua.c>
    +<services/fieldbus/opcua_client/opcua_client.c>
    +<crypto/asymmetric/rsa.c>
    +<network_drivers/tls/ssh_rsa.c>
    +<crypto/asymmetric/bignum.c>
    +<../core_setup/hal/portable/portable_bignum.c>
    +<crypto/hash/sha512.c>
    +<services/security/csrf/csrf.c>
    +<crypto/rng/rng.c>
    +<crypto/cipher/chacha20.c>
    +<services/security/auth_lockout/auth_lockout.c>
    +<services/timing_position/time_source/time_source.c>
    +<services/storage/config_store/config_store.c>
    +<../core_setup/hal/host/host_nvs.c>
    +<server/signaling/device_id.c>
    +<network_drivers/presentation/codec/inflate/inflate.c>
    +<network_drivers/presentation/codec/deflate/deflate.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<services/iot/telemetry/telemetry.c>
    +<services/web/dashboard/dashboard.c>
    +<services/web/dashboard/dashboard_routes.c>
    +<services/storage/partition_monitor/partition_monitor.c>
    +<services/storage/partition_monitor/partition_monitor_routes.c>
    +<network_drivers/presentation/codec/cbor/cbor.c>
    +<network_drivers/presentation/codec/msgpack/msgpack.c>
    +<server/signaling/gpio_map.c>
    +<server/signaling/gpio_map_routes.c>
    +<services/iot/udp_telemetry/udp_telemetry.c>
    +<services/security/guardrails/guardrails.c>
    +<server/failsafe.c>
    +<server/sleep_sched.c>
    +<server/filesystem/wearlevel.c>
    +<services/net/netadapt/netadapt.c>
    +<services/peripherals/dshot/dshot.c>
    +<services/fieldbus/hart/hart.c>
    +<network_drivers/application/nts/nts.c>
    +<services/iot/dds/dds.c>
    +<services/iot/xmpp/xmpp.c>
    +<services/fieldbus/rawl2/rawl2.c>
    +<services/web/spa_router/spa_router.c>
    +<services/energy/goose/goose.c>
    +<services/machine_tool/mtconnect/mtconnect.c>
    +<services/transportation/j2735/j2735.c>
    +<services/transportation/nema_ts2/nema_ts2.c>
    +<services/fieldbus/snp/snp.c>
    +<services/fieldbus/directnet/directnet.c>
    +<services/energy/sep2/sep2.c>
    +<services/fieldbus/profinet/profinet.c>
    +<services/transportation/ntcip/ntcip.c>
    +<services/energy/openadr/openadr.c>
    +<services/energy/mms/mms.c>
    +<services/fieldbus/cclink/cclink.c>
    +<services/fieldbus/powerlink/powerlink.c>
    +<services/fieldbus/sercos/sercos.c>
    +<services/fieldbus/profibus/profibus.c>
    +<services/fieldbus/lonworks/lonworks.c>
    +<services/fieldbus/mbplus/mbplus.c>
    +<services/fieldbus/interbus/interbus.c>
    +<services/energy/iccp/iccp.c>
    +<services/transportation/wave/wave.c>
    +<services/transportation/utmc/utmc.c>
    +<services/transportation/ocit/ocit.c>
    +<services/machine_tool/atc/atc.c>
    +<services/net/southbound/southbound.c>
    +<server/exc_decoder.c>
    +<services/file_transfer/http_delivery/http_delivery.c>
    +<server/signaling/hw_health.c>
    +<network_drivers/application/mdns_adaptive/mdns_adaptive.c>
    +<services/net/sockpool/sockpool.c>
    +<services/storage/psram_pool/psram_pool.c>
    +<services/net/happy_eyeballs/happy_eyeballs.c>
    +<services/radio/wifi_sniffer/wifi_sniffer.c>
    +<server/signaling/link_manager.c>
    +<services/radio/cc1101/cc1101.c>
    +<services/peripherals/fdc2214/fdc2214.c>
    +<services/peripherals/ldc1614/ldc1614.c>
    +<services/peripherals/vl53l0x/vl53l0x.c>
    +<services/radio/radio_sniff/radio_sniff.c>
    +<services/radio/ble_gatt/ble_gatt.c>
    +<services/security/tls_policy/tls_policy.c>
    +<services/radio/wisun/wisun.c>
    +<server/logbuf.c>
    +<server/update/ota_rollback.c>
    +<services/security/totp/totp.c>
    +<services/net/webhook/webhook.c>
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    integration/fieldbus/test_dispatch
extra_scripts =
    pre:test/ensure_test_keys.py

; Pluggable monotonic clock (server/clock): default millis(), custom clock
; divided down to the internal 1000 Hz, plus the microsecond base and latency
; budgeting. Builds server/clock/clock.c, which owns the installed-clock state.
[env:native_clock]
extends = native_base
build_src_filter =
    -<*>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/pc_clock/test_clock

; Concurrency proof for the cross-thread slot fields (pc_atomic state / rx_head /
; rx_tail). Two threads drive one slot's SPSC ring with the production access
; pattern; asserts byte-exact in-order delivery. Portable (-pthread), runs on any
; host including Windows - this verifies the data integrity / logic.
[env:native_concurrency]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -O1
    -pthread
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_concurrency
test_build_src = no

; Same harness under ThreadSanitizer: proves ZERO data races on the slot fields
; (the pc_atomic acquire/release happens-before lets the plain rx_buffer[] writes
; be read on the other core safely). TSan ships with clang/gcc on Linux, so this
; env is run by the test CI (ubuntu); it is not buildable on the Windows host.
[env:native_tsan]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -g
    -O1
    -fsanitize=thread
    -pthread
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    integration/transport/test_concurrency
test_build_src = no

; QPACK field-section compression for HTTP/3 (network_drivers/presentation/http/http3/qpack,
; RFC 9204): the Appendix B.1 worked example (literal field line with a static name reference), the
; encoder's exact bytes for an indexed / name-reference / literal-name field line, a round-trip
; through all three representations, and rejection of any dynamic-table reference. Pure host codec.
[env:native_qpack]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_qpack

; QUIC packet header + packet-number codec (network_drivers/presentation/http/http3/quic_packet,
; RFC 9000 sec 17): the long-header build/parse round-trip, a Version Negotiation packet (Version 0
; + supported-version list), the short (1-RTT) header parse, and packet-number truncation coding
; against the Appendix A.2 (encode) and A.3 (decode) worked examples. Pure host codec.
[env:native_quic_packet]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_packet

; QUIC frame codec (network_drivers/presentation/http/http3/quic_frame, RFC 9000 sec 19):
; builder/parser round-trips for PADDING/PING/HANDSHAKE_DONE, ACK (single-range + a hand-built
; multi-range-with-ECN cursor advance), CRYPTO, STREAM (with/without Offset, LEN-absent to packet
; end), MAX_DATA, CONNECTION_CLOSE (transport + app), a multi-frame sequential parse, truncation.
[env:native_quic_frame]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_frame

; QUIC Initial packet crypto (crypto/hkdf + quic_aead +
; quic_crypto, RFC 9001): HKDF-Expand-Label key derivation, AEAD_AES_128_GCM (software AES-128 +
; GHASH) and header protection. Checked against FIPS 197 (AES block), GCM Test Case 4, and RFC 9001
; Appendix A - A.1 secrets, A.3 server Initial byte-exact, A.2 client Initial + sample, A.4 Retry tag.
[env:native_quic_crypto]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_crypto

; TLS 1.3 stream record layer (network_drivers/tls/tls_record, RFC 8446 sec 5), the software arm
; selected when the vendor ships no TLS stack (PC_TLS_SOFTWARE): the 5-byte TLSPlaintext header and
; its length-validating parse, TLSCiphertext protect/unprotect with the real content type sealed
; inside the record, the iv XOR seq nonce, the backward scan that recovers the type past the padding,
; and the record counter that never travels and does not advance on a forged record. The key + record
; KAT is byte-exact against an independent reconstruction (tools/crypto/gen_tls_record_kat.py):
; stdlib HKDF-Expand-Label under the "tls13 " prefix plus OpenSSL AES-128-GCM. Pure host tests.
[env:native_tls_record]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_TLS=1
build_src_filter =
    +<network_drivers/tls/tls_record.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<mmgr/protomem.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
test_filter =
    unit/tls/test_tls_record

; DTLS 1.3 record layer (network_drivers/presentation/security/dtls/dtls_record, RFC 9147 sec 4):
; DTLSPlaintext + DTLSCiphertext protect/unprotect, the unified header, sequence-number encryption
; (sec 4.2.3) and reconstruction (sec 4.2.2), the AEAD nonce, and the anti-replay window (sec 4.5.1).
; The key + record KAT is byte-exact against an independent HKDF-Expand-Label + AEAD_AES_128_GCM +
; AES-ECB reconstruction (RFC 5869 / RFC 8446 / SP 800-38D). Pure host tests.
[env:native_dtls]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
build_src_filter =
    +<network_drivers/presentation/security/dtls/dtls_record.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/tls/test_dtls_record

; DTLS 1.3 handshake framing + reliability (network_drivers/presentation/security/dtls/dtls_handshake,
; RFC 9147 sec 5 + 7): the 12-byte DTLS handshake header, overlap-tolerant message reassembly, the
; ACK message (content type 26), and the stateless HelloRetryRequest cookie (sec 5.1). The cookie
; wire format is byte-exact against an independent Python-stdlib HMAC-SHA256, proving the field
; layout and the client-address binding. Pure host tests.
[env:native_dtls_hs]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
build_src_filter =
    +<network_drivers/presentation/security/dtls/dtls_handshake.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/tls/test_dtls_handshake

; TLS 1.3 messages the DTLS 1.3 handshake adds to tls13_msg (RFC 8446 sec 4.1.4 / 4.4.1),
; compiled for the DTLS path (PC_ENABLE_DTLS, not HTTP/3): the HelloRetryRequest builder, the
; cookie extension parse, the empty EncryptedExtensions, and the message_hash transcript wrapper.
; The HelloRetryRequest is byte-exact, anchored on the RFC 8446 sec 4.1.3 magic random. Also the RFC
; 7250 Raw Public Keys codec (PC_ENABLE_TLS_RPK): the Ed25519 SubjectPublicKeyInfo encoder, the
; RawPublicKey Certificate message, the server_certificate_type EncryptedExtensions, and the
; server_certificate_type ClientHello parse. Pure host tests.
[env:native_dtls_tls13]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
    -DPC_ENABLE_TLS_RPK=1
build_src_filter =
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/protomem.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
test_filter =
    unit/tls/test_dtls_tls13

; DTLS 1.3 server handshake state machine (network_drivers/presentation/security/dtls/dtls_conn,
; RFC 9147 sec 5-6): the one-round-trip full handshake (TLS_AES_128_GCM_SHA256 / X25519 / Ed25519)
; over the DTLS record + framing layers, reusing the TLS 1.3 messages + key schedule. A minimal
; in-test DTLS client completes the handshake end to end - deprotecting the server flight, verifying
; the CertificateVerify signature and Finished MAC over the real transcript, and confirming both
; sides install identical application-traffic keys. Also drives the HelloRetryRequest group
; renegotiation (sec 5.1): a ClientHello with no X25519 key_share draws an HRR + address-bound cookie,
; and the retry that echoes it completes over the message_hash || HRR || ClientHello2 transcript; a
; retry without the cookie is rejected. An RFC 7250 Raw Public Keys case (PC_ENABLE_TLS_RPK) offers
; server_certificate_type = RawPublicKey and confirms the server answers with a 44-byte Ed25519
; SubjectPublicKeyInfo whose key CertificateVerify signs. Pure host tests.
[env:native_dtls_conn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
    -DPC_ENABLE_TLS_RPK=1
build_src_filter =
    +<network_drivers/presentation/security/dtls/dtls_conn.c>
    +<network_drivers/presentation/security/dtls/dtls_record.c>
    +<network_drivers/presentation/security/dtls/dtls_handshake.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/tls/test_dtls_conn

; CoAP over DTLS (services/iot/coap/coaps, RFC 7252 sec 9): the bridge that drives a DtlsConn
; handshake and, once established, unwraps each epoch-3 application record, answers it with
; coap_server_process(), and re-wraps the response. An in-test DTLS 1.3 client completes the handshake
; and exchanges a CoAP GET, decrypting the CoAP response from the returned DTLS record; a replayed
; application record is dropped. Pure host tests.
[env:native_coaps]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
    -DPC_ENABLE_COAP=1
build_src_filter =
    +<services/iot/coap/coaps.c>
    +<services/iot/coap/coap.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/security/dtls/dtls_conn.c>
    +<network_drivers/presentation/security/dtls/dtls_record.c>
    +<network_drivers/presentation/security/dtls/dtls_handshake.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/iot/test_coaps

; CoAP-over-DTLS server front-end (services/iot/coap/coaps_server): the per-peer DtlsConn pool +
; ingest/poll seam on top of pc_coaps_process(). An in-test DTLS 1.3 client drives the server only through
; coaps_server_ingest() and an output sink, exercising peer-address routing, the handshake + CoAP
; exchange, the poll-driven retransmission timer (RFC 9147 sec 5.8), and idle-connection reaping. Two
; simultaneous peers are routed to their own connections. Pure host tests.
[env:native_coaps_server]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_DTLS=1
    -DPC_ENABLE_COAP=1
build_src_filter =
    +<services/iot/coap/coaps_server.c>
    +<services/iot/coap/coaps.c>
    +<services/iot/coap/coap.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/security/dtls/dtls_conn.c>
    +<network_drivers/presentation/security/dtls/dtls_record.c>
    +<network_drivers/presentation/security/dtls/dtls_handshake.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/iot/test_coaps_server

; TLS 1.3 key schedule for the QUIC handshake (network_drivers/tls/tls13_kdf,
; RFC 8446 sec 7.1 / 4.4.4): Early/Handshake/Master secret Extract chain, client/server handshake +
; application traffic secrets, the server handshake write key/iv label path, and the server Finished
; verify_data over a full ClientHello..CertificateVerify transcript. Pinned to RFC 8448 sec 3.
[env:native_tls13_kdf]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/tls/tls13_kdf.c>
    +<crypto/kdf/hkdf.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/tls/test_tls13_kdf

; QUIC transport parameters codec (network_drivers/presentation/http/http3/quic_tp, RFC 9000
; sec 18): the sec 18.2 defaults, an encode/parse round-trip over the connection IDs + every varint
; parameter + the migration flag, parsing a hand-built byte string, skipping unknown/GREASE ids, and
; rejecting a duplicate parameter, an oversized connection ID, and out-of-range values.
[env:native_quic_tp]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_tp

; TLS 1.3 handshake messages for the QUIC handshake (network_drivers/presentation/http/http3/
; tls13_msg, RFC 8446 sec 4): ClientHello parse (X25519 key_share + capability flags), and the server
; flight. Byte-exact vs RFC 8448 sec 3 for ServerHello / Certificate / Finished, plus the ALPN + QUIC
; transport-params EncryptedExtensions and an Ed25519 CertificateVerify sign/verify round-trip.
[env:native_tls13_msg]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/protomem.c>
    +<mmgr/secure.c>
    +<mmgr/arena.c>
test_filter =
    unit/tls/test_tls13_msg

; TLS 1.3 server handshake state machine for QUIC (network_drivers/presentation/http/http3/
; quic_tls, RFC 9001 / RFC 8446): a full interop round-trip - drive the server with a hand-built
; ClientHello, run the client half of the key schedule independently, and confirm both sides derive
; identical handshake + application secrets and the server accepts the client Finished. Plus
; capability-negotiation rejections (no h3 ALPN, bad Finished) and partial-CRYPTO handling.
[env:native_quic_tls]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_tls

; TLS 1.3 QUIC handshake with the X25519MLKEM768 post-quantum hybrid group (IANA 0x11ec,
; PC_ENABLE_PQC_KEX=1): drives the server with a hybrid ClientHello, then verifies it as a conforming
; client - ML-KEM Decaps (an independent reference) + X25519, the 64-byte ML-KEM||X25519 secret, and
; both Finished MACs. Reuses the classical test_quic_tls suite plus the added hybrid round-trip.
[env:native_quic_tls_pqc]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
    -DPC_ENABLE_PQC_KEX=1
    -DPC_WORKER_TASK_STACK=16384
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<crypto/hash/sha3.c>
    +<crypto/pqc/mlkem.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/http/test_quic_tls

; QUIC v1 server connection engine (network_drivers/presentation/http/http3/quic_conn, RFC 9000
; / RFC 9001): the test acts as a QUIC client - builds real Initial / Handshake / 1-RTT packets and
; drives a server QuicConn through a full handshake (per-level AEAD, CRYPTO reassembly, ACKs,
; coalescing, HANDSHAKE_DONE) and a 1-RTT request/response stream exchange.
[env:native_quic_conn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_conn.c>
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/http/test_quic_conn

; HTTP/3 application engine (network_drivers/presentation/http/http3/h3_conn, RFC 9114): drives
; h3_conn through the quic_conn callback seam - a QPACK-encoded request on a request stream dispatches
; the right method/path (+ a POST body), the control stream sends SETTINGS on handshake completion,
; and a response (HEADERS + DATA) round-trips through the QUIC stream send buffer.
[env:native_h3_conn]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/h3_conn.c>
    +<network_drivers/presentation/http/http3/h3_frame.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http3/quic_conn.c>
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/http/test_h3_conn

; End-to-end HTTP/3 capstone (network_drivers/presentation/http/http3): a QUIC client in the
; test completes the TLS 1.3 handshake against a quic_conn + h3_conn server, sends a real HTTP/3 GET
; (QPACK HEADERS in a STREAM frame), and verifies the app-served response (HEADERS :status 200 + DATA
; body) - the whole stack composed: QUIC crypto/framing, TLS 1.3, streams, HTTP/3 framing, QPACK.
[env:native_h3_e2e]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/h3_conn.c>
    +<network_drivers/presentation/http/http3/h3_frame.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http3/quic_conn.c>
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_h3_e2e

; HTTP/3 server glue (network_drivers/presentation/http/http3/quic_server): the UDP-facing pool
; that routes datagrams by Destination Connection ID to a pool of QuicConn + H3Conn engines. A QUIC
; client in the test hands datagrams to the pcb the server bound and reads its replies back out of the
; host stack's send log, driving a full handshake + HTTP/3 GET and verifying the 200 + body - the same
; end-to-end flow as test_h3_e2e but through pc_quic_server_begin / poll / respond over the real UDP
; listener, exercising the connection-ID routing, the ingest ring, and new-connection creation from a
; client Initial. The HTTP/3 dispatch bridge (h3_server.c) belongs to native_h3_server: it reaches the
; route table and the TCP slot pools, which this env does not build.
[env:native_quic_server]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    +<network_drivers/presentation/http/http3/quic_server.c>
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/transport/net_addr.c>
    +<shared_primitives/ip.c>
    +<network_drivers/presentation/http/http3/h3_conn.c>
    +<network_drivers/presentation/http/http3/h3_frame.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<network_drivers/presentation/http/http3/quic_conn.c>
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<crypto/hash/sha256.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<server/clock/clock.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_quic_server

; HTTP/3 dispatch bridge end-to-end through PC (the full Layer-7 app built with
; PC_ENABLE_HTTP3=1): a QUIC client completes the handshake and sends an HTTP/3 GET, quic_server
; routes it to the reserved dispatch slot, match_and_execute runs the registered route, and the
; handler's send() is routed back onto the request stream via pc_quic_server_respond. Verifies the whole
; wire: a real route handler answers an HTTP/3 request. Reuses the native_stack_http full-app base.
[env:native_h3_server]
extends = native_stack_http
build_flags =
    ${native_stack_http.build_flags}
    -DPC_ENABLE_HTTP3=1
build_src_filter =
    ${native_stack_http.build_src_filter}
    +<network_drivers/transport/udp.c>
    +<network_drivers/transport/udp/udp_listener.c>
    +<network_drivers/transport/net_addr.c>
    +<network_drivers/transport/udp/udp_client.c>
    +<network_drivers/presentation/http/http3/quic_server.c>
    +<network_drivers/presentation/http/http3/h3_server.c>
    +<network_drivers/presentation/http/http3/quic_conn.c>
    +<network_drivers/presentation/http/http3/quic_tls.c>
    +<network_drivers/tls/tls13_kdf.c>
    +<network_drivers/presentation/http/http3/tls13_msg.c>
    +<network_drivers/presentation/http/http3/quic_tp.c>
    +<network_drivers/presentation/http/http3/quic_crypto.c>
    +<../core_setup/hal/portable/portable_aes128gcm.c>
    +<crypto/kdf/hkdf.c>
    +<network_drivers/presentation/http/http3/quic_packet.c>
    +<network_drivers/presentation/http/http3/quic_frame.c>
    +<network_drivers/presentation/http/http3/quic_varint.c>
    +<network_drivers/presentation/http/http3/h3_conn.c>
    +<network_drivers/presentation/http/http3/h3_frame.c>
    +<network_drivers/presentation/http/http3/qpack.c>
    +<network_drivers/presentation/codec/hpack_prim/hpack_prim.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<crypto/hash/sha512.c>
    +<crypto/asymmetric/curve25519.c>
    +<crypto/asymmetric/ed25519.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    integration/http/test_h3_server

; chacha20-poly1305@openssh.com AEAD (network_drivers/presentation/ssh): ChaCha20 vs RFC
; 8439 sec 2.3.2 block vector, Poly1305 vs RFC 8439 sec 2.5.2, and the OpenSSH construction
; (length decode, encrypt/decrypt round-trip, tag-tamper + wrong-seqnr rejection). Pure host crypto.
[env:native_ssh_chachapoly]
extends = native_base
build_src_filter =
    +<crypto/cipher/chacha20.c>
    +<crypto/mac/poly1305.c>
    +<crypto/aead/chachapoly.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_chachapoly

; AES-256-GCM AEAD for aes256-gcm@openssh.com (RFC 5647) host-tested here: seal/open vs the
; NIST/McGrew AES-256-GCM Test Case 16 vector, tamper rejection, and the invocation-counter
; advance. Software AES-256 + GHASH; no heap, no stdlib.
[env:native_ssh_aesgcm]
extends = native_base
build_src_filter =
    +<crypto/aead/aesgcm.c>
    +<../core_setup/hal/portable/portable_aesgcm.c>
    +<mmgr/arena.c>
    +<mmgr/plaintext.c>
    +<mmgr/secure.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_aesgcm

; ECDSA P-256 for ecdsa-sha2-nistp256 (RFC 5656) host-tested on the software path: pubkey,
; deterministic sign, and verify pinned byte-exact to the RFC 6979 A.2.5 (P-256/SHA-256)
; vectors, plus tamper rejection. Self-contained field/point math + RFC 6979 (HMAC-SHA256).
[env:native_ssh_ecdsa]
extends = native_base
build_src_filter =
    +<crypto/asymmetric/ecdsa.c>
    +<crypto/mac/hmac_sha256.c>
    +<crypto/hash/sha256.c>
    +<mmgr/arena.c>
    +<mmgr/secure.c>
    +<mmgr/plaintext.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/ssh/test_ssh_ecdsa

; The declarative frame builder (mmgr/frame.h + frame.c): the single engine that
; turns a static pc_field spec into wire bytes, so the ~160 formatting sites in this library carry
; a table rather than a hand-written append sequence. Covers every field kind and width, the
; fail-closed contract at the exact byte boundary (a frame that does not fit reports 0 AND leaves a
; valid empty string, which is what a length comparison against snprintf's would-be length silently
; got wrong), the append rewind that keeps a header line whole-or-absent, NULL string handling, and
; a differential check of the numeric and float conversions against the libc printf whose output
; these frames replace byte for byte.
[env:native_frame]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_frame

; Layer 1 driven through a REAL backend: the env declares PC_PHYSICAL_HAS_BACKEND=1,
; so and test/mocks/physical stands in for silicon instead of the
; no-op stubs in physical.c. Nothing else in the tree builds that backend, and without it every L1
; readout answers empty - which means a caller that mishandles a live link reads exactly like one
; that works, and no assertion can tell them apart. Here the link actually comes up: the station
; associates and reports its SSID, channel and a negative dBm RSSI; the softAP takes its own address
; without stealing the route; Ethernet then takes the default route from the station, the way a
; stack picks one when both are up. That is also the only way to separate the same-signature members
; a stub build cannot - eth->init from eth->ready, ip6->init from ip6->ready, and link->mac from
; link->egress_mac, which return DIFFERENT addresses once a wired link carries traffic. Addresses
; are RFC 5737 TEST-NET-1 and RFC 3849 2001:db8::/32, the MACs are checked for the IEEE 802 U/L and
; I/G bits, the SSID cap is IEEE 802.11-2020 9.4.2.2's 32 octets, and a global IPv6 address is
; checked not to be an RFC 4291 sec 2.5.6 link-local. PC_RADIO_WIFI_PS is set to max modem so the
; keep-awake refcount is provable: the first busy_hold forces sleep off and the balancing release
; restores the configured mode, which is indistinguishable from the held state at the default of 0.
; This is the only env that compiles radio_power.c's backend arm at all.
[env:native_phy]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_PHYSICAL_HAS_BACKEND=1
    -DPC_ENABLE_ETHERNET=1
    -DPC_ENABLE_IPV6=1
    -DPC_ENABLE_RADIO_POWER=1
    -DPC_RADIO_WIFI_PS=2
build_src_filter =
    -<*>
    +<network_drivers/physical/physical.c>
    +<network_drivers/physical/radio_power.c>
    +<../test/mocks/physical/physical_mock.c>
    +<shared_primitives/ip.c>
    +<mmgr/protomem.c>
test_filter =
    unit/physical/test_phy

; The bounded-run walks (mmgr/protostr): len, diff, eq, starts, find, has, copy, the
; step rungs and the classifiers. The number parsers this module also carries are exercised by
; native_primitives against the numparse contract and are only checked for binding here. Covers the
; two opposite cap contracts the header draws - len's nul_cap is a WILLINGNESS to keep looking while
; every compare's read_cap is a PROMISE that the bytes exist - the first-difference index at every
; position and every alignment, equality refusing a prefix where starts accepts it (the same walk
; reading the tie the other way), find's first-occurrence rule at every position, its empty needle
; matching at the start, and a haystack NUL ending the search since a needle byte is never NUL.
; Case folding is checked to touch the letters and NOT '_' vs '?' or '[' vs '{', which differ in the
; same bit. copy's bound belongs to the destination and always terminates. find is then diffed
; against a naive search written in the test over the inputs that break a search: periodic and
; self-overlapping haystacks where a failed match cannot skip the needle's length, needles that are
; their own prefix, every substring of the subject taken from every position, a single flipped byte
; at every position of a near-miss, and high-bit bytes that a byte compared as signed would order
; backwards - each at every offset within a word and under both case rules. Every subject is an
; aligned padded region, because that is the only kind of memory the library is ever handed. Plus
; one test that each table member is the walk its name says: the table is initialized positionally.
[env:native_protostr]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_protostr

; The byte-span walks (mmgr/protomem): copy, move, compare, find, fill, zero, one
; register-width load or store per step. cpy and set store a WHOLE word and mask the tail, so the
; lanes between n and the end of the word n stops in go to zero - those lanes are the padding of the
; borrow, which is what lets the store stay a dumb word instead of a byte walk. Every assertion here
; is written against that lane boundary rather than against n, and the destinations are aligned
; because a borrow is. Covers every length through two full words, a source at every offset within a
; word (the funnel that assembles the wanted word from the two aligned words holding it), both
; overlap directions of move, cmp's sign taken from the first differing byte at every position and
; ordered as UNSIGNED, cmp and chr not stopping at a NUL because a span has no terminator, chr's
; bound and its first-match rule, and the fill splatting a high-bit value into every lane. Plus one
; test that each table member is the walk its name says: the table is initialized positionally.
[env:native_protomem]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_protomem

; A double read as the three fields it is (mmgr/float_bits.h): sign at bit 63,
; exponent at 62..52, mantissa at 51..0, by mask and shift, and merged back from those three. The
; oracle is the layout written out in the test, not a second accessor. Covers that the three masks
; partition all 64 bits exactly once, the known encodings (1.0 is the bias with a zero mantissa,
; 3.14159 is exponent 1024, negative zero differs from zero in the sign alone), the non-finite and
; subnormal exponent fields, and the round trip: split into fields and merged back is the value that
; went in, at every one of the 64 bit positions, so no field is a bit short or a bit wide. The merge
; masks each field to its own width, so a field carrying junk above it cannot reach a neighbour.
[env:native_float_bits]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_float_bits

; The bounded no-heap builder (mmgr/membuild.h): bump-append into a caller-owned
; region with ok latching false the first time something would not fit, so the caller tests one flag
; at the end instead of a return per call. Covers that the capacity reserves a byte for the
; terminator, that a refused append writes nothing and every later append is a no-op, that a
; zero-capacity builder does not even write the terminator, and the two clipping appenders that
; deliberately do NOT latch - byte-wise for display text, all-or-nothing for a number, since half a
; number reads as a different number. The renderings are diffed against libc printf, which is what
; they replace: %llu, %llx and %0Nu across the 32-bit fast path and the value just past it, %lld
; including the minimum where negating the value would overflow, %.<sig>g at every precision from 1
; to 10 over both signs of a value set that includes the rounding ties, the decade carries, the
; subnormals and the exponent extremes, and %.<n>f over the range that form occupies. Plus the sign
; bit read for negative zero, the infinity and NaN words, and the XML and JSON escapes.
[env:native_membuild]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_membuild

; The fixed-width serializers (mmgr/endian.h): a width moved between an integer and
; the bytes at a pointer, both orders, 16/32/64. Each writer is diffed against a shift loop written
; in the test rather than against its opposite number, so a pair that is wrong the same way both
; ways cannot agree its way to a pass. Covers the returned width a caller advances by, the reader
; inverting the writer, a big-endian encoding being the byte reverse of the little-endian one, the
; round trip at every offset within a word (the file writes a byte at a time so a strict-alignment
; die is served), and a narrow write leaving the bytes above it alone so packed fields do not
; clobber each other.
[env:native_endian]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_endian

; The byte verbs (mmgr/bytes.h): append into a pc_span, take out of a pc_cspan, and the
; offset-passing reads a parser walks a raw payload with. The append side keeps counting pos past
; cap on overflow, so an undersized region reports the capacity it should have had instead of only
; failing - asserted on both the single byte and the multi-byte width. The take side is checked at
; the boundary and one past it, with the sticky error and a refused read leaving both the cursor and
; the output untouched. The length-prefixed blob reads are the peer-controlled path: the prefix is a
; full u32 the peer picks, size_t is 32 bits on esp32 and c2000, and every bound here is a
; subtraction against the space that remains rather than a sum that would wrap - a 0xFFFFFFFF prefix
; is refused with the offset back where it started, and an exact fit is still accepted.
[env:native_bytes]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_bytes

; The LSB-first bit writer (mmgr/bitio.h) the DEFLATE encoder and the SSH
; zlib@openssh.com compressor both write their bitstreams through. Bits are read back by a bit
; reader written in the test, so the packing order is asserted against a decoder's walk rather than
; against the writer's own accumulator. Covers the whole-byte spill out of the accumulator, a
; single put wider than a byte, the zero padding an align puts in the high bits of a partial byte,
; an align on a boundary emitting nothing, and the capacity edge: an exact fill is not an overflow,
; a byte past cap latches it and leaves the byte past cap untouched, and an align with no room
; latches rather than writing.
[env:native_bitio]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_bitio

; The raw load (mmgr/rawmemcpy.h): bytes at a pointer read as a wider type in the
; machine's own order, and the ladder proto_raw_read steps down. Every scalar rung is diffed
; against a byte loop written in the test at every offset within a word, so the machine order is
; asserted from a runtime endianness probe rather than from a config macro. PROTO_RAW_WORD is
; checked against PROTO_WORD_BITS, which the die states, and not against the build machine's
; pointer width. The mover is run over the cross product of every destination offset, every source
; offset and lengths through two full words: the head loop aligns the destination, so the source
; offset is what selects the co-aligned branch or the funnel that joins two adjacent aligned source
; words with a shift pair. Poison outside the moved span is checked on every case, so a rung that
; writes past its width fails here rather than in the caller that borrowed the next bytes.
[env:native_rawmemcpy]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_rawmemcpy

; The bounded byte region (mmgr/span.h): a pointer and the capacity that belongs to
; it, bound together. Rule 19 converts stack arrays - whose length the compiler ties to the object -
; into borrowed regions, and the naive rewrite to a bare pointer plus a separate cap argument is
; where a 4-byte sizeof() or a mismatched capacity slips through with no diagnostic. Covers the
; extent recovered from the array type, the normalization that makes {nullptr, n} unconstructible
; so an omitted check writes nothing instead of dereferencing null, and the sub-span clamping that
; keeps a truncated frame from producing a pointer past the allocation.
[env:native_span]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_span

; The shared ring primitive (mmgr/ring.h) and its three views: bytes by head/tail, whole
; messages by segment, and claimable slots by mask. Both TCP transports and both UDP rings sit on
; this file and it had no suite of its own - it was only ever exercised as a side effect of theirs,
; which is how the wrap could move from a modulo to a mask on the strength of transport envs alone.
; Covers the power-of-two capacity the mask index requires, a wrapped entry reading back whole, a
; segment staying invisible until it is published and refusing rather than overwriting one in
; flight, and the slot view: first taker wins, ready is marked minus held, next is the lowest set,
; and a losing hold cannot redirect the wire by clobbering the winner's keepout.
[env:native_ring]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_ring

; The secure pool accessor (mmgr/secure): the SAME pool mechanism as the plaintext side
; (mmgr/arena) instantiated a second time at a disjoint address, so only what differs is
; covered here - the access and control layer. Reclaiming WIPES, and wipes before the bytes become
; available again, so a secret cannot survive its borrow or reach the next tenant; the scope guard
; makes that hold on the early-return paths a hand-written wipe gets forgotten on. And the two
; pools being disjoint regions means ownership is decided by address alone - a secure pointer can
; never be accepted where a plaintext one is required, or the reverse, with no per-allocation
; metadata. Allocator mechanics themselves belong to test_arena.
[env:native_secure_pool]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/arena.c>
    +<mmgr/plaintext.c>
    +<mmgr/secure.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_secure_pool

; Both pool accessors at PC_WORKER_COUNT=2. Slot resolution is unobservable in a single-worker
; build - pc_worker_self() is an inline compile-time 0 there, so a test cannot present any other
; identity and every borrow lands on slot 0 whatever the accessor does. Only here does the id
; become a real per-task read, so this is the only env in which a worker borrowing from its OWN
; slot, and a caller that is not a server worker landing on the ghost instead of colliding with
; worker 0, are distinguishable from each other.
[env:native_pool_workers]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_WORKER_COUNT=2
build_src_filter =
    -<*>
    +<mmgr/arena.c>
    +<mmgr/plaintext.c>
    +<mmgr/secure.c>
    +<network_drivers/session/worker.c>
    +<../core_setup/hal/portable/portable_platform.c>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_plaintext
    unit/mmgr/test_secure_pool

; Mounted storage (server/filesystem/mnt) - the backend vtable and its built-in RAM backend,
; host-tested through that backend (the Arduino FS backend is board-layer and HW-verified).
; Files, directories, stat, and the readdir cursor. Pure, no heap.
[env:native_mnt]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MNT=1
build_src_filter =
    +<server/filesystem/mnt.c>
    +<server/filesystem/filesystem.c>
    +<mmgr/frame.c>
    +<mmgr/protostr.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_mnt

; Lane math (mmgr/swar.h): one 32-bit word as four byte lanes. has-zero across
; every lane, the guard-bit range compares the base64 decoder classifies with, and the bounded
; NUL scan diffed against strnlen at every length, NUL position, and alignment.
[env:native_swar]
extends = native_base
build_src_filter =
    -<*>
    +<mmgr/protomem.c>
test_filter =
    unit/mmgr/test_swar
test_build_src = no

; The littlefs-backed pc_mnt_backend used by the host tests that need a real tree
; (test/mocks/lfs_mock.h): round-trip, seek, directory listing, stat, rename/remove,
; append, and a full volume refusing rather than pretending. Real littlefs on a RAM
; block device, so the host answers the way the device does.
[env:native_lfs_mock]
extends = native_base
build_flags =
    ${native_base.build_flags}
    -DPC_ENABLE_MNT=1
build_src_filter =
    -<*>
    +<server/filesystem/mnt.c>
    +<mmgr/protomem.c>
test_filter =
    protocols/storage/test_lfs_mock

; <<< END GENERATED TEST ENVS <<<
