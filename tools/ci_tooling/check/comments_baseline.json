[
  "history|src/network_drivers/presentation/codec/msgpack/msgpack.c|// The format byte is MessagePack's framing, so stepping over it is this codec's step. It used to",
  "history|src/network_drivers/presentation/http/http3/quic_conn.c|// keys for (so the peer can decrypt it) - and then the connection is closed. It replaces the normal",
  "history|src/network_drivers/presentation/http/http3/quic_crypto.c|// huge size_t rather than fail. This guard used to live inside protocore_aes128gcm_open().",
  "history|src/network_drivers/presentation/http/http3/quic_crypto.c|// length and the tag pointer separately, so it no longer has a combined length to range-check on",
  "history|src/network_drivers/presentation/http/http_parser/http_parser.c|// bit, instead of the range compares + 15-case switch it replaces:",
  "history|src/network_drivers/presentation/security/dtls/dtls_record.c|// detached-tag api no longer range-checks a combined length on the caller's behalf, so this would",
  "history|src/network_drivers/presentation/ssh/connection/ssh_forward.c|// The dispatch loop now polls every slot uniformly; it used to poll only ACTIVE slots, so preserve",
  "history|src/network_drivers/presentation/ssh/transport/ssh_dh.c|// overwritten, after which the outgoing mode is no longer knowable.",
  "history|src/server/core/worker.h|* Each worker blocks between service iterations (it no longer free-runs the poll),",
  "history|src/network_drivers/transport/tcp/tcp_conn.c|// remotely-triggerable panic under connection churn. Skip if the slot no longer owns a live pcb",
  "history|src/network_drivers/transport/tcp/tcp_conn.c|// same way) - callers no longer thread it through, so it cannot disagree.",
  "history|src/protocore_config.h|* The worker no longer free-runs a poll: it blocks on a task notification and a",
  "history|src/server/io/response.c|// the server object they used to hang off, which is why the readers below are functions: a",
  "history|src/server/storage/hotswap/hotswap.c|// through a handle to a card that is no longer there.",
  "history|src/server/web/dashboard/dashboard_routes.c|// its own schedule, so it can arrive first. This used to be inferred from a stored server",
  "history|src/server/web/edge_cache/edge_cache_proxy.c|// is no longer readable there - the window must be resolved against this captured copy.",
  "measurement|src/crypto/aead/aes128gcm.h|* worth of records, but building a cipher context and tearing it down costs ~9,200 cycles on an",
  "measurement|src/crypto/asymmetric/ecdsa.c|// Measured (crypto bench): the S3 P-256 MODMULT path's ~14% -O3 win is carried by -fpeel-loops (bisected",
  "measurement|src/crypto/cipher/chacha20.c|// (bisected on-device); pin just that transform on the -O2 floor. The P4 takes full -O3 via the per-die defau",
  "measurement|src/crypto/cipher/chacha20.c|// Measured (crypto bench): ChaCha20's additional ~8.8% S3 win at -O3 is carried entirely by -funswitch-loops",
  "measurement|src/crypto/cipher/chacha20.c|// the arduino framework's -Os, and ChaCha runs ~2.36x faster at -O2 (measured on-device, CCOUNT). It is",
  "measurement|src/crypto/crypto_opt.h|// MEASURED to carry a TU's win - capturing it without -O3's code-size / miscompile / cache-thrash baggage. A ",
  "measurement|src/crypto/crypto_opt.h|// Per-variant default, measured on the crypto bench (main_cryptobench). The ESP32-P4 (RISC-V) is faster or fl",
  "measurement|src/crypto/mac/hmac_sha256.c|// class (bisected on-device: -fpeel-loops and -funswitch-loops both no-op), not a single transform, and not",
  "measurement|src/crypto/mac/poly1305.c|// path on the S3 and runs materially faster than the framework -Os; it is constant-time by structure",
  "measurement|src/network_drivers/application/mdns_adaptive/mdns_adaptive.c|// announcing slower than that makes the device silently undiscoverable - the opposite of the",
  "measurement|src/network_drivers/datalink/roaming.h|///< measured signal strength (dBm; more negative is weaker)",
  "measurement|src/network_drivers/presentation/codec/base64/base64.c|// timing-invariance measured on the ESP32-S3 (CCOUNT does not vary with the input bytes).",
  "measurement|src/network_drivers/presentation/http/http3/quic_crypto.c|// costs ~556 cycles per packet plus a pool borrow and wipe. (The ECB block itself is ~7,842 - a",
  "measurement|src/network_drivers/presentation/http/http3/quic_crypto.c|// would cost ~8,400 cycles to encrypt sixteen bytes.",
  "measurement|src/network_drivers/presentation/http/http3/quic_crypto.h|///< Built once: ~556 cycles per record, plus the pool",
  "measurement|src/network_drivers/presentation/http/http_parser/http_parser.c|// bytes before n is even measured, capping bracket content at 43 - short of the 46",
  "measurement|src/network_drivers/presentation/security/dtls/dtls_record.c|// costs ~556 cycles per record plus a pool borrow and wipe, independent of record size.",
  "measurement|src/network_drivers/presentation/ssh/connection/ssh_client.c|// (esp_ecc_point_multiply / esp_ecdsa_verify), HW-measured ~5 ms/point-mul. curve25519/ed25519 and",
  "measurement|src/network_drivers/presentation/ssh/transport/ssh_keymat.h|// ~9,200 cycles on an ESP32-S3, a fixed price per packet that dominates small interactive traffic",
  "measurement|src/network_drivers/presentation/ssh/transport/ssh_packet.c|// (ssh_dh.cpp) and reused per packet, because standing one up costs ~9,200 cycles regardless of packet",
  "measurement|src/network_drivers/presentation/ssh/transport/ssh_transport.c|// Reset the re-key timer: the volume/time budget is measured from this completed KEX.",
  "measurement|src/network_drivers/tls/tls.c|//     is far faster than x25519 (which stays software). Measured on an ESP32-P4: P-256 ECDHE ~10 ms vs",
  "measurement|src/network_drivers/tls/tls.c|// a P-521 one runs ~2.4x a P-256/x25519 one in software. Measured end-to-end on an ESP32-S3 (full TLS 1.2",
  "measurement|src/network_drivers/transport/tcp/tcp_conn.c|//                   (measured 20 vs 71 cyc on the S3). Atomic: CONN_FREE is written from the stack",
  "measurement|src/protocore_config.h|*     arithmetic in radix-2^16) peaks at ~10.5 KB (measured on an ESP32-S3): the",
  "measurement|src/protocore_config.h|*     modexp) uses ~7 KB (measured on a DevKitV1).",
  "measurement|src/protocore_config.h|* (measured on an ESP32-P4: P-256 ECDHE ~10 ms vs x25519 ~132 ms, and the full TLS handshake ~29 ms",
  "measurement|src/protocore_config.h|* (protocore_hwhealth_cap_leak: measured vs expected RC decay). No heap/stdlib. Default off.",
  "measurement|src/protocore_config.h|* Measured cost on an ESP32-S3 at 240 MHz: ~52 cycles per pool entry point, and a",
  "measurement|src/protocore_config.h|* P-256 through the HW via `ecc_alt`), P-256 is dramatically faster than x25519, which stays software",
  "measurement|src/protocore_config.h|* both are constant-time; measured on the ESP32-S3, SWAR is ~1.9x faster than the scalar path and 5.36x",
  "measurement|src/protocore_config.h|* carrier WIDER than the register is synthesized from halves and is measurably slower than the",
  "measurement|src/protocore_config.h|* faster than mbedTLS (882 vs 1639 vs 4728 cyc on a credential), at 0.00-cycle input-dependent variance.",
  "measurement|src/protocore_config.h|* from the measured SD envelope (docs/FEATURE_PERFORMANCE.md): append sequentially in ~32 KiB pages,",
  "measurement|src/protocore_config.h|* governor oscillates no matter how correct the hysteresis is. Measured on an ESP32-S3: dropping",
  "measurement|src/protocore_config.h|* handshake fails cleanly (no corruption). Measured peak for ONE ECDSA P-256",
  "measurement|src/protocore_config.h|* interpolation rate (typically 125 / 250 Hz) and the robot answers each command with its measured",
  "measurement|src/protocore_config.h|* mark + alloc + release touches three of them, so roughly 156 cycles on every borrow. Worth paying",
  "measurement|src/protocore_config.h|* only). Measured with the pluggable clock (protocore_millis()).",
  "measurement|src/protocore_config.h|// ladder and 64-bit index arithmetic - a shape no target executes, measured on a machine that does",
  "measurement|src/protocore_config.h|// record costs ~556 cycles plus a pool borrow and wipe. (The ECB block it protects is ~7,842 cycles on",
  "measurement|src/protocore_config.h|// same reason as the AEAD contexts, though the win is smaller: measured on an S3, rebuilding it per",
  "measurement|src/protocore_config.h|// sequential write unit (the measured durable-throughput knee)",
  "measurement|src/protocore_config.h|// \u2502                              \u2502   The contexts buy ~9,200 cycles per packet - a FIXED cost   \u2502          \u2502",
  "measurement|src/server/clock/clock.h|* it spins on the same clock. Measured against ::protocore_millis, so a custom clock governs it too.",
  "measurement|src/services/energy/iec60870/iec60870.h|*        pulse total) companion to the measured values.",
  "measurement|src/services/energy/iec60870/iec60870.h|// are shared: the low bit is value-specific (SPI for a single point, OV overflow for a measured value),",
  "measurement|src/services/energy/iec60870/iec60870.h|///< measured value, normalized",
  "measurement|src/services/energy/iec60870/iec60870.h|///< measured value, scaled",
  "measurement|src/services/energy/iec60870/iec60870.h|///< measured value, short float",
  "measurement|src/services/energy/iec60870/iec60870.h|///< measured-value overflow (QDS bit 0)",
  "measurement|src/services/iot/redis_resp/redis_resp.c|// several times faster than snprintf on the ESP32-S3, where this was the dominant cost of encoding a",
  "measurement|src/services/machine_tool/fanuc_j519/fanuc_j519.h|///< measured Cartesian pose (world -> tool0).",
  "measurement|src/services/machine_tool/fanuc_j519/fanuc_j519.h|///< measured joint angles.",
  "measurement|src/services/machine_tool/mtconnect/mtconnect.h|///< a measured value (<Samples>).",
  "measurement|src/services/storage/wal/wal.c|// faster than the bit-by-bit form on the ESP32-S3, where the WAL write path is CRC-bound (~4.4 MB/s ->",
  "measurement|src/services/timing_position/gnss/gnss_survey.c|// on the polar axis: latitude is +/-90, height is measured along z",
  "measurement|src/services/timing_position/nmea2000/nmea2000.h|///< measured pressure (Pa, 0.1 Pa/bit, signed)",
  "meta|src/crypto/cipher/aes256ctr.c|// per-vendor constant that belongs in core_setup/ (see the handover) - stated here for now.",
  "meta|src/network_drivers/presentation/http/http3/h3_conn.c|// ignore regular headers for now (routing is by method + path)",
  "meta|src/server/web/dashboard/dashboard_routes.c|// to infer it from, and \"has this service started\" was always the real question.",
  "meta|src/server/web/edge_cache/edge_cache_proxy.c|// `registered` is the whole test now: it was always the real question, and the stored server",
  "tradeoff|src/protocore_config.h|* (fewer cross-thread protocore_conn_send marshals per KB) at the cost of RAM per concurrent relay",
  "tradeoff|src/protocore_config.h|* while chasing a memory bug; not worth shipping."
]
